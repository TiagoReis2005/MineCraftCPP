---
name: minecraftcpp-project
description: "MineCraftCPP — from-scratch Vulkan voxel/Minecraft clone; decisions, stack, and current milestone progress"
metadata: 
  node_type: memory
  type: project
  originSessionId: cbb7935b-588e-4f3b-ac97-25d32ddf69d3
---

Building a Minecraft clone from scratch in C++ at `c:\Users\Jaime Reis\Desktop\MineCraftCPP`.
Full staged plan: `C:\Users\Jaime Reis\.claude\plans\i-want-you-to-peaceful-oasis.md`.

**Locked decisions (from Q&A):**
- Renderer: **Vulkan 1.3** (dynamic rendering + synchronization2; user chose it over OpenGL knowingly). Priorities: rendering techniques + threaded meshing.
- Toolchain: **MSVC Build Tools 2022 + CMake**. Deps via CMake FetchContent (GLFW, GLM; VMA+stb added at M2). Vulkan SDK gives headers/loader/validation/glslc.
- Scope: "go bigger" — creative MVP + multi-chunk streaming + world save/load.
- Game modes: Creative, Survival, Spectator.
- Textures: user manually drops PNGs in `assets/textures/blocks/` and `items/`, loaded by filename into a texture array (magenta fallback for missing). Block inventory icons rendered as **3D isometric cubes** like Minecraft; flat items as 2D sprites.

**Environment:** machine has NO compiler/CMake/Vulkan SDK yet — only Git, VS Code, winget (Windows 11 x64). M0 install commands are in README.md and the plan.

**Progress (as of 2026-06-30):** M0 done. **M1 code-complete** (all files written: VkContext/Swapchain/Renderer .cpp + main.cpp). Color-only swapchain (no depth yet — depth added in M2). Uses Vulkan 1.3 core dynamic rendering + sync2 + vkQueueSubmit2; no volk (links Vulkan::Vulkan loader directly). Per-frame image-available semaphore + fence (2 frames in flight); per-swapchain-image render-finished semaphore. Shaders compiled by CMake via glslc, copied next to exe with assets/.

**BLOCKED ON USER:** machine still has no toolchain. User must run the 3 winget installs (README/plan), reopen shell, then `cmake -S . -B build -G "Visual Studio 17 2022" -A x64` + `cmake --build build --config Debug`, run `build\bin\Debug\MineCraftCPP.exe`. Expected: sky-blue window with RGB triangle, resizable, no validation errors. Verify this BEFORE building M2 on top.

**M1 VERIFIED clean on AMD Radeon 780M** (Vulkan SDK 1.4.350, MSVC 19.44). Fixed: removed deprecated device-layer enabling in vkCreateDevice.

**M2 code-complete (awaiting user build).** Added: VMA (via ${vma_SOURCE_DIR}/include, NOT the CMake target) + stb (FetchContent); VkContext now owns VmaAllocator + immediateSubmit(); Swapchain has D32_SFLOAT depth; gfx/GpuBuffer (AllocatedBuffer + host/device buffer helpers); gfx/TextureArray (scans assets/textures/blocks/*.png by filename into R8G8B8A8_SRGB 2D array, auto-detects size, hash-colored placeholder per missing file, NEAREST sampler, single mip); world/Block+BlockRegistry+Section(16^3)+Mesher (face-cull, per-face shading, ChunkVertex pos/uv/layer/light); core/Camera (fly WASD+mouse, proj[1][1]*=-1); Renderer rewritten with depth + descriptor set (UBO binding0 + sampler2DArray binding1) + push-constant chunkPos + chunk pipeline (cullMode NONE for M2). shaders/chunk.{vert,frag}. main builds one heightmap test chunk. Controls: WASD/mouse/Space/Shift/Ctrl-boost/ESC. Default block texture names: grass_top, grass_side, dirt, stone, sand, oak_log, oak_log_top, oak_leaves.

Build: re-run `cmake -S . -B build -G "Visual Studio 17 2022" -A x64` (downloads VMA+stb) then `cmake --build build --config Debug`. Known deferrals: back-face culling (winding) deferred to M3; mipmaps to M6.

**M2 VERIFIED** on AMD 780M — user dropped real PNGs (grass/dirt/stone), terrain renders correctly, 8388 indices. Fixed D9025 by removing global /W3 (kept /permissive-; /w stays on vendored TUs).

**M3a code-complete (awaiting user build).** Split M3 into M3a (streaming, single-threaded budgeted) + M3b (threading). Added: world/Noise (Perlin+fbm), world/Chunk (column 16x128x16, BlockId vector, generated/meshed flags, ChunkMesh), world/WorldGen (heightAt fbm, base48 amp22, grass/dirt/stone + sand below y40), world/World (ChunkMap keyed ChunkCoord, ringOffsets sorted nearest-first, update(): gen budget 8/frame to renderDistance+1, mesh budget 8/frame to renderDistance requiring 4 neighbors generated, unload beyond +4 via deferred-destroy queue 3 frames, getBlock with floorDiv). Mesher: replaced Section buildMesh with buildColumnMesh(center,E,W,S,N) neighbor-aware (direct-index blockAt, no map lookups) — culls chunk-boundary faces. Renderer.drawFrame now takes (Camera,World), iterates chunks, per-chunk push-constant offset (cx*16,0,cz*16). main: World world; world.init(&registry); camera at (0,80,0). renderDistance=6 default. Section.h now unused (kept, harmless). Build: re-run cmake configure (3 new sources) then build.

Known deferrals still open: back-face culling (cullMode NONE), frustum culling (draws all loaded chunks — fine at RD6 on 780M), mipmaps. immediateSubmit per-mesh-upload on main thread (M3b moves to worker meshing + batched upload).

**M3a VERIFIED** working (streamed but stuttered the farther you flew — main-thread meshing). User then requested **32³ cubic chunks**. So pivoted: replaced columns with cubic chunks + threading in one step ("M3-cubic").

**M3-cubic code-complete (awaiting user build).** Now: world/Chunk = 32³ cube at (cx,cy,cz) with std::atomic<int> state (CHUNK_EMPTY/GENERATING/GENERATED/MESHING/READY). core/ThreadPool (hw_concurrency-1 workers). World: ChunkCoord{x,y,z} map of shared_ptr<Chunk>; kVerticalChunks=4 (128 tall); update() = streamChunks (create+enqueue generate to renderDistance+1, all cy) / scheduleMeshing (when self GENERATED + horizontal neighbors generated → set MESHING, enqueue buildCubeMesh on worker) / collectAndUpload (drain completed_ queue under mutex, BATCHED single immediateSubmit for up to 8 meshes/frame, guard: only assign if chunk still MESHING) / unloadDistant (renderDistance+4, deferred-destroy 3 frames) / processDeferredDestroys. Mesher: buildCubeMesh(self,+X,-X,+Y,-Y,+Z,-Z) 6-neighbor cubic. WorldGen per-cube (base56 amp26). GpuBuffer: added createDeviceBuffer (no-copy) for batched staging. Renderer iterates READY chunks, push-constant offset (cx,cy,cz)*32. main camera at (0,100,0). renderDistance=5 cubes default.
Threading model (verified-by-reasoning safe): workers only touch captured shared_ptr chunks (immutable once GENERATED) + push to mutex-guarded completed_; main owns the map + all Vulkan/VMA calls; release/acquire on state + pool-queue mutex give happens-before for neighbor block reads. Pool.stop drops queued tasks, joins. Section.h orphaned/unused.

**M3-cubic VERIFIED** — stutter completely gone, can fly far smoothly, terrain good, no warnings/crashes. User asked for smoother terrain (deferred full biomes).

**M4a code-complete (awaiting user build).** (1) Smoother worldgen: base64/amp24, low-freq base fbm(0.006,4oct) + small detail fbm(0.03,2oct)*0.15. (2) Block break/place: world/Raycast (Amanatides-Woo DDA, returns hit block + face normal), World::setBlock (edits READY chunk, sets dirty + marks boundary neighbors dirty). main: raycast from camera.front() reach 6, left-click break→Air, right-click place→selected at block+normal, keys 1-6 select Grass/Dirt/Stone/Sand/Wood/Leaves (printed). (3) No-flicker re-mesh: refactored Chunk state machine — REMOVED CHUNK_MESHING, now enum {EMPTY,GENERATING,GENERATED,READY} + atomic<bool> meshInFlight + dirty. scheduleMeshing handles BOTH initial (GENERATED) and remesh (READY&&dirty) via meshInFlight guard; re-meshed chunk stays READY (draws old mesh) until new ready. collectAndUpload validates via meshInFlight, defer-destroys OLD mesh when swapping. Known benign caveat: editing a chunk that is mid-remesh races a single aligned uint16 (corrected by follow-up dirty re-mesh) — acceptable on x86.

**M4a VERIFIED** — smoother terrain, responsive break/place, no flicker, no border holes, clean.

**M4b code-complete (awaiting user build).** Camera slimmed to view-only (position/yaw/pitch + addLook/front/forwardXZ/rightXZ/view/projection; input moved out). New game/Player (owns Camera + GameMode {Creative,Survival,Spectator}): handleLook (mouse), handleModeSwitch (key G cycles, prints), updateFly (Creative+Spectator: free 3D flight, no collision), updateSurvival (instantaneous horizontal walk via forwardXZ/rightXZ, gravity 28, jump 9 when onGround, terminal 60, per-axis AABB collision via collidesAt iterating voxels in box halfWidth0.3/height1.8/eye1.62 vs world.getBlock!=Air). canEdit()=mode!=Spectator. main: Player replaces Camera; spawn (0,100,0) Creative; edits gated by canEdit; drawFrame(player.camera). Ctrl=boost(fly)/sprint(walk). Simplification: Creative flies through terrain (no collision) — only Survival collides; can refine later.

**M4b VERIFIED w/ 3 fixes requested → M4c code-complete (awaiting build).** Fixes: (1) Creative now COLLIDES (updateFly(collide) param; Creative collide=true fly-but-stop-at-blocks, Spectator collide=false noclip). (2) Jump "creep" (0→2→0.1→0) fixed: survival Y resolution now SNAPS flush to surface (land: eye.y=floor(feet)+1+eyeHeight; ceiling: floor(head)-height+eyeHeight) instead of reverting; collidesAt shrinks box by 1e-3 so the snapped boundary isn't double-counted. (3) Scroll wheel hotbar: Window has scrollCallback + consumeScroll(); main uses hotbarIndex (0-5) set by keys 1-6 OR scroll (up=prev), selectedBlock=kHotbar[idx]. Also dt clamped to 0.1 in main. Survival has no fall damage/health (intended for now). No new source files this round (rebuild only, no reconfigure).

**M4c partial — user feedback round 2 → M4d code-complete (awaiting build).** Fixes: (1) Jump less floaty: kGravity 28→32, kJumpSpeed 9→8.6 (~1.15 blk). (2) Creative redesigned per user: now walks by default (gravity+collision like survival), DOUBLE-TAP SPACE toggles flight (handleFlyToggle, 0.30s window); creative flight = horizontal WASD (forwardXZ, NOT look-dir) + space up + shift down + collision, no gravity. Player.flying default true (spawn hovers while terrain loads); handleModeSwitch sets flying=(mode==Creative). Spectator stays look-dir noclip (moveFly lookDir=true). Refactored: moveWalk (survival + creative-walk) + moveFly(collide,lookDir). (3) Placement guard: Player::collidesWithBlock(ivec3) AABB test; main skips right-click place if block would overlap player. Rebuild only (no new files/CMake change).

**USER REQUESTED FEATURE (not yet built): block shapes** — full/stairs/slabs(half)/pillars(logs) like real MC. This is a block-model system: BlockDef gains a shape; Mesher emits per-shape geometry (slab=half box, stairs=composite, cross=plants, pillar=axis textures) and per-shape occlusion (slab only occludes one face → culling logic gets more complex); collision AABB per shape; placement picks orientation (slab top/bottom from hit.y, stairs from facing). Logs ALREADY render pillar-like (top/side textures, just no sideways-placement rotation). Slotted as a milestone — ask user whether before/after HUD+beauty.

**M4e (feedback round 3, code-complete, rebuild only):** (1) Jump height → ~1.25 blocks (1 block+4px): kJumpSpeed 8.6→8.8 (g=32; 8.8 chosen below continuous-1.25 to offset semi-implicit Euler overshoot). (2) Sprint/fly-boost now LATCHED: added Player.sprinting_ bool — set true on Ctrl, false when not moving (wish/move ~0); moveWalk uses kSprint, moveFly uses kFlyBoost when sprinting_. (3) Creative fly AUTO-LANDS: moveFly collide branch sets flying=false when delta.y<0 and Y-collision (descended onto ground). NOTE: interpreted user's "fly turns off when hitting ground" as a request for auto-land — confirm; if they meant the opposite (stay flying), remove that one line.

**M4f (feedback round 4, code-complete, rebuild only):** (1) Crouch (Shift in walk modes = survival + creative-walk): Camera.crouchOffset lowers RENDER eye only (view() subtracts it; physics/body-box unchanged at eyeHeight 1.62) smoothed via approach(); slows move (kSneakFactor 0.35); EDGE PROTECTION via hasGroundBelow() — while crouching+grounded, horizontal moves that leave footprint unsupported are reverted. (2) Spectator now uses SAME movement as creative fly: moveFly lost lookDir param, always forwardXZ (horizontal WASD + space up + shift down); spectator = collide=false (noclip) + always flying (handleFlyToggle only in Creative). (3) Sprint FOV: applyCameraEffects lerps camera.fovDeg to kBaseFov(70)+kSprintFov(12) while sprinting_. Can't sprint while crouching. **User said "after that start the m5" — M5 is the agreed next milestone (do after this build verifies).**

**M4g (round 5, rebuild only):** sprint cancels on horizontal collision (sprinting_=false in moveWalk + moveFly x/z collide branches). Hold-to-break/place: main uses breakCooldown/placeCooldown (kActionRepeat 0.2s) instead of edge detection; release resets cooldown to 0 for instant next click. ALL movement/mode feedback now addressed.

**Next: M5 (START HERE — detailed plan below).** HUD + 3D inventory block icons. Split M5a (2D UI) then M5b (3D icons). NOTE: do NOT bundle with unverified movement — but M4g tweaks are trivial; if already building, proceed.

**M5a CODE-COMPLETE (awaiting build; this build also includes unverified M4g tweaks).** Files created: shaders/ui.{vert,frag}; gfx/UIRenderer.{h,cpp} (1x1 white tex R8G8B8A8_UNORM, single descriptor set, alpha-blend pipeline w/ depth OFF but declares sc depthFormat to match pass, per-frame host vbuf 6*8192, quad()/texQuad()/beginFrame()/recordDraw()); ui/Hud.{h,cpp} (crosshair + hotbar bar slot 50px + selected border). Renderer.drawFrame/recordCommandBuffer now take UIRenderer* and call ui->recordDraw(cmd,currentFrame_) after chunk loop, before vkCmdEndRendering. main creates ui+hud, per frame ui.beginFrame(extent)+hud.build(...,hotbarIndex,kHotbarCount), drawFrame(cam,world,&ui); ui.cleanup() after renderer.cleanup(). Added UIRenderer.cpp+Hud.cpp to CMake SOURCES. NOTE Renderer.kFramesInFlight==UIRenderer.kFramesInFlight==2 (currentFrame_ indexes vbuf_). Build = reconfigure (new sources) + build. Verify: crosshair center + hotbar bar bottom, white highlight follows number keys/scroll.

**M5a+outline CODE-COMPLETE (awaiting build; bundles unverified M4g + M5a).** Added since M5a: (a) BLOCK OUTLINE — shaders/outline.{vert,frag} (unit-cube 12-edge LINE_LIST, inflate eps 0.003, soft black a=0.4, depthTest LESS_OR_EQUAL write OFF, blend); Renderer has outlinePipeline_ (REUSES pipelineLayout_ = chunk's UBO set + vec4 push) + outlineVbuf_ (24 verts); drawFrame + recordCommandBuffer now take (bool hasTarget, glm::ivec3 targetBlock); drawn after chunks before UI; main passes hit.hit/hit.block. (b) Cobblestone block (BlockId + registry, user added cobblestone.png). (c) Hotbar → 9 slots (kHotbar: Grass/Dirt/Stone/Cobblestone/Sand/Wood/Leaves + 2 Air"-"); placement skips Air. Build = reconfigure + build.

**M5b CODE-COMPLETE (awaiting build; new source ItemIconRenderer.cpp -> reconfigure).** 3D block icons in hotbar: ui/ItemIconRenderer renders each non-Air block as an isometric cube (64px) into an offscreen R8G8B8A8_SRGB image via REUSED chunk.vert/frag (own descriptor UBO{view,proj}+block-array sampler, own pipeline declaring SRGB+D32, iso view rotateX30*rotateY45, ortho -0.9..0.9 proj[1][1]*=-1, cube centered -0.5..0.5, per-face light). One immediateSubmit renders all (shared depth + inter-icon depth barrier). UIRenderer gained registerImageView(view) (owned=false, not freed on UI cleanup) + kMaxTextures 32. main: iconRenderer.init(&ctx,&textures,&registry,&ui); slotIcons[9]=iconTexId per hotbar block (-1 for Air); hud.build(...,slotIcons,kHotbarCount) draws sprite in each slot (16px content @3x). Cleanup: ui.cleanup() then iconRenderer.cleanup(). Also: outline now opaque near-black (frag a=1.0) + thinner (t=1/64). If icon shows bottom not top, flip rotateX sign.

**BIG REFACTOR (2026-07-01, rebuild only) — DATA-DRIVEN BLOCK REGISTRY + back-face culling.** Per user (tired of editing 3 files per block): (1) BlockId is now `uint16_t` runtime handle (NOT an enum) + `constexpr BLOCK_AIR=0`. All `BlockId::X` removed. (2) BlockDef has a FLUENT BUILDER (.named/.allFaces/.faces/.column/.notSolid/.transparent/.cullsSelf/.slab/.cross) + new field `cullSameNeighbor`. (3) BlockRegistry::add(BlockDef)->BlockId (auto-id), registerDefaults() = ONE add() line per block (the ONLY place to add a block); named handles grass/dirt/stone/sand for worldgen; count(). (4) WorldGen ctor now takes (seed, const BlockRegistry&), stores grass_/dirt_/stone_/sand_; World::init passes *registry. (5) main hotbar AUTO-POPULATES from registry (first kMaxHotbar=9 non-air blocks) — no main edit to add a placeable block; prints registry.def(id).name. (6) ItemIconRenderer icons_ now std::vector sized reg->count(); iconTexId bounds-checked. **Adding a block = ONE line in BlockRegistry::registerDefaults.** 
**LEAVES fix:** cullSameNeighbor=false for leaves (keep leaf-to-leaf faces so you see leaves behind), true for glass (glass-to-glass culls). Enabled BACK-FACE CULLING in chunk pipeline (Renderer): cullMode=BACK, frontFace=COUNTER_CLOCKWISE. IMPORTANT FIX: the kFace table (Mesher.cpp) had INCONSISTENT winding — +X/-X faces were wound CW-from-outside (inward normals) while +Y/-Y/+Z/-Z were CCW; symptom was east/west faces missing with culling on. Reversed the +X and -X vertex arrays so all 6 faces are CCW-from-outside (normals verified). Now culling works. Icon pipeline stays cullMode NONE.

**F3 DEBUG OVERLAY (2026-07-01, reconfigure — new sources).** Press F3 to toggle. Bitmap FONT: ui/Font (loads assets/textures/font/ascii.png 128x128 = 16x16 grid of 8x8 glyphs; measures per-glyph widths from alpha for proportional text; drawText with shadow via UIRenderer). UIRenderer gained public texQuad(texId,x,y,w,h,u0,v0,u1,v1,color) for glyph sub-UVs. core/SystemStats (Windows psapi): processMemoryBytes (WorkingSet) + cpuUsagePercent (GetProcessTimes, stateful, %of-one-core). VkContext: deviceName (from props) + gpuMemoryUsedBytes() (vmaCalculateStatistics). World: loadedChunkCount()/readyChunkCount(). ui/DebugOverlay + DebugInfo struct: LEFT col = version/fps/XYZ/Block/Chunk/Facing/chunks loaded+rendered; RIGHT col (right-aligned) = gpuName/Display/GPU mem/Alloc rate/RAM/CPU%; TARGET panel (RIGHT side, right-aligned, below system stats; header "Targeted Block: x,y,z" UNDERLINED via ui.quad) + name + face(from hit.normal) + tags(shape,opaque/transparent,culls_self). Player kFlySpeed = kWalkSpeed*2 (=9.4, base fly = 2x walk; Ctrl still 3x boost). main: F3 edge toggle, smoothed fps, prevGpuMem for alloc rate, builds DebugInfo when on. Font texId registered after hotbar/selection (id 3), UIRenderer kMaxTextures=32. Fonts available: ascii/accented/ascii_sga/asciillager/nonlatin_european.

### USER ASSETS DROPPED (2026-07-01) + REQUESTS (do on the plan, NOT all now):
- assets/models/player.json = Bedrock geometry: geometry.npc.steve (WIDE, 4px arms) + geometry.npc.alex (SLIM, 3px arms) + geometry.cape; 64x64 tex; bone hierarchy root>waist>body>head/arms/legs + outer layers (hat/jacket/sleeves/pants inflate). 
- assets/textures/entity/player/{slim,wide}/*.png = 9 skins each (alex,ari,efe,kai,makena,noor,steve,sunny,zuri), 64x64.
- assets/textures/gui/sprites/hud/hotbar.png (182x22) + hotbar_selection.png (24x23) + heart/food/armor/air sprites (survival HUD later).
- **DONE (rebuild only): thick outline + sprite hotbar + inverting crosshair.** (1) Outline now THIN BARS not lines: Renderer::createOutline builds 12 edge boxes (thickness t=1/48 block, 432 tri verts), TRIANGLE_LIST, outlineVertexCount_; outline.vert scales from block center by inflate 0.002. (2) UIRenderer REWRITTEN multi-texture: registerTexture(png)->id via stbi (SRGB), createTexture, per-quad DrawCmd{texId,invert,first,count} merged runs, one descriptor set per texture (pool 16), TWO pipelines pipeline_(alpha) + invertPipeline_(invert blend ONE_MINUS_DST_COLOR/ONE_MINUS_SRC_COLOR). API quad/sprite(texId)/invertQuad. (3) Hud uses hotbar.png(182x22)+hotbar_selection.png(24x23) scaled 3x bottom-center, sel at barX+(-1+20*slot)*3; crosshair = 3 invertQuads (H bar + 2 V bars, no center overlap). main registers hudTex.hotbar/selection, hud.build(...,hudTex). If sel misaligned, tweak the -1 offsets.
**M7a CODE-COMPLETE (rebuild only): block shapes + cutout leaves.** Block.h: BlockShape enum {Cube,SlabBottom,SlabTop,Cross} + BlockDef.shape; BlockId +StoneSlab +CobblestoneSlab (Count=10). Registry: leaves.opaque=false (cutout, oak_leaves.png has alpha=0 gaps, chunk.frag already discards a<0.5); StoneSlab/CobblestoneSlab shape=SlabBottom opaque=false. Mesher: added appendQuad/emitCubeFace/emitSlab(yLow,yHigh,drawBottom; side uv samples matching vertical band)/emitCross; buildCubeMesh dispatches on shape; NEW cull rule for cubes: hidden = isOpaque(nid) || nid==id (so leaves cull leaf-to-leaf but draw vs air, and neighbors draw faces toward leaves); slab bottom face culled only if block below opaque; added buildSingleBlock(reg,id) (all faces, no cull, 0..1) used by ItemIconRenderer.buildCube (centers -0.5) so slab ICONS show slabs. Hotbar now 9 real blocks incl 2 slabs. KNOWN: slab collision still full-block (Player::collidesAt treats non-Air as full cube) — per-shape AABB later. **GLASS added (2026-07-01):** generated placeholder assets/textures/blocks/glass.png (16x16 opaque light frame + transparent center + faint diagonal highlight, user can replace); BlockId::Glass (opaque=false, Cube, tex "glass"); the existing cull rule (isOpaque(nid)||nid==id) already gives MC glass behavior — solid neighbors keep faces (seen through glass), glass-to-glass culls. Cutout via chunk.frag discard (frame opaque, center a=0). Hotbar slot 9 now Glass (dropped CobblestoneSlab from hotbar; still registered). Count=11. DEFERRED: true translucent/glass/water blend pass (needs 2nd pipeline + mesher split into opaque+translucent meshes, back-to-front) — do with M6 water; trees in worldgen (cross-cube feature placement) to showcase leaves.

**PLAYER Stage A DONE (2026-07-01, reconfigure — new PlayerSkin.cpp).** F5 perspective: Camera has enum Perspective{First,ThirdBack,ThirdFront} + member; view() offsets in 3rd person (dist 4, lookAt eye±front*dist -> eye). Player::handlePerspective (F5 edge cycles) called in update. Raycast/targeting still from real eye+front (crosshair approximate in 3rd person — MC does same; no camera-wall collision yet). game/PlayerSkin (pickPlayerSkin(playerDir)): custom override (entity/player/custom/*.png, slim if name has "slim"/"alex"), else RANDOM from entity/player/{slim,wide}/ (slim=Alex 3px arm, wide=Steve 4px); returns {texturePath,slim,name}; prints pick. main calls it (playerSkin used by Stage B model). **PLAYER Stage B CODE-COMPLETE (2026-07-01, reconfigure — new files).** game/PlayerModel (ModelVertex{pos,uv,shade}, buildPlayerModel(slim): addBox with Bedrock box-UV per part — head(-4,24,-4)8³ uv0,0; body(-4,12,-2)8x12x4 uv16,16; rightArm(-4-armW,armY)armWx12x4 uv40,16; leftArm(4,armY) uv32,48; rightLeg(-4,0)4x12x4 uv0,16; leftLeg(0,0) uv16,48; slim armW=3 armY=11.5 else 4/12; pixel space feet@y0, 32px tall). shaders/model.{vert,frag} (skin sampler2D + shade, discard a<0.1). gfx/ModelRenderer: loads skin 2D SRGB (stb, fallback 2x2 white), own pipeline (cullMode NONE, depth LESS write) + descriptor (binding0 UBO SHARED from Renderer.cameraUBOs(), binding1 skin), push-constant mat4 model; record(cmd,frame,modelMatrix) after chunks in Renderer pass. Renderer: cameraUBOs() getter, drawFrame/recordCommandBuffer +ModelRenderer*+mat4+visible. Player::feetPosition()=eye-(0,1.62,0). main: buildPlayerModel(playerSkin.slim), modelRenderer.init(...,renderer.cameraUBOs(),skinPath,data); modelMatrix=translate(feet)*rotateY(radians(-yaw-90))*scale(1/16); modelVisible=perspective!=First; cleanup modelRenderer BEFORE renderer. **Stage B VERIFIED** — body right spot, facing correct. Fixed: model scale 1/16→1.85/32 (1.85 blocks tall, head@1.62 eye). 3rd-person camera collision: Camera.thirdPersonDist (view() uses it); Player::updateThirdPersonCamera raycasts eye→camDir (0.1 step, margin 0.2, min 0.3) vs world.getBlock, called end of update. (box-UV face orientation seemed OK to user — not flagged.) **Stage C = animations + 1st-person arms + head pitch.**

**PLAYER Stage B FIXES (2026-07-01, reconfigure — builds clean) — 3 user requests:** (1) **Arm/skin mirror fix:** the box-UV was horizontally mirrored (verified against MC's real head net: region left of the face [0,8] is the character's RIGHT side, so on the front face +X must map to MAX-U; my old code had +X→min-U). Invisible on symmetric Steve body/legs but flipped custom-skin arms. FIX in PlayerModel.cpp addBox: reversed U on the 4 "band" faces (front/back/east/west) — swapped the u0/u1 args; top/bottom already consistent. Seams re-derived & verified. (2) **Overlay layer added:** addBox gained `inflate` param (default 0) growing geometry outward but keeping the same UV region; buildPlayerModelParts adds hat(32,0)/jacket(16,32)/right-sleeve(40,32)/left-sleeve(48,48)/right-pant(0,32)/left-pant(0,48) at inflate 0.5. model.frag already discards a<0.1 so transparent overlay texels vanish (no shader change). (3) **Head/body yaw separation:** `buildPlayerModel`→`buildPlayerModelParts(slim)` returns `PlayerModelParts{head, body}` (head = head+hat; body = rest), both in same feet-origin space. ModelRenderer now holds body+head vbuf/ibuf and `record(cmd,frame,const ModelPose&)` draws both with separate push-constant matrices; new struct `ModelPose{glm::mat4 body,head;}` in ModelRenderer.h. Renderer.drawFrame/recordCommandBuffer take `const ModelPose&` (fwd-decl `struct ModelPose;`). Player: added `bodyYaw_` (deg, lagged) + `bodyYaw()` getter + `updateBodyYaw(dt)` (called in update before updateThirdPersonCamera): snaps to camera.yaw on first frame; if moving (horiz speed>0.1) body eases toward camera.yaw fast (dt*12); else head may lead up to maxHead=45° before body eases to keep within (dt*10); wrapDeg helper. main: body matrix from player.bodyYaw() (`place`); head matrix = place * (T(neck 0,24,0)*Ry(-(yaw-bodyYaw))*Rx(pitch)*T(-neck)) — head also PITCHES with camera now. **AWAITING USER VISUAL VERIFY** (arms un-mirrored, overlay/hat visible, body lags head when looking around, snaps when walking). If overlay z-fights, bump kOverlay >0.5. If head pitch sign wrong, negate headPitch.

**CORRECTION (2026-07-01):** the band-face reversal above was the WRONG axis — user reported the whole skin was then horizontally inverted + top torso face mirrored. Real bug: TOP & BOTTOM faces were mirrored relative to the band. FIX: reverted front/back/east/west to the ORIGINAL standard UVs, and instead reversed U on TOP and BOTTOM (swapped their two U args). Front-top/front-bottom seams verified consistent. (Lesson: my MC-net reasoning had the character right/left backwards; trust user's visual feedback.) **Head look-around delay:** added Player.headYaw_ (getter headYaw()) that softly trails camera.yaw horizontally (headYaw_ += wrapDeg(camera.yaw-headYaw_)*min(1,dt*15)) in updateBodyYaw (init snaps to camera.yaw with bodyYaw_). main head matrix now uses headYaw() for yaw; pitch STILL = camera.pitch (exact/on-point per user: "vertical gotta be on point, horizontal can have delay"). Chain: pitch instant -> head yaw soft lag -> body yaw 45° threshold. Builds clean (only LNK1168 when game .exe running — close to relink).

**CORRECTION 2 (2026-07-01) — HANDEDNESS.** Reverting the band was wrong too. User gave precise per-face feedback: head+leg SIDE faces inverted, BOTH arms' front/back mirrored, torso front OK. Root cause: original build had character-right on the wrong side of X. Derived truth from MC skin layout (head "right side" cell = tex [0,8]) + "skins drawn as others see you" + GLM lookAt handedness: **model faces -Z, character-right = +X**; front/back map +X->min-U (readable), which my front/back ALREADY did (so they stayed unchanged). TWO fixes in PlayerModel.cpp: (1) addBox EAST(+X) now samples the leftmost "right" net cell [tu,tu+d] and WEST(-X) the "left" cell [tu+d+w,tu+d+w+d] — i.e. swapped the two side faces' U regions (fixes head/leg/torso/arm SIDE faces). (2) buildPlayerModelParts swapped limb texture regions so the +X limb uses the RIGHT region and -X the LEFT (arm 40,16<->32,48; leg 0,16<->16,48; sleeve 40,32<->48,48; pant 0,32<->0,48) — positions unchanged; fixes "both arms front/back mirrored" (was drawing opposite limb's texture). Front/back/top/bottom untouched. Builds clean. **AWAITING VISUAL VERIFY.** If an individual arm's inner/outer is still swapped once animations reveal inner faces, that's MC's left-limb `mirror` flag (not yet implemented) — revisit in Stage C.

**CORRECTION 3 (2026-07-01) — top/bottom V flip.** User: "top face of torso is rotated 180 and mirrored." Top/bottom had correct U (matches front-top seam, +X->min-U) but flipped V: the top cell's -Z (front) edge must sit at MAX-V (bottom of the top cell, adjacent to the front region), but it was at min-V. FIX in addBox: swapped v0/v1 on Top and Bottom faces (Top now `tu+d+w, tv+d, tu+d, tv`; Bottom `tu+d+w+w, tv+d, tu+d+w, tv`). Builds clean. **UV VERIFIED CORRECT by user.**

**TWEAKS (2026-07-01, verified/dialed-in):** overlay inflate kOverlay = **0.3** (user's sweet spot). Slim-arm Y bug fixed: was `armY = slim?11.5:12` (slim arms sat 0.5px low); real MC slim(Alex) arms top-align with torso identically to wide — only width (3 vs 4) differs. Now `armY = 12.0` for both. slim armW/armX still 3.

**PLAYER STAGE C started (2026-07-01, reconfigure not needed — no new files) — per-part rig + walk/idle anim + J inspect pose.** Split the model into 6 independently-transformed parts. PlayerModel.h: `enum PlayerPart {PP_Torso,PP_Head,PP_LeftArm,PP_RightArm,PP_LeftLeg,PP_RightLeg,PP_Count}`; `PlayerModelParts{ModelData parts[PP_Count]; glm::vec3 pivots[PP_Count];}`. buildPlayerModelParts emits each part (base box + overlay) into its slot with pivots: Head(0,24,0), RightArm(4,22,0), LeftArm(-4,22,0), RightLeg(2,12,0), LeftLeg(-2,12,0), Torso(0,0,0). ModelRenderer: ModelPose now `glm::mat4 parts[PP_Count]`; vbuf_/ibuf_/indexCount_ arrays[PP_Count]; init loops parts, record loops parts pushing per-part matrix, cleanup loops. ModelRenderer.h now includes game/PlayerModel.h (for PP_Count). Renderer just forwards ModelPose (unchanged). Player: added limbSwing_ (walk phase += speedXZ*dt*2.2), limbAmount_ (smoothed 0..1 = speedXZ/kWalkSpeed), animTime_ (+=dt); getters limbSwing()/limbSwingAmount()/animTime(); updateAnimation(dt) called every frame. **J key** (handleDebugPose, edge via jPrev_) toggles debugPose_ (getter debugPose()): when on, update() SKIPS movement + body/head-yaw (player freezes, camera still orbits via handleLook + updateThirdPersonCamera). main builds ModelPose per-part: normal = place*joint(pivot rot): Torso identity, Head rotY(headYawExtra)*rotX(pitch), arms rotX(±armA) legs rotX(±legA) with armA=sin(swing)*0.7*amt legA=sin(swing)*0.9*amt + idle sway sin(animTime*1.6)*0.04*(1-amt); DEBUG pose = spread: head +6y, arms rotZ(±95°) + translate ±6x, legs rotZ(±20°) + translate ±3x/-4y (all faces visible). `joint(part,rot)=trans(pv)*rot*trans(-pv)`. Builds clean. **AWAITING VERIFY** (J = frozen spread T-pose; walking swings limbs; head still tracks camera w/ lag). TODO next: 1st-person arms, head pitch already in, refine anim curves, maybe MC left-limb mirror flag once inner faces visible.

**STAGE C round 2 (2026-07-01):** (1) HEAD BOTTOM FACE FIX: user (via J pose) saw head bottom "rotated 180 & mirrored." My earlier top/bottom V-flip over-corrected the bottom. Re-derived from MC net: correct bottom = ORIGINAL orientation `tu+d+w, tv, tu+d+w+w, tv+d` (+X->maxU, -Z->maxV). Reverted bottom to that; TOP left as-is (user confirmed top correct; note top U may be symmetric-hidden but not touched). (2) CROUCH WALK: Player.crouching_ set in applyCameraEffects (getter crouching()); main leans the UPPER body (torso+head+arms) forward about the waist pivot (0,12,0) by leanFrac*-0.5 rad, leanFrac = smoothed camera.crouchOffset/0.3 (eases in/out); legs stay vertical + still swing. Head/arms still animate on top of the lean. Builds pending (game .exe was running at edit time — relink on close).

**SKIN UV FULLY VERIFIED by user (2026-07-01)** — inner faces correct too, no left-limb mirror flag needed. Crouch-walk + head-bottom fix also in.

**STAGE C round 3 (2026-07-01, rebuild only) — attack swing + 1st-person arm.** (1) Player: swingT_ (starts 1.0=idle), triggerSwing() sets 0, updateAnimation advances dt/0.3; getter swingProgress(). main computes `swingA = sin(progress*pi)*1.1` rad. Triggers: on successful break/place AND on initial click edge even with no target (leftPrev/rightPrev added). (2) 3rd person: right arm rotX(-armA + swingA) — chops forward on use. (3) FIRST-PERSON ARM: ModelPose gained `uint32_t mask = 0xFFFFFFFF` (bit per PlayerPart); ModelRenderer::record skips parts not in mask. In First perspective: modelVisible=true (false in Spectator — MC hides hand), mask = RightArm only; fpPlace = translate(renderEye w/ crouchOffset) * rotY(-yaw-90) * rotX(+pitch) * scale(1.85/32) — camera-anchored; arm = fpPlace * trans({1,-25,3}px) * joint(RightArm, rotX(75° + swingA*0.9) * rotZ(-10°) * rotY(-8°)). Offsets {1,-25,3} + angles are FIRST-GUESS tuning values — expect user iteration on hand placement. Known v1 limitation: fp arm drawn in world pass, can clip into very close walls. Builds clean. AWAITING VERIFY.

**STAGE C round 4 (2026-07-01, RECONFIGURE — 3 new sources) — DATA-DRIVEN BEDROCK ANIMATIONS.** User dropped vanilla `assets/animations/player.animation.json` (Molang-based: move.arms/legs use variable.tcos0, attack.rotations uses variable.attack_time, sneaking, bob, + keyframed swim/crawl for later). Built subsystem `src/anim/`: (1) **Json.{h,cpp}** — mini JSON parser (handles // comments, order-preserving object). (2) **Molang.{h,cpp}** — MolangExpr::parse/eval: full expr subset (+,-,*,/ , comparisons, &&,||, ?:, ??, unary -, 'string' literals→0, math.sin/cos DEGREES + pow/sqrt/clamp/lerp/abs/mod/floor/ceil/min/max, variable./query./v./q. normalization, `this` via MolangContext{vars map, thisValue}; unknown vars/functions→0; ?? checks var-undefined). (3) **PlayerAnimator.{h,cpp}** — loads all anims (bones lowercased; channels = single-expr key or keyframes lerped); evaluate(Input{distanceMoved m, moveAmount 0-1, lifeTime, attackTime=swingProgress, sneakAmount=leanFrac}) → per-part mat4: sets vanilla context vars (tcos0 = cos(dist*4*38.17°)*amount*57.3; attack_body_rot_y = sin(sqrt(t)*360°)*11.5; dist*4 = Java limbSwing units), layer stack {bob, move.arms, move.legs, attack.rotations, sneaking*weight} accumulates per-bone rot(deg)/pos(px) in BEDROCK space with per-component `this`=accumulated; **axis map bedrock→ours: (x,y,z)→(-x,y,-z)** (bedrock faces +Z/right=-X; ours -Z/right=+X = 180°Y turn); bone hierarchy root>(waist>body>head/arms), root>legs; internal pivots root(0,0,0) waist(0,12) body(0,24) head(0,24) arms(±4,22) legs(±2,12); boneMat = T(pos+pivot)*Rz*Ry*Rx*T(-pivot). main.cpp: animator.load(resolve(assets/animations/player.animation.json)) (prints [Anim] loaded N); third-person pose branch REPLACED: pose = place * animM[part], head gets extra rotY(headYawExtra)*rotX(pitch) on top. Player: limbSwing_ now RAW METERS (was *2.2). fp-arm + J-pose branches unchanged (still procedural). CMake: +src/anim/{Json,Molang,PlayerAnimator}.cpp. Builds clean. **AWAITING VERIFY — watch for sign issues:** if sneak leans BACKWARD or shifts oddly → flip Z in the (x,y,z)→(-x,y,-z) map or root pos; if limbs swing mirrored → flip x; body attack twist sign via attack_body_rot_y. All conversions centralized in PlayerAnimator.cpp boneMat/ctx setup.

**ANIM FIXES round 1 (2026-07-01) from user feedback ("right arm rotated not swinging; crouch model shrinks + teleports off pivot"):** (1) **Molang truthiness epsilon** — attack.rotations rightarm-Y ternary `math.sin(...*180) ? ... : 0` relies on sin(180°)==0 exactly; float sin(pi)≈1e-7 is truthy → permanent ~30° yaw on right arm at idle (attack_time=1). Added `truthy(v) = fabs(v)>1e-5` used by Ternary, &&, ||, ! (comparisons stay exact). (2) **Position-channel Z sign** — bedrock anim POSITIONS are authored +Z = model's BACKWARD (derived: sneak root pos z+9 must re-center the 28°-forward-leaned body BACK over the feet; crawl root z+17 same logic with body horizontal). Was mapping z→-z (moved sneak body 24px forward off the pivot = the "teleport"); now position map = (-x, y, +z) while ROTATION map stays (-x, y, -z). "Model gets smaller" in sneak = vanilla neck compression (body -2px, head -1 more, child-of-body → head down 3px total) + lean — correct behavior, reads right once the shift is fixed. Builds pending (game was running).

**HELD BLOCK IN HAND (2026-07-02, RECONFIGURE — new source + 2 shaders).** User: fp arm too low + no block/item visible in hand. (a) Confirmed the dropped player.animation.json is 3RD-PERSON ONLY — Bedrock 1st-person anims live in a separate `player.first_person.animation.json` (animation.player.first_person.attack_rotation/.bob_hand/.idle/.walk etc.); ASKED user to drop it from the same repo into assets/animations/ (not yet provided). (b) NEW gfx/HeldItemRenderer.{h,cpp} + shaders/held.{vert,frag}: chunk-style pipeline (ChunkVertex input, sampler2DArray from TextureArray view()/sampler(), camera UBO shared from Renderer, mat4 push constant, cull NONE, depth on) drawing per-block meshes built ONCE at init via buildSingleBlock+uploadMesh for every registry id (block-shaped: slabs/cross work). record(cmd,frame,mat4,blockId). Renderer.drawFrame/recordCommandBuffer gained (HeldItemRenderer*, heldMatrix, uint16_t heldBlock, bool heldVisible); drawn after player model. main: heldRenderer.init(...renderer.cameraUBOs(), &textures, &registry); heldM = pose.parts[PP_RightArm] * trans({handX(5.5 slim/6 wide), 10.5, -4.5}) * rotY(45°) * scale(6px) * trans(-0.5 center) — attached to the ARM matrix so it follows walk swing + attack chop in BOTH perspectives; heldVisible = selected!=AIR && canEdit() && modelVisible; cleanup heldRenderer before renderer. (c) FP arm RAISED: offset {1,-25,3}→{1.5,-23,2}, rotX 75°→70°. CMake +src/gfx/HeldItemRenderer.cpp. Builds clean. AWAITING VERIFY (block visible in hand 1st+3rd, follows swing; fp arm height better; tune heldM offsets/scale on feedback).

**FIRST-PERSON ANIMS FROM FILE + held size (2026-07-02, rebuild only).** User dropped `assets/animations/player_firstperson.animation.json` (vanilla fp anims: empty_hand base pose, attack_rotation, attack_rotation_item, walk hand-bob, breathing_bob, map/crossbow/spear/vr variants). Implementation: (1) Molang: identifiers now LOWERCASED at parse (Molang is case-insensitive; file uses `Math.Pi`) + `math.pi` → const π. (2) PlayerAnimator: load() now ADDITIVE (both files into one anims_ map; main loads both); Input gained `slim`; context extracted to buildContext() with **attack_time REST AT 0** (bedrock convention — fp item anims are only zero at t=0; swinging = attackTime<1, ctx attack_time = swinging?progress:0 — 3rd-person anims are zero at both ends so unaffected) + fp vars: first_person_item_rotation_factor = swinging?1:0 (self-zeros fp attack terms at rest), hand_bob = 0.1*moveAmount (vanilla clamps per-tick speed to 0.1), walk_distance = raw meters, short_arm_offset_l/r = slim?0.5:0, is_using_vr/bob_animation/map_angle/player_arm_height defaults. boneMat lambda → free fn boneTransform(rotDeg,posPx,pivot). NEW **evaluateFirstPerson(in, armDelta, itemDelta)**: layers {fp.walk, fp.attack_rotation} on rightarm (pivot = arm shoulder) + {fp.attack_rotation_item, fp.breathing_bob} on rightitem (pivot origin); returns DELTAS (zero at idle) — fp.empty_hand base pose SKIPPED (authored for Bedrock's internal camera rig; our hand-tuned fpPlace*trans({1.5,-23,2})*rotX70/rotZ-10/rotY-8 stays the base). main: fp arm = fpPlace * base * armDelta * joint(baseRot); hardcoded swingA chop REMOVED (file's attack_rotation replaces it); ain built once for both perspectives; heldM = arm * trans({handX,10.5,-4.0}) * itemDelta * rotY45 * scale(**4.5** was 6 — user: block too big) * center. Builds clean. AWAITING VERIFY (fp walk hand-bob subtle, fp attack whoosh direction, block size, breathing bob).

**CROUCH RAYCAST FIX (2026-07-02, rebuild only).** User: crosshair/raycast mismatch while crouching. Cause: view rendered from position-crouchOffset but raycastVoxel shot from camera.position (0.3 higher). Added `Camera::renderEye()` (= position - (0,crouchOffset,0)) and used it in Camera::view(), main raycastVoxel origin, main fpPlace anchor, Player::updateThirdPersonCamera. Builds clean.

**SPECTATOR POLISH + no-autofly (2026-07-02, rebuild only).** 4 user requests: (1) **Creative no auto-fly on switch**: handleModeSwitch now sets flying=false always (was flying=(mode==Creative)); initial spawn still hovers via member default flying=true; double-tap Space to fly. (2) **HUD hidden in spectator**: Hud::build gained `bool showHotbar=true` (crosshair always draws; bar/icons/selection gated). (3) **Spectator hotbar on scroll**: main scans `assets/textures/gui/sprites/spectator/*.png` at startup (filename order, ≤9, ui.registerTexture; folder currently DOESN'T EXIST — user must drop PNGs; prints note); `spectatorHudTimer` set to 3.0s on scroll while spectator, decays by dt; spectator hud.build uses spectatorSlots + showHotbar=timer>0. (4) **Spectator 3rd person = ghost head**: ModelPose gained `float opacity=1`; ModelRenderer pipeline now ALPHA BLENDS (srcAlpha/1-srcAlpha; alpha 1 = same as before), push constants = {mat4, vec4 tint} 80B stages VERT|FRAG; model.frag `layout(offset=64) vec4 tint` → outColor = rgb*shade*tint.rgb, alpha=tint.a; main: spectator+3rd person → pose.mask=head only, opacity=0.5. Builds clean.

**FP SWING FIX (2026-07-02):** user "arm punches god / disappears on break-place". Cause: first_person_item_rotation_factor=1 constant during swing → fp attack pos-y term `-factor*attack_time*15` = -15px plunge off screen near swing end. Fix: factor = swinging ? (1 - attackT) : 0 — matches the curves (all terms bounded ±4px, zero at both ends; start-of-swing snap to ~+55° rot is the intended whoosh). One line in buildContext. Builds clean.

**SPECTATOR ICONS DROPPED by user (2026-07-02):** assets/textures/gui/sprites/spectator/ = close, scroll_left, scroll_right, teleport_to_player, teleport_to_team. main scan now maps vanilla slots: teleport_to_player→0, teleport_to_team→1, close→8, scroll_*→skipped (page arrows), unknown names fill slots 2-7. Icons verified copied next to exe. Builds clean.

**ITEM FLICK DAMP (2026-07-02):** user "block leaves his hand when swinging (like throwing)". attack_rotation_item POSITION amplitudes ±10px (authored for Bedrock attachable rig) → block visually left the palm. evaluateFirstPerson layers now carry per-layer posScale: attack_rotation_item = 0.2 (max ~2px excursion, rotations kept full so the block still twirls). Builds clean.

**CAMERA PRESETS (2026-07-02, rebuild only).** User dropped Bedrock camera-entity presets (format 1.26.10) in assets/cameras/: first_person, third_person, third_person_front (the F5 trio) + free, follow_orbit, fixed_boom, death (extras for later: death cam, cinematic free cam). Engine now LOADS at startup (main, via mc::parseJsonFile navigating minecraft:camera_entity/components): **FOV 66** from first_person camera.field_of_view (was 70; Player gained public `baseFov` replacing kBaseFov constexpr; sprint +12 on top; camera.fovDeg init'd from it), **orbit radius 4** from third_person camera_orbit.radius (Player.thirdPersonRadius, used as maxDist in updateThirdPersonCamera), **min dist 0.25** from camera_avoidance.distance_constraint_min (Player.thirdPersonMinDist, was 0.3), **invert_x_input true** from third_person_front camera_orbit (Player.invertFrontLookX; handleLook negates dx when perspective==ThirdFront — authentic MC front-view orbit). Missing files keep defaults (try/catch per file); prints [Camera] line. Builds clean.

**Z-ROTATION SIGN FIX (2026-07-02):** user "idle bob arms clip INTO torso instead of swaying out — rotation inverted". Bedrock anim Z-rotations follow Java's y-down convention, NOT the naive 180°-Y conjugation. boneTransform rotation map now **(-x, y, +z)** (was (-x,y,-z)); X verified by walk/sneak, Y left unchanged (untested visually — if the attack body twist ever looks backwards, negate y next). Side effects: fp whoosh roll + item twirl direction flip too (MC-correct now). Builds clean; USER-CONFIRMED bob direction was the last wrong axis.

**FP ARM RETUNE + ARM BREATHING (2026-07-02):** user: fp arm "comes from my ear" (shoulder anchor was ~eye height; FOV 66 made it obvious) + arm static at idle while block breathed. (1) fp base now trans({1.5,-28,3.5}) + rotX(**100°**) (was {1.5,-23,2} + 70°): shoulder ~6px below eye/off-screen, hand HIGHER than shoulder (MC style), hand lands ~(5.5,-3.9,-8.3)px ≈ bottom-right quadrant at FOV 66. (2) evaluateFirstPerson Layer gained `itemToArm`; breathing_bob (authored on rightitem bone) now routes to the ARM so hand+block breathe together (block inherits via arm matrix; no double-apply). Builds clean.

**M7b PART 1 (2026-07-02, rebuild only) — PER-SHAPE COLLISION + SLAB PLACEMENT + STEP-UP.** All Stage C anim work user-VERIFIED before this. (1) World::boxCollides(mn,mx,outTopY*,outBottomY*): iterates overlapped cells, per-shape Y boxes (SlabBottom y..y+.5, SlabTop y+.5..y+1, Cross none, !isSolid skip); out params = highest top/lowest bottom overlapped (caller inits ±1e9). (2) Player: collidesAt/hasGroundBelow now via boxCollides (ground check = thin box feet-0.05..feet-0.001, works on slab tops); moveWalk Y-resolution snaps to REAL box tops (land: eye.y=topY+eyeHeight — feet flush at +0.5 on slabs; ceiling: bottomY-height). (3) **STEP-UP (user rule: walk up max 8px/slab height, taller = jump)**: kStepHeight=0.55; tryStepUp in x/z collision branches (only when wasOnGround && vy<=0; lifts eye by 0.55, re-tests, gravity+snap settle); can't clear full blocks. (4) Slab variants: BlockDef gained counterpart (BlockId) + hiddenFromHotbar + .slabTop()/.hidden() builders; registry addSlab lambda registers bottom + hidden "_top" variant, cross-linked (stone_slab, cobblestone_slab). Mesher already handled SlabTop. (5) RaycastHit gained `point` (entry hit point). (6) main: placement resolves slab variant (normal.y>0→bottom, <0→top, side face→fract(point.y)>0.5→top); collidesWithBlock now (reg, placedId, cell) shape-aware (top slab placeable in your lower-half cell); hotbar skips hiddenFromHotbar. Builds clean. AWAITING VERIFY: walk up slabs w/o jumping, stand on slab at half height, place top slabs via underside/upper-side-face, jump still required for full blocks. NEXT M7b: stairs (facing variants via same counterpart-ish pattern), inventory screen, translucent pass.

**M7b part 1 VERIFIED by user (all 5 checks: step-up, blocks need jump, slab half-height stand, top-slab placement, slab staircases).** Then user requested **DOUBLE SLABS incl. MIXED materials** (2 slabs same cell = full block; breaking removes only the aimed half). **DOUBLE SLABS built (2026-07-02, rebuild only):** (1) BlockShape::DoubleSlab + BlockDef.halfBottom/halfTop (halfBottom = bottom-variant id that remains when top breaks; halfTop = TOP-variant id remaining when bottom breaks). (2) Registry: after slabs, auto-registers hidden combos for EVERY ordered pair of slab materials (N² — 2 slabs → 4 combos), name "a+b", opaque=true (culls neighbors like cube); slabCombos_ map keyed (bottom<<16|top) with `slabCombo(bottomMat, topMat)` lookup (materials = SlabBottom ids). (3) Mesher: emitSlab gained drawTop param (also fixed: standalone SlabTop's ceiling face now culled vs opaque above); DoubleSlab case emits both halves with their OWN defs (mixed textures + correct side bands), internal seam never emitted; buildSingleBlock handles it too. Collision: DoubleSlab hits default full-box path ✓. (4) main BREAK: DoubleSlab → aimed half via normal.y or frac(point.y rel block.y) → cell becomes remaining half id (not AIR). PLACE: slab vs complementary slab's exposed half-face merges: bottom-slab top face clicked → slabCombo(hit, selected); top-slab underside clicked → slabCombo(selected, counterpart(hit)); guarded by collidesWithBlock(addedHalf); consumed flag skips normal placement. Builds clean. AWAITING VERIFY (merge same/mixed, break halves separately).

**JAVA BLOCK MODELS DOWNLOADED (2026-07-02):** user asked to fetch github.com/InventivetalentDev/minecraft-assets tree 26.2 assets/minecraft/models/block → done via sparse checkout (git clone --depth 1 --branch 26.2 --filter=blob:none --sparse, then sparse-checkout set that path; copied to project). **2659 JSON files now in assets/models/block/** (5.6MB, incl _all/_list index files). Format = JAVA EDITION models (NOT Bedrock geometry like the player): element boxes from/to in 0-16px, per-face {uv,texture(#var),cullface}, `parent` inheritance chain (e.g. stone_stairs.json sets #side=block/stone, inherits stairs.json's 2-box geometry: bottom [0,0,0]-[16,8,16] + top-back [8,8,0]-[16,16,16]). Also inner_stairs/outer_stairs corner variants, slab.json, cube_all.json, etc. NOT yet consumed by engine. TO USE (future, esp. for M7b stairs / full data-driven block models): (a) reuse anim/Json.h parser; (b) resolve parent chain (merge textures+elements); (c) map Java tex path "block/stone"→our flat "stone" (last path segment); (d) mesher emits arbitrary from/to boxes w/ per-face uv+cullface+rotation. For now stairs can be hardcoded like slabs OR bootstrapped from stairs.json geometry.

## THE BIG BLOCK REFACTOR (2026-07-02, user-requested Java-style architecture; RECONFIGURE — 3 new sources) — COMPILED CLEAN FIRST BUILD, AWAITING VISUAL VERIFY.
User asked (with Java ModBlocks example): polymorphic Block classes (stairs/slab/pillar/glass/leaves/layer/fence/door/pressure-plate/gate+), full registry refactor, waterlog-ready state data, survival mining strength/tool/drop-pool, SEPARATE mesh vs collision vs outline geometry, neighbor-updating fences, multi-cell blocks (door/bed), time cycle.
**Architecture now:**
- `world/Block.h`: **BlockState** = uint32 (low16 BlockId, high16 data; data bit15 RESERVED waterlogged; fromRaw/withData helpers). AABB{min,max}. ToolType enum. Drop{item,min,max,chance}. PlacementCtx{cell,clickedCell,normal,hitPoint,playerYawDeg,sneaking}. **class Block**: Properties builder (strengthIs/needs/prefers/notSolid/transparent/cullsSelf/canReplace/hidden/light/soundIs; fields strength,tool,requiresTool,solid,opaque,cullSame,replaceable,hiddenFromHotbar,lightLevel,sound) + texture chaining (allFaces/faces/column→gatherTextureNames/resolveTextures VIRTUAL, faceLayers()); virtuals: defaultState, appendMesh(BlockMeshCtx&), collisionBoxes(state), outlineBoxes(state), isOpaque(state), connectsToFence(state), placementState(ctx), tryMergeInto(reg,clicked,ctx,out), canPlaceAt, onPlaced, onUse, breakResult(state,hitPoint,cell,normal)→remaining state, onBroken, breakSeconds(tool) (MC-ish: strength*1.5 adequate / *5 wrong-required-tool; <0 unbreakable), drops(state,tool) (default self; none if requiresTool unmet).
- `world/BlockMesh.h/.cpp`: ChunkVertex/ChunkMeshData moved here; BlockMeshCtx{mesh,origin,state,reg,neighbor fn}; **emitBox(mesh,origin,from,to,layers[6],faceMask)** — generic sub-box with auto UV slice mapping (verified identity w/ old UVs for unit boxes); emitCross; kFaceNeighbor; face table/light moved from Mesher.
- `world/Blocks.h/.cpp`: **SlabBlock**(bit0 top; placement by face/frac; tryMergeInto→combo via dynamic_cast+reg.slabCombo), **DoubleSlabBlock**(bottomSlab_,topSlab_ ids; split mesh w/ seam culled; breakResult→remaining half state; no drops), **StairBlock**(bits0-1 facing = ascent dir from facingFromYaw(playerYaw), bit2 upside-down; mesh/collision = base slab + quarter box), **PillarBlock**(bits0-1 axis from placement normal; layer swizzle), **GlassBlock**(no drops), **LeavesBlock**(drops: self w/ Shears else sapling 5%), **LayerBlock**(bits0-2 layers-1, merge adds layer, 1 layer = no collision, mesh h=n/8), **FenceBlock**(mesh post 6-10px + 2-rail arms toward connectsToFence neighbors (opaque or fence/gate — arms auto-update via existing remesh-on-edit); collision = post 1.5 TALL; outline = post 1.0), **FenceGateBlock**(bits0-1 facing,bit2 open; onUse toggles; collision full-span 1.5 closed/none open), **DoorBlock**(bit0 upper,bits1-2 facing,bit3 open; 2 textures per half (own gather/resolve; layerBottomHalf_/layerTopHalf_); 3px panel via doorPanel(): closed on -facing edge, open on left edge; canPlaceAt needs air above; onPlaced writes upper; onBroken removes other half; onUse toggles BOTH), **PressurePlateBlock**(1px plate, no collision, bit0 pressed data-only). facingFromYaw/facingDir helpers (0=-Z,1=+X,2=+Z,3=-X).
- `world/BlockRegistry`: vector<unique_ptr<Block>>; template add(unique_ptr<T>)→T& (assigns id); block(id/state), isOpaque(state), isSolid(state), slabCombo(bottomMat,topMat); registerDefaults() = ONE statement per block: air(replaceable), grass/dirt/stone(needs Pickaxe 1.5)/cobblestone(2)/sand, oak_log PillarBlock, leaves, glass, stone_slab+cobblestone_slab, stone/cobblestone/oak_stairs, oak_fence, oak_fence_gate, oak_door("oak_door_bottom/top" tex), stone_pressure_plate, snow_layer("snow" tex), + hidden DoubleSlab combos per ordered slab pair.
- `world/Chunk`: stores vector<uint32_t> raw states; get→BlockState, set(BlockState) (implicit from BlockId keeps WorldGen untouched).
- `world/World`: getState/setState (+getBlock/setBlock compat wrappers), registry() accessor; **boxCollides** now iterates per-block collisionBoxes with FULL 3D box tests + checks one layer BELOW (fences reach y+1.5); setState keeps dirty/neighbor-marking.
- `world/Mesher`: buildCubeMesh dispatches `reg.block(state).appendMesh(ctx)` (one shared neighbor closure per chunk); buildSingleBlock uses defaultState + air neighbors (icons/held).
- Outline: Renderer drawFrame/record take `const std::vector<AABB>& outlineBoxes` (world-space; main builds from block.outlineBoxes(state)+cell); PushConstants now {vec4 chunkPos/boxMin; vec4 boxSize} (32B, chunk.vert unaffected); outline.vert scales unit box by boxSize; multi-box outlines (fence/stairs) = loop of draws.
- **Timed mining** (main): survival holds left → mineCell/mineProgress, need=breakSeconds(ToolType::None) (hand), swing loops while mining; break applies breakResult+onBroken+prints [Drop] lines (no inventory yet). Creative = instant + kActionRepeat. Placement: onUse first (unless sneaking) → tryMergeInto (guarded by collidesWithBlock(merged)) → placementState+replaceable-check+canPlaceAt+guard+setState+onPlaced.
- Player::collidesWithBlock(reg, BlockState, cell) iterates collisionBoxes.
- **`core/GameTime.h`**: 20-min day; dayFraction/dayCount/sunHeight/sunAngle/skyColor (day↔dusk↔night lerp)/clockText; main advances + renderer.setSkyColor(gameTime.skyColor()) (clear color now member skyColor_); F3 line "Time: HH:MM (day N)" via DebugInfo.timeText.
- CMake += world/Block.cpp, BlockMesh.cpp, Blocks.cpp. DebugOverlay += timeText line. F3 target tags now strength/opacity/needs-tool/data.
**DEFERRED (stated to user):** bed (2-cell horizontal — architecture ready via onPlaced/onBroken), waterlog behavior (bit reserved), crack overlay while mining, item drop entities/inventory pickup (pool prints to console), stair inner/outer corners, pressure plate activation, fence-vs-fence-arm COLLISION boxes (only post collides; arms visual/outline), walls/trapdoors/buttons (same patterns). Door textures oak_door_bottom/top + oak_planks/snow/oak_sapling pngs may be MISSING → magenta placeholder until user drops them.

**SHAPE-AWARE RAYCAST (2026-07-02, rebuild only):** user: aiming at a bottom slab's real top surface (mid-cell) wouldn't merge the 2nd slab — had to aim at the CELL top ("at the sky"). Cause: raycastVoxel treated non-air cells as full cubes (side-face normal through the empty half). Rewrote Raycast.cpp: DDA walk unchanged, but per non-air cell the ray now tests block.outlineBoxes(state) via rayBox (slab method; entry normal = -sign(dir) on max-tEnter axis; origin-inside → normal 0, t clamped 0); ray PASSES THROUGH empty parts of partial blocks (can target behind fence gaps); hit.point = true surface point. World gives registry via world.registry(). Slab-top merge now works aiming at the actual surface; doors/plates target their panels. KNOWN: fence arms aren't targetable (outlineBoxes = post only; arms would need neighbor-aware outline boxes — future). Builds clean.

**SPECTATOR: no outline (2026-07-02):** outline boxes skipped when spectator (crosshair only; canEdit already false). Future spectator interactions (chest peek, entity POV) get their own path.

**COMBINE-AT PLACEMENT (2026-07-02):** user: aiming THROUGH a slab's empty half at the block behind should let the new slab fill that cell (vanilla canBeReplaced behavior). Added `Block::tryCombineAt(reg, existing, out)` virtual — "my item placed into an occupied cell": SlabBlock fills the existing slab's empty half via slabCombo (mixed OK, direction from existing's half — no aim needed); LayerBlock reuses +1-layer rule. main placement: if target cell NOT replaceable → tryCombineAt (guarded by collidesWithBlock(combined)) BEFORE the normal free-cell path. Builds clean.

**TEST HOTBAR (2026-07-02, temporary until E-inventory):** registry gained byName(); main hotbar = explicit list {stone_slab, cobblestone_slab, stone_stairs, oak_stairs, oak_fence, oak_fence_gate, oak_door, stone_pressure_plate, snow_layer} + fallback fill from remaining non-hidden ids. Old basics off the bar. REVERT/replace when inventory screen lands.

**FENCE/DOOR FEEDBACK ROUND (2026-07-02, rebuild only).** User: door icon showed bottom half only; double doors should pair; fence arms had NO outline and NO collision (players slipped between posts); stairs outline seam OK to leave. Fixes: (1) **Box virtuals are now world-aware**: collisionBoxes/outlineBoxes(BlockState, const World*, const glm::ivec3& cell) — world nullable (icons/standalone → base shape). Callers updated: World::boxCollides(this,cell), Player::collidesWithBlock(+world param; 3 main call sites), Raycast (fence ARMS now targetable too), main outline. (2) **FenceBlock**: fenceConnections(world,cell) via connectsToFence; collision = post+arms 0..1.5 (continuous wall); outline = post 0..1 + arms hugging rails y 6..15px. (3) **DoorBlock hinge** (data bit4 = hingeRight): placementState checks cell at rightOf(facing) — same-facing left-hinge door there → new door hinge-right (double doors swing APART); doorPanel open edge = hinge side; PlacementCtx gained `const World* world` (main sets). (4) **Block::appendItemMesh virtual** (default = appendMesh; buildSingleBlock uses it): DoorBlock override renders BOTH halves stacked in one cell (icon + held). Stairs outline middle-seam left as-is (union-silhouette outline = future polish; user OK'd). Builds clean.

**ITEM MESH AUTO-FIT (2026-07-02):** user: door icon halves were "clustered" (I'd squeezed both into one cell) — and flagged it as a GENERAL multi-cell bug (bed too). Fix: (1) DoorBlock::appendItemMesh now emits TRUE geometry — two 1-tall boxes at y 0..1 and 1..2 (second via origin offset so both get clean 0..1 UVs; NOTE: emitBox UVs break for boxes exceeding 0..1 in an axis — always emit multi-cell item meshes as per-cell boxes with shifted origins). (2) buildSingleBlock gained a generic FIT PASS: computes mesh AABB; if any dimension > 1, uniform-scales (1/maxDim) + centers into the unit cell; meshes already within the cell untouched (slabs/plates/fences unchanged). Works for any future multi-cell item (bed). Builds clean.

**EDGE-BASED OUTLINES + STAIR SILHOUETTE (2026-07-02):** user asked to fix the stair outline (was 2 overlapping box wireframes w/ interior seam). Outline system reworked: (1) Block.h `struct Segment{a,b}` + virtual **outlineEdges(state,world,cell)** — DRAWN wireframe (outlineBoxes still drives raycast targeting); default = 12 edges per outline box (visuals unchanged for everything else). (2) **StairBlock::outlineEdges** = exact L-prism silhouette: 6-vertex profile {(0,0),(1,0),(1,1),(.5,1),(.5,.5),(0,.5)} in (h=ascent-axis, y), y-mirrored when top, mapped per facing (0:-Z h→1-z; 1:+X; 2:+Z; 3:-X), 18 segments (2 rings + 6 laterals) — NO interior seam. (3) Renderer createOutline = ONE solid unit cube (36 verts) drawn per bar; main thickens each edge into a thin AABB bar (half=1/128) then existing {boxMin,boxSize} push path. Fence/multi-box outlines still show joint seams (default per-box edges) — same recipe (custom outlineEdges) available if user wants those cleaned. Builds clean.

**CLOSE-TIME RETURN-OR-DROP (2026-07-05, BUILT CLEAN).** User: closing a container should return the crafting-grid items AND cursor stack to the inventory, dropping whatever doesn't fit — 3 cases (grid items, carrying X into a full-of-Y inv, carrying X-stack topping up partial X stacks then dropping leftover). All are one rule: inv.add() (tops up matching then fills empties) and DROP the leftover it returns. setOpen(false) had been discarding inv->add's return (items VANISHED); now returnOrDrop lambda pushes leftover to pendingDrops_. CRITICAL wiring: setOpen(false) runs at ESC/E time (top of loop) BEFORE the invOpen UI block, which is then skipped (invOpen false) so its takeDrops never ran -> drops stranded. Fixed: moved the takeDrops->throwStack drain OUT of the invOpen block to an unconditional `if(session)` drain every frame (covers Q, off-panel, and close-overflow). leaveSession (Save&Quit) also drains right after setOpen(false) BEFORE session->destroy so overflow spawns + gets saved as ground drops (captureLevel includes drops.items()) instead of vanishing. Prior LNK1168 fail was just the game being open at the 590s wait timeout; code was fine.

**DROP CARRIED STACK BY CLICKING OFF-PANEL (2026-07-05, BUILT CLEAN).** User: dragging an item on the cursor + clicking OUTSIDE the container drops it (LMB whole stack like shift+Q, RMB one), vanilla. handleSlotClicks gained `bool outside` param; when outside && carrying: LMB -> pendingDrops_.push_back(held_) + clear + reset drag; RMB -> push {id,1} + decrement. File-local inRect(mx,my,x,y,w,h). buildCreative computes overPanel = catalog rect (extended up by kTabH for tabs) OR inventory panel rect; buildPanel = the single kSurvW x panelH rect; pass !overPanel. Reuses existing pendingDrops_/takeDrops()->session->throwStack() (already wired in main's invOpen block, throws from player eye w/ velocity). cell is -1 when outside so no slot logic double-fires; drop runs at top of handleSlotClicks before the cell>=0 blocks. Build PENDING (game running; queued).

**OPTIONS: WIRE INPUT SETTINGS + KEYBIND CATEGORIES (2026-07-05, BUILT CLEAN).** (1) kOptW 155->172 (wider option buttons). (2) Removed the R rain debug keybind + Session's R->weather.toggle (and rPrev_). (3) Key Binds screen now shows CATEGORY HEADERS: buildKeyBinds flattens binds into Row{header|bind} inserting a centered grey header when category changes; scroll counts rows. (4) WIRED input settings via Session::applySettings -> player.setInputOptions(invertMouse, sneakToggle, sprintToggle, autoJump) reading settings.opt (invertMouse/sneakMode/sprintMode/autoJump, >0.5 = 2nd value): Player got invertMouse_/sneakToggle_/sprintToggle_/autoJump_ + sneakActive_/sneakLatch_/sneakPrev_/sprintPrev_. updateSneakState(computed in update() before movement; toggle flips sneakLatch_ on key edge) -> moveWalk + applyCameraEffects use sneakActive_. Sprint toggle in moveWalk (edge flips sprinting_). Invert Mouse: handleLook negates dy. Auto-Jump: moveWalk tracks bumpedWall (blocked, no step-up) -> if autoJump_ && wasOnGround && moving && !crouch && headroom 1 up -> velocity_.y=kJumpSpeed. View Bobbing (main): first-person eval uses fpIn copy with moveAmount/distanceMoved zeroed when opt viewBobbing index 1 (OFF). NOT wired (told user): Scroll Sensitivity + Discrete Scrolling — our scroll is already per-notch discrete (sign-based), needs a smooth-scroll system to be meaningful.

**KEYBINDS SHADOW BUG FIX (2026-07-05, BUILT CLEAN).** Regression from the keybinds wiring: moveWalk local `int kSprint = bindKey(...)` (key code ~341) SHADOWED the file `constexpr float kSprint = 1.6f` sprint-speed constant, so `speed = kWalkSpeed * kSprint` = 4.7*341 = "F1 car". Fix: renamed ALL movement key-code locals in moveWalk+moveFly to *KeyCode suffix (fwdKeyCode/backKeyCode/leftKeyCode/rightKeyCode/jumpKeyCode/sneakKeyCode/sprintKeyCode) so none collide with speed constants (kSprint/kWalkSpeed/kJumpSpeed/kSneakFactor). LESSON: watch for locals shadowing the k* speed constants in Player.cpp.

**OPTIONS WIDEN + TOUCHSCREEN REMOVED + KEYBINDS CLASS (2026-07-05, BUILT CLEAN).** (1) Wider option columns: added kOptW=155*kScale (vs kHalfW=98 for pause/worlds); each options SUB-PAGE shadows `const float kHalfW = kOptW;` at its top (one line, widens all its buttons without touching pause/worlds alignment). Fits at default 1280 window (may clip at ~854). (2) Removed Touchscreen Mode (Mouse) + Attack Indicator (Video, ->Entity Shadows). (3) KEYBINDS CLASS: core/KeyBinds.{h,cpp} — vector<Bind{id,category,name,def,key,rebindable}>; mouse buttons stored as NEGATIVE codes (-1 LMB/-2 RMB/-3 MMB, fixed/non-rebindable); resetAll/load/save (keybinds.txt "id:code"), key(id)/setKey/reset, static keyName(code)->"Space"/"Left Shift"/"A"/"Left Button". Window: keyCallback+mouseButtonCallback record lastKey_ (Escape/unknown ignored), consumeKeyPress(). MenuScreen: init gained KeyBinds* (3rd param); Input.keyPressed; rebinding_ index + keyBindsDirty_ + takeKeyBindsDirty(); buildKeyBinds REWRITTEN — real list, scroll, per-row [name | key button (click->rebinding_, next keyPressed sets it, shows "> ? <") | Reset(enabled if !=def)], bottom Reset Keys|Done. main: keyBinds instance loads keybinds.txt, menu.init(...,&keyBinds), menuIn.keyPressed=window.consumeKeyPress(), save on takeKeyBindsDirty; action keys E/F3/drop now keyBinds.key(...). Player: setKeyBinds + bindKey(id,fallback); moveWalk/moveFly/handleFlyToggle/applyCameraEffects(sneak)/handleModeSwitch(G)/handlePerspective(F5) all use bindKey; session->player.setKeyBinds(&keyBinds) in enterWorld. R(rain) left hardcoded in Session (debug, Session has no binds ptr). CMake += KeyBinds.cpp. NOTE main.cpp user-modified earlier this session (kept).

**FULL VANILLA OPTIONS TREE (2026-07-05, BUILT CLEAN).** User gave 11 screenshots of the whole options tree. Settings: added generic `std::map<std::string,float> opt` + optGet/optSet, persisted as `opt.<key>:<val>` lines (one loop in save/load) — so cosmetic menu values (volumes, graphics toggles, skin parts, chat/accessibility knobs) remember without bloating the struct; sliders store 0..1, cycles store the option index. MenuScreen: Page enum += SkinCustom/MusicSounds/VideoSettings/Controls/MouseSettings/KeyBinds/ChatSettings/Accessibility/ResourcePacks; back() maps sub-pages -> Options (Mouse/KeyBinds -> Controls). Helpers: optCycle (button "Label: Value", cycles opt index; values ORDERED so index 0 = default, avoids a defIdx param — e.g. {"ON","OFF"} for on-by-default, {"OFF","ON"} for off), optPercent (0-100% slider bound to opt, has def), subButton (opens page), subHeader (dim+title, returns content-y), doneButton (returns to parent). buildOptions RESTRUCTURED to vanilla 2-col: FOV slider|Online(locked) top, then grid Skin Custom|Music&Sounds / Video|Controls / Language|Chat / Resource Packs|Accessibility, Done. Sub-pages match screenshots. REAL/wired (live via main's applySettings): FOV (Options), Sensitivity (Mouse), Render Distance + Fullscreen (Video, fullscreen toggles s.fullscreen directly), Language (existing screen). Everything else = cosmetic opt-store toggles (persist, no engine effect yet). KeyBinds = read-only bind list w/ wheel scroll (optScroll_). Lang keys used are vanilla ids with English fallbacks (display English until lang files add them). slider ids unique per page (0,10-56). NOTE: main.cpp was user-modified this session (intentional, kept).

**PAUSE MENU VANILLA RECONSTRUCTION (2026-07-05, BUILT CLEAN).** User gave a vanilla pause-menu screenshot. buildPause rebuilt: 5 rows centered as a block (blockH=4*kBtnH+kSquare+4*kGap) with "Game Menu" title above — Back to Game (full, Resume) / Advancements|Statistics (half, LOCKED disabled) / 4 icon squares centered (iconRowW=4*kSquare+3*kGap) bug|feedback|friends|social (all decorative no-op for now) / Options...|Multiplayer... (Options works, Multiplayer LOCKED via menu.shareToLan) / Save and Quit to Title (full, SaveQuit). Icons found in assets: gui/sprites/pause_menu/{bug,social_interactions,player_reporting}.png + icon/chat_modified.png (feedback) + friends/friends.png. MenuScreen::Textures += iconBug/iconFeedback/iconSocial (registered in main from pause_menu/ + icon/). Lang keys added (en+pt): menu.shareToLan, gui.advancements, gui.stats (PT: Multijogador.../Progressos/Estatísticas). Functional: Back to Game, Options, Save and Quit. Locked/decorative: Advancements, Statistics, all 4 icons, Multiplayer (no screens yet — friends/social/advancements/stats future work).

**DROP MAGNET CHECKS INVENTORY ROOM (2026-07-05, BUILT CLEAN).** User: with a full inv, drops still glide toward the player and bounce off (can't be picked up). Fix: Inventory::hasRoomFor(id) (matching partial stack < max OR empty slot; false for AIR); Drops::update `collectable` now also requires inv.hasRoomFor(it.id) — gates BOTH magnet glide and pickup, so a full bag leaves items lying still. Partial pickup still works: hasRoomFor true -> add() fills what it can -> leftover count remains -> next frame hasRoomFor false -> stops. inv already passed to update().

**LMB SPLIT-DRAG = STANDARD EVEN DISTRIBUTION (2026-07-05, BUILT CLEAN).** User: 3 items can't split into 3 slots (code needed >4). Root cause: the count/2 cap + maxEven forced per>=2, so 3 items -> maxEven=1 -> place-all-in-1. Reconciled ALL prior cases by dropping the per>=2 idea entirely and using vanilla even distribution: dragWant(total,n,i) = per=total/n, rem=total%n, `per + (i<rem?1:0)` (remainder tops up the FIRST slots so the LAST-dragged slots take the smaller share); gather cap raised to `< held_.count` (each slot gets >=1). NOTHING left on cursor (all distributed). Verified: 3/3 -> 1/1/1; 31/16 -> 15x2+1x1 (last slot=1, matches "16th pours leftover"); 32/16 -> 16x2. TRADEOFF told user: 32 over 17 now distributes 15x2+2x1 (no cursor dump) instead of hard-stopping at 16 — the leftover-oscillation (32's divisors 8/16 both hit leftover 0 mid-drag) makes a clean "stop" cap impossible, and even-distribution resolves the real gripe (items dumping back to cursor). Preview cursor shows 0 during drag now.

**LMB SPLIT-DRAG OVERFLOW SLOT (2026-07-05, BUILT CLEAN).** Refinement of the cap: for ODD totals the leftover 1 should POUR into one extra slot, not stay on cursor. 31 TNT -> 15 slots x2 (1 in hand) then the 16th slot receives the 1. New file-local `dragWant(total,n,i)`: maxEven=max(1,total/2); evenSlots=min(n,maxEven); per=total/evenSlots; i<evenSlots -> per, i==evenSlots -> total-per*evenSlots (overflow pours remainder), else 0. Gather cap raised from total/2 to CEIL: `max(1,(count+1)/2)` (31->16 allows overflow, 32->16 none since even). Commit + BOTH preview blocks now use dragWant(total,n,i) per ordinal instead of uniform per (total captured before the loop; held_ decremented as running remaining). 32 even: 16x2 no overflow (17th still rejected). 5: 2x2 then 3rd pours 1. Small totals <=3: place-all-ish.

**LMB SPLIT-DRAG SLOT CAP (2026-07-05, BUILT CLEAN).** User: 32 TNT split over 16 slots = 2 each (cursor 0), but hovering the 17th recomputed floor(32/17)=1 each and dumped 15 back on cursor; wants it to STOP once it can't split evenly (>=2) anymore. Fix: gather now caps dragCells_ at `maxSlots = max(1, held_.count/2)` — every slot keeps >=2 items; slots beyond total/2 are rejected (not added), so the ghosts/cursor freeze instead of re-diluting. Matches both stated cases: 32 TNT -> max 16 slots (2 each, 0 left); 5 TNT -> max 2 slots (2 each, 1 left). held_ unchanged during drag so held_.count==total throughout. Small stacks (<=3) can't split (cap 1 = anchor only, place-all). 1-each spreads are the RMB place-one drag's job, not this.

**LMB SPLIT-DRAG PREVIEW GATING (2026-07-05).** User: LMB drag-to-split-even should only trigger after entering a SECOND slot; even split leftover=1 for odd. Investigated: the split MATH was already correct (per=max(1, floor(originalHeld/dragCells.size())), remainder stays on cursor; size==1=place-all via clickStack, size>=2=even split) — held=5 over 2 slots -> 2+2, cursor 1 exactly as wanted. Real issue = the split PREVIEW ghost showed on the single anchor slot immediately on press (before moving), so it FELT triggered early. Fix: both preview blocks (buildCreative ~638 + buildPanel ~888) gated `!dragCells_.empty()` -> `dragCells_.size() >= 2`. Now no split ghost until the 2nd slot; cursor keeps full count (dragPreviewRemaining_ stays -1) during single-slot hold. Behavior unchanged, perception fixed. Build PENDING (game running; queued). KNOWN RELATED (not fixed, not asked): first click of an intended double-click-collect places the stack via drag single-cell path, emptying held_ so the 2nd click's collect no-ops.

**UI CLICK BLEED FIX (2026-07-05, BUILT CLEAN).** User: clicking "Back to Game" in pause broke a block in-world. Cause: creative break fires on leftNow (button HELD, not edge), and the LMB from the menu button was still down when the game resumed. Fix: Session.swallowMouse_ + ignoreMouseUntilRelease(); interact() early-returns (updating leftPrev_/rightPrev_) while swallowing until BOTH buttons release, then clears. Called from setCursorCaptured(true) alongside syncLook() — every recapture ignores the closing click. Works because the Resume frame sets it before next frame's session->update runs interact.

**CAMERA RESYNC + FRONT INVERT + TOOLTIP NINE-SLICE (2026-07-04, BUILT CLEAN).** (1) Pause-menu camera flick: with the App/Session split, player.update isn't called while a menu is open, so the old `else{firstMouse_=true}` resync never ran during pause -> stale mouse delta snapped camera on resume. Fix: Player::syncLook() {firstMouse_=true}; setCursorCaptured lambda MOVED below the session unique_ptr, capture changed [&window]->[&], and the captured branch now calls `if(session) session->player.syncLook()` — every recapture drops the pending delta (inventory already worked since Session ticks with acceptInput=false during inv). (2) 3rd-person FRONT view now ALWAYS inverts horizontal look (`if perspective==ThirdFront dx=-dx`), removed the invertFrontLookX gate (kept member as (void) no-op override); vanilla behavior, user requested. (3) User added assets/textures/gui/sprites/tooltip/{background,frame}.png (100x100 nine-slice, mcmeta border 9 / 10 stretch_inner). InventoryScreen::Textures += tooltipBg/tooltipFrame (registered in main); file-local nineSlice(ui,tex,x,y,w,h,bd,buv) helper (bd=drawn corner px, buv=border/texSize); drawTooltip gained bgTex/frameTex params -> draws bg then frame nine-sliced (bd=4*kScale, buv 0.09/0.10) with the old flat quad as fallback when tex ids are 0; all 3 call sites pass tex_.tooltipBg/Frame. pad 3->4 kScale.

**CRAFTING TABLE LAYOUT DECOUPLED + TITLE NUDGE (2026-07-04).** User: crafting-table slots moved lower "for no reason" + move survival name 2px down. Root cause: buildPanel SHARED kSurvH/kSurvGridY/kSurvHotbarY between survival + crafting table, so the survival +10 header shift dragged the (UNEDITED) crafting_table.png slots down too. Fix: crafting table gets its own kTableH=184/kTableStorageY=84/kTableHotbarY=160 (original vanilla positions); buildPanel now computes local panelH/gridY/hotbarY = craftingTable ? table : surv, and ALL usages (py centering, cell hit-test sy/hy, slotPos, panel texQuad size+UV) use the locals — the two panels can't affect each other anymore. kSurvGridX=8 shared (both). kSurvTitleY 5->7 (name 2px lower). Build PENDING (game running; queued).

**E-ACCENT COMPOSE + HOTBAR SEL + INVENTORY HEADER (2026-07-04, BUILT CLEAN).** (1) "Minério"->"minrio": verified ascii.png cells 0x80-0x8F are EMPTY in this pack (0 pixels), and accented.png skips e-grave/acute/circ/dia on purpose. Font.cpp: accentFor() composes those 4 = plain 'e' glyph + 2-3 hand-placed accent pixels (ui.quad) in the cell's free top rows; textWidth/drawRaw use legacy glyph if widths_[cb]>0 else compose. So é/è/ê/ë now render on any pack. (2) Hotbar selection (hud/hotbar_selection.png = 24x23) drawn 25x24 (was 24x23) — user: "stretch 1px right + 1px bottom, like vanilla". (3) User edited inventory.png: now 176x194 (+10px header band at top y0-16). ALL interiors shifted +10: kSurvH 184->194, kSurvGridY 84->94, kSurvHotbarY 160->170, kSurvCraftY 17->27, kSurvResultY 26->36 (kSurvGridX 8, kSurvCraftX 88, kSurvResultX 148 unchanged — X didn't move, verified via corner (88,27) + ASCII map). Survival preview box gy(8)->gy(18), look anchor gy(22)->gy(32). Header title: InventoryScreen.setHeaderTitle(playerName) set in main on E-open; buildPanel draws it (or lang container.inventory "Inventory" fallback) at kSurvTitle (8,5) dark no-shadow, ONLY on survival panel (crafting table not edited). lang keys container.inventory/crafting added (PT: Inventário/Mesa de Trabalho). Measurement technique: ASCII-map the PNG at 2px res marking grey-139 '#' / opaque '=' / transparent '.' to read layout after user edits.

**FULL TRANSLATION: BLOCK NAMES + TAB NAMES (2026-07-04).** User: block names + tab names didn't translate ("make everything one shot"). InventoryScreen: init gained const Lang* (3rd param); blockLabel(reg,id) member = lang tr("block."+name, prettyName fallback) — used by ALL tooltip sites (creative catalog, panel cells, craft result); creative tab titles via kTabKeys itemGroup.buildingBlocks/natural/functional. main: openWorldsPage subtitles translated (gameMode.creative/survival/spectator + selectWorld.day) — note: built at page-open, so a language switch mid-Worlds-page shows old strings until reopened (language screen unreachable from there anyway). Block name list extraction (169): regex over BlockRegistry.cpp for hardCube/softCube/planksCube/woodSlab/stoneSlabF/woodStairs/stoneStairs/make_unique + woodSet(w) expansions — reusable technique. pt_pt.json: FULL dictionary — all menu keys + itemGroup + ~169 block.<name> PT-PT entries (Pedregulho, Tábuas de Carvalho, Laje/Escadas/Cerca/Portão de X, Minério de X, Lã <Cor>, Ardósia=deepslate, Bloco de Relva=grass...); en_us.json: itemGroup + day/spectator keys (block.* omitted — prettyName IS the English fallback). F3/debug intentionally untranslated. Build PENDING (game was running; background Wait-Process->build queued).

**LANGUAGE MENU + ACCENTED FONT (2026-07-04, BUILT CLEAN).** User asked for the language menu + pointed out accented.png exists in fonts (it did: 144x900 = 16 cols of 9x12). VERIFIED the glyph layout by dumping ASCII art of the atlas and reading it: rows 0-3 = {U+C0..CF}, {D0,D1,D2..D6,D9..DD,E0..E3}, {E4..E7,EC..EF,F1,F2..F6,F9,FA}, {FB,FC,FD,FF,U+100..10B} — Latin-1 order SKIPPING glyphs vanilla keeps in ascii.png CP437 half (è/é/ê/ë lowercase -> ascii bytes 0x8A/0x82/0x88/0x89). Font rewritten: UTF-8 decode (2-byte; 3/4-byte -> '?'), ext_ map codepoint->{cellX,cellY,width} (widths alpha-measured like ascii), accented glyphs drawn 9x12 at y-3*scale so letter bodies align with 8px ascii glyphs (accents poke above the line, vanilla-style); init(ui, asciiPng, accentedPng="") — degrades gracefully without the file. kHeldShrink 0.8->0.56 (user: 30% more off). LANGUAGE MENU: MenuScreen Page::Language + Actions OpenLanguages/SetLanguage + LangEntry{code,name} + setLanguages + actionLanguage(); opened from the TITLE language icon square AND an Options "Language..." button (langReturn_ tracks where Done/ESC returns); page = scrollable button list of Lang::list entries, current marked "> name <", clicking returns SetLanguage; main handles: OpenLanguages scans assets/lang, SetLanguage sets settings.language + lang.load + settings.save — menus RETRANSLATE LIVE next frame (tr per frame). pt_pt.json rewritten WITH proper accents (Português, Opções, Visão, Ecrã...); en_us/pt_pt gained options.language(.title/.empty) keys. Lang files are hand-written by me (user: "build the lang json yourself").

**HELD SHRINK + FRIENDS ICON + LANG CLASS (2026-07-04, BUILT CLEAN).** (1) Held blocks 20% smaller: kHeldShrink=0.8 multiplied into dispM scale in main (tuning knob). (2) Title friends square now uses assets/textures/gui/sprites/friends/friends.png (user uploaded the whole friends/ sprite set — accept/reject/toast etc. available for a future friends UI). (3) core/Lang.{h,cpp}: vanilla-format lang files assets/lang/<code>.json (flat key->string, parsed with anim/Json); Lang::tr(key, englishFallback) — game fully playable with ZERO lang files; load() logs count or fallback notice; displayName() from "language.name"(+region); static list(langDir) -> {code,name} sorted, READY for the language-selection screen (user asked to prep). settings.language is now the CODE (default "en_us"; migration in Settings::load resets any value containing a space, e.g. old "English (US)"). main: lang.load after settings; menu.init(mtex, &lang). MenuScreen: lang_ pointer + tr(key,fallback) member; ALL menu labels translated with vanilla keys (menu.singleplayer/multiplayer/online/options/quit/game/returnToGame/returnToMenu, selectWorld.title/select/create/delete/deleteButton/deleteQuestion/deleteWarning/enterName/enterSeed/gameMode/newWorld/empty, gameMode.creative/survival, gui.cancel/done, options.title/fov/sensitivity/renderDistance/fullscreen/on/off, custom profile.name/profile.uploadSkin); title bottom-right shows lang displayName. Drop a vanilla pt_pt.json (or any) into assets/lang + set language:pt_pt in options.txt and menus translate; the SCREEN to pick languages is the declared next step (Lang::list feeds it). InventoryScreen strings (tab names, block tooltips) NOT translated yet (block names need per-block keys).

**BLOCK TAGS + TAG RECIPES + COLLECT/CRAFT-ALL POLISH (2026-07-04, BUILT CLEAN).** User: double-click collect must sweep crafting grid; recipes must use TAGS (their long-standing ask) — registry tags blocks, recipes pull tag members; fuel tag + per-block burn value for the future furnace. TAGS: Properties gains tags vector + fuelSeconds; builders .tag("logs") and .burns(seconds) (burns auto-applies "fuel" tag); BlockRegistry: tags_ map populated in addImpl (tags_.clear() in registerDefaults), API withTag(name)->const vector<BlockId>& (static empty for unknown) + hasTag(id,tag). Tagged: oak/spruce/birch logs .tag("logs").burns(15); NEW planksCube helper (oak_planks BLOCK DID NOT EXIST — only as texture! now registered) tags "planks"+burns(15) for all 7 planks; woodSlab burns(7.5), woodStairs/fence/gate/crafting_table/bookshelf burns(15). FURNACE-READY: query withTag("fuel") + props().fuelSeconds. RECIPES: Ingredient{id OR anyOf(tag members)} with matches(); "#tag" syntax in addShaped keys/addShapeless; shaped mirror matching via Ingredient::matches; shapeless = two-pass greedy (exact ids bind first, then tags); registerDefaults GENERATES families by iterating tags: #logs -> log->4 planks (name-derived), #planks -> slab(x6)/stairs(x4)/door(x3) name-derived; crafting_table + chest ring + bookshelf(simplified, no books) take "#planks" (MIXED woods allowed, vanilla); stones stay a name-pair data table (irregular); startup log prints recipe/tag counts. UI: collectAll(inv,held,extra,extraN) sweeps craft_ too (double-click gathers grid leftovers); double-click the RESULT slot = craft-all onto cursor until full/starved (lastResultClick_ 0.4s); shift-click result (craft-all into inv) unchanged. New saves note: oak_planks appended at registry END so existing saves (name-based) unaffected.

**CRAFTING SYSTEM + CRAFTING TABLE UI (2026-07-04, BUILT CLEAN 1st try).** User picked crafting + container UIs as next phase. game/Recipes.{h,cpp}: RecipeBook with shaped (matched at any grid offset + horizontally MIRRORED, via bounding-box compare) + shapeless (sorted multiset compare); ingredients/results registered by NAME via addShaped(rows,result,count,key)/addShapeless — unknown names drop the recipe silently (DORMANT recipes: furnace ring + chest ring pre-registered, activate when those blocks get added). registerDefaults: per wood (oak/spruce/birch/dark_oak/acacia/jungle/cherry/mangrove): log->4 planks (shapeless), 2x2 planks->crafting_table, 3 planks->6 slab, stairs shape->4, 2x3->3 door, chest ring (dormant); stone families (stone/cobblestone/smooth_stone/bricks->brick_/stone_bricks->stone_brick_/sandstone/red_sandstone/granite/diorite/andesite/nether_bricks->nether_brick_): 3->6 slab + stairs shape->4; 2x2 conversions: stone->4 stone_bricks, granite/diorite/andesite->4 polished_*, 4 sand->1 sandstone, red_sand->red_sandstone; cobble ring->furnace (dormant). NO stick recipes yet (fences/gates wait for the item registry). Block.h: Properties.container + opens(kind); kContainerNone/Crafting/Furnace/Chest constants; crafting_table marked .opens(kContainerCrafting). Session: ContainerRequest{kind,cell} + takeContainerRequest(); interact() right-click on container block (not sneaking) queues it BEFORE onUse. main: recipes instance; container request handled right after session->update (opens invScreen in Crafting mode, releases cursor); invMode app var (E sets Creative/Survival by player mode); invScreen.init(itex,&recipes); itex.craftingPanel = crafting_table.png. InventoryScreen: Mode enum {Creative,Survival,Crafting}; build(mode) dispatch; buildSurvival RENAMED buildPanel(bool craftingTable) — shared 176x184 storage(y84,4 rows)/hotbar(y160) layout; survival upper = player box + 2x2 grid at (88,17) + result icon (148,26); crafting table upper = 3x3 at (30,17) + result (124,35) (all pixel-measured; result interiors 26px -> hover box resX-5..+21); cell encoding kCraftCell0=100+ for craft_[9] (2x2 uses [0..3] row-major w=2), result NOT a cell (resultHover separate); handleSlotClicks slotAt handles 100+, shift-click craft cell dumps to inv.add, massMove gated cell<100; result click: pick into cursor (merge if same id & fits) consuming 1 of each grid slot, SHIFT = craft-all loop with inventory-space precheck (space scan per iteration); craftResult_ recomputed after consumption; setOpen(false) returns held_ AND craft grid to inv (or clears if no inv). Tooltips + hover highlight for craft cells and result. NOT DONE (next): furnace block+smelting UI, chest block entity + generic_54 UI (both have dormant recipes + reserved kContainer kinds + measured PNG (generic_54 6 rows) waiting).

**ARCHITECTURE: APP vs SESSION SPLIT (2026-07-04, BUILT CLEAN 1st try).** User (correctly) diagnosed no separation between in-world game and main menu; asked for full restructure + explicitly authorized forward-looking structure ("bypass minimal-code instinct"). NEW game/Session.{h,cpp}: owns World, unique_ptr<WorldSave>, Level, Player, Inventory, Drops, Mining, GameTime, Weather, spectatorHudTimer + private spawn/cooldown/input-edge state. API: start(registry, saveDir, settings, PlayerTuning{thirdPersonRadius/MinDist/invertFrontLookX from camera JSONs}) loads level+world+applies; update(ctx, window, renderer, dt, FrameInput{inventoryOpen, scroll, now}) = ENTIRE gameplay step (player physics, hotbar keys, Q throw, raycast+outline_, interact() break/place, mining+crack draws, clock/weather+R, renderer env feed, autosave 60s, world.update, drops, spawn scan); save(); destroy(ctx) (save + waitIdle + world.destroy); applySettings; throwStack; getters outlineBoxes()/targetHit()/level(). Gameplay members PUBLIC by design; header comment marks future homes: entities/mobs, armor/offhand/pouch equipment, container state. main.cpp REWRITTEN (~1200->~700 lines): app shell only (window/vk/renderers/ui/menus/settings/skin/preview/panorama) + std::unique_ptr<Session> session — session==nullptr IS the title (no hidden player, no world object, menuCam static Camera for drawFrame); gameplay step = `if (session && !menuOpen) session->update(...)` — pause = skipping the call (gdt concept DELETED, all inGame gates DELETED); leaveSession lambda = invScreen close + session->destroy + reset + renderer drop/crack clears + debugOn=false; enterWorld/exitToTitle use it. App tracks its own UI input edges (leftPrev etc. separate from Session's internal edges). Renderer::drawFrame/recordCommandBuffer now take const World* (null at title: sky/chunks/weather skipped; panorama+preview+UI carry the frame). kActionRepeat moved into Session.cpp. CMake += Session.cpp. Old scattered vars (inGame/spawnPlaced/gdt/breakCooldown/mineCell-era lambdas levelFromGame/applyLevelToGame/saveWorldNow/destroyWorldGpu) all absorbed into Session.

**MODE-SWITCH KEEPS MOMENTUM (2026-07-04, BUILT CLEAN).** User: G-cycling modes mid-fall froze the player in the air. Cause: handleModeSwitch zeroed velocity_ on every switch (pre-momentum-era "clean slate"). Fix: removed the reset — velocity carries across Survival/Creative/Spectator switches (spectator glides it out via kFlyGlide); flying=false on switch kept.

**MENU HOTKEY LOCKDOWN + WINDOW PERSISTENCE (2026-07-04, BUILT CLEAN).** (a) F3 gated inGame (toggle + overlay draw) and forced off on SaveQuit — no debug at the title; audited other hotkeys (E/Q/R/1-9/scroll/G/F5/mouse) already gated. (b) Window persists: Settings gains windowW/H (clamped 640-7680/360-4320) + fullscreen, saved as "window:WxH"/"fullscreen:N"; Window gains setFullscreen (borderless via glfwSetWindowMonitor primary-monitor mode, remembers windowed pos/size for restore, sets resized_ so swapchain recreates), isFullscreen, windowedSize (live when windowed, remembered when fullscreen). main: EARLY boot Settings peek loads options.txt BEFORE window.init (real settings load stays after camera JSON) so the window opens at persisted size/mode; Options menu gained "Fullscreen: ON/OFF" toggle button applied live in the menu block (compare-then-set avoids thrash); on exit main captures isFullscreen + windowedSize into settings before save.

**TITLE = FULL FREEZE (2026-07-04, BUILT CLEAN).** User (via F3 at title): game time kept ticking at the main menu after Save&Quit. Cause: gdt condition was (menuOpen && inGame) — title (menuOpen, !inGame) fell through to dt. Fix: `gdt = (inGame && !menuOpen) ? dt : 0` — title AND pause now freeze time/weather/drops/mining/player completely; F3 toggle+overlay intentionally NOT gated (user debugs in menus). exitToTitle also resets gameTime to freshWorldSeconds (clean F3 clock). Preview model idle: previewPose now evaluates with pain=ain but lifeTime = real `now` so the model keeps breathing while gameplay time is frozen.

**PREVIEW CURSOR-LOOK + POLISH BATCH + DROPS IN SAVE (2026-07-04, BUILT CLEAN).** User: title preview good as-is; batch of fixes: (1) INVENTORY MODEL WATCHES CURSOR (vanilla): InventoryScreen stores previewLook{Valid_,Yaw_,Pitch_} at both preview draw sites (atan((mouse-anchor)/300px), anchors: survival gx(52),gy(22); creative pxR+89,py+15; reset in build()); main preview block: when inGame&&invOpen&&valid -> baseYaw=180+yaw*0.4 (body), head joint extra rotY(yaw*0.75)+rotX(-pitch*0.6); title keeps drag previewYaw. SIGNS were reasoned not tested — if head turns wrong way flip signs. (2) NO WORLD AT TITLE: world.update gated on inGame (boot/exitToTitle world stays chunk-less; first update next frame after Play). (3) Player Name field REMOVED from Options (title-only now). (4) WALK ANIM GROUNDED: Player::updateAnimation gains striding = !flyingNow && onGround_; limbSwing_ accumulates + amount targets only when striding (air/jump eases to idle). (5) SHIFT+DOUBLE-CLICK the red X (creative destroy slot) wipes inv.slots+inv.main (lastDestroyClick_ 0.4s; single click still trashes carried). User noted for LATER: armor slots, offhand slot, pouch/shulker slot. (6) SEEDS: Noise verified to actually use seed (mt19937 perm shuffle); blank-seed creation now random_device XOR steady_clock (guaranteed distinct); seed printed on world enter. (7) DROPS SAVED: Drops::Item made public + items()/setItems(re-rolls phases); WorldSave Level.drops (DropLine{pos,vel,block-name,count,age}; line "drop:name,count,px,py,pz,vx,vy,vz,age"); levelFromGame/applyLevelToGame round-trip (unknown names skipped, count clamped 64).

**OFFSCREEN PLAYER PREVIEW (2026-07-04, BUILT CLEAN).** User screenshot: world-space title model was mispositioned/weird-rotated (camera pitch + fov distortion) + wanted the preview in BOTH inventories next to armor slots. Replaced with gfx/PlayerPreview.{h,cpp}: 256x512 (1:2) offscreen color(sc->imageFormat)+depth target, own 2 host UBOs (fixed camera lookAt (0,0.98,4.1)->(0,0.93,0), fov 30, aspect 0.5, proj Y-flip), embeds a SECOND ModelRenderer instance (init with own UBOs — ModelRenderer is reusable!), record() = barriers (UNDEFINED->COLOR/DEPTH, then ->SHADER_READ) + own dynamic rendering + viewport before the main pass; transparent clear (0,0,0,0) so UI alpha-blends it; rebuildSkin() for live skin swap (called in upload path after modelRenderer re-init). Renderer: setPlayerPreviewRenderer + setPlayerPreview(active, ModelPose) recorded right after vkBeginCommandBuffer; Renderer.h now includes ModelRenderer.h (stores ModelPose by value). main: pose branch for title REMOVED (modelVisible &&= inGame); previewPose built when (!inGame || invOpen) = idle animator pose at origin * rotY(previewYaw) * scale(1.85/32), full mask; ui.registerImageView(playerPreview.view()) -> playerPreviewTex passed to MenuScreen::Textures.playerPreview AND InventoryScreen::Textures.playerPreview. Draw sites (all letterboxed w = h*0.5, NO crop — square would clip arms): title right column between name box (h*0.20) and Upload Skin (h*0.80) at w*0.78; survival inventory box measured (26,8,52x70) gui px center x=52; creative tab_inventory box (73,6,32x43) center x=89. previewYaw drag zone unchanged (title, mouseX>0.55w, !pointerOverWidget). previewYaw init 180 — if the model shows its BACK by default, flip to 0. Model position/size in boxes derived from pixel measurements of the user's PNGs.

**TITLE v3: PANORAMA + MODEL PREVIEW + SKIN UPLOAD (2026-07-04, BUILT CLEAN 1st try).** User uploaded panorama art + wanted: cubemap background, icon squares centered UNDER Realms, player-model preview on the right (mouse-drag horizontal rotate), name text box ABOVE the model, "Upload Skin..." button BELOW it. gfx/PanoramaRenderer.{h,cpp} + shaders/panorama.{vert,frag}: 6 faces from assets/textures/gui/title/background/panorama_0..5.png (1024^2), inward cube (order: 0 front -Z, 1 right +X, 2 back +Z, 3 left -X, 4 top +Y v=(1-z)/2, 5 bottom -Y v=(z+1)/2 — user must verify seams/top orientation!), LINEAR sampler, push={mat4 viewProj} rotation-only (yaw t*1.8deg/s, pitch -12+3sin(t*.05), fov 80), no depth, 6 draws w/ per-face sets (SkyRenderer-style texture upload). Renderer: setPanoramaRenderer + setTitlePanorama(active,time); when active&&valid: panorama drawn first and sky/chunks/weather SKIPPED (model+held+UI still draw); falls back to live world if <6 faces. main: panoramaRenderer after weatherRenderer; renderer.setTitlePanorama(!inGame, now) each frame (title camera pan REMOVED — camera static, model preview needs it). MODEL PREVIEW: pose block gained `if (!inGame)` branch before the First-person branch: full-body animator.evaluate idle pose placed at eye + front*2.6 + rightXZ*1.15 - (0,1.05,0), rotY(previewYaw) (init 180 = facing camera); drag: in menu block before build, page==Title && leftDown && !menu.pointerOverWidget() && mouseX>0.55w -> previewYaw += dx*0.4 (lastPreviewMx state); heldVisible gained inGame gate. MenuScreen: Action::UploadSkin; hoverAny_/pointerOverWidget() set by all widget helpers; title layout = SP/MP/Realms, 3 icon squares centered (kSquare row), Options/Quit halves row, right column at w*0.78: "Player Name" textField(id 20, 110gui wide) at h*0.20 + "Upload Skin..." button at h*0.80 (model floats between). SKIN UPLOAD: core/FileDialog.{h,cpp} Win32 GetOpenFileNameA (OFN_NOCHANGEDIR, pragma comdlg32); copies picked PNG to assets/textures/entity/player/custom/, slim if stem has slim/alex (lowercased), settings.skin=stem saved immediately, then vkDeviceWaitIdle + modelRenderer.cleanup + buildPlayerModelParts(slim) + re-init = live swap. CMake: PanoramaRenderer.cpp + FileDialog.cpp. Model preview screen-position is eyeballed (front 2.6/right 1.15) — expect user tuning of where it sits vs the right column UI.

**M6 SAVE/LOAD + FULL VANILLA TITLE + WORLD LIST (2026-07-03, BUILT CLEAN 1st try).** User uploaded the widget sprites and asked for: vanilla title (Singleplayer, Multiplayer LOCKED, Realms LOCKED, friends/language/accessibility icon squares, Options, Quit), a worlds list, and the save system incl. profile (name/skin/language/fov). SAVE SYSTEM: world/WorldSave.{h,cpp} — saves/<folder>/level.txt (text key:value; items+cracks stored by block NAME so registry changes don't corrupt) + chunks/c_x_y_z.bin (RLE pairs raw,count; magic MCCH v1); Level struct {name,seed,timeSeconds,mode,flying,hasPlayer,playerPos,yaw,pitch,selected,hotbar[9],main[36],cracks}; static list(root) mtime-sorted + world deletion + makeFolder (sanitized unique). ONLY EDITED chunks saved: Chunk gains atomic `edited` + rawBlocks(); World::setState marks edited; gen worker overlays save->loadChunk AFTER WorldGen (thread-safe per-file handles; chunk stays un-edited after overlay); unloadDistant writes edited chunks before erase; World::flushEdited() for quit/autosave; World::setSave (init resets save_=null; ringOffsets_.clear() for re-init; ThreadPool verified restartable). main.cpp lifecycle lambdas: levelFromGame / saveWorldNow / destroyWorldGpu (waitIdle + world.destroy + drops.clear + mining.setCracks({}) + renderer clears) / applyLevelToGame (fresh world hovers creative until spawn scan applies spawnMode) / enterWorld / exitToTitle (fresh random panorama world) / openWorldsPage. Boot world = random-seed panorama, never saved; spawnPlaced starts TRUE (title hovers y=100). Autosave 60s + save on window close + "Save and Quit to Title" in pause. GameTime seconds()/setSeconds()/freshWorldSeconds(). Mining::setCracks(), Drops::clear(). MENUS: MenuScreen pages None/Title/Worlds/CreateWorld/ConfirmDelete/Pause/Options; Actions OpenWorlds/PlayWorld/CreateWorld/DeleteWorld/Resume/Done/SaveQuit/Quit (+payload getters actionFolder/createName/createSeedText/createSurvival); widgets skinned via Textures struct (button/highlighted/disabled 3-sliced 4px corners via texQuad, slider handle 8x20 drawn 1:1, text fields, icons; flat fallback if tex id 0); title = SP + disabled MP/Realms + bottom row [friends][language][Options 98][Quit 98][accessibility] + playerName/language bottom-right; Worlds page: scrollable rows (name + gray "Mode - Day N - folder"), click select, double-click play (0.35s), Play/Create/Delete(confirm)/Cancel; CreateWorld: name + seed text fields (Window::consumeText char callback; backspace repeat 0.35/0.05 in main) + Game Mode toggle; seed blank=random_device, numeric=stoul, words=std::hash. Options gained a Player Name text field. Settings: playerName/skin/language persisted; pickPlayerSkin(dir, preferred) reuses the remembered skin stem across launches; friends square uses icon/invite.png. NOT saved (told user): ground drops + weather (fresh each load); the three icon squares are visual-only.

**TITLE + PAUSE + OPTIONS MENUS (2026-07-03, BUILT CLEAN).** User chose menus BEFORE save/load (M6 next; pause "Quit Game" becomes "Save and Quit to Title" with M6). New: core/Settings.{h,cpp} (fovDeg 30-110, sensitivity 0.008-0.24 shown as % where 0.08=100%, renderDistance 2-12; key:value options.txt via mc::resolve, loaded at startup AFTER camera-preset JSON so file wins, saved on Done + app exit). Camera: addLook now uses new member lookSensitivity (3rd param removed). ui/MenuScreen.{h,cpp}: pages None/Title/Pause/Options, Actions None/Play/Resume/Done/Quit; flat-quad vanilla-style buttons (200x20 gui px, kScale=3, hover = white border + bluish fill) + sliders (label on track, 8px handle, activeSlider_ drag id) — no widget art in assets yet (user can drop widget/button.png later for skinning); back() = ESC step-back (Options->returnPage_, Pause->None, Title stays). main.cpp wiring: boots to title (inGame=false, cursor released via setCursorCaptured(false)); title = live world panorama (camera.yaw += 3*dt) with vignette + big "MineCraftCPP" + Singleplayer/Options/Quit; ESC = close inv > menu.back() > open Pause; E gated inGame && !menu.open(); gdt = (menuOpen && inGame) ? 0 : dt freezes player/drops/mining/gameTime/weather while paused (world streaming keeps running); gates switched from !invOpen to !uiOpen (+inGame) for interaction/outline/Q/scroll/whiff/R; HUD only inGame, crosshair !uiOpen; menu.build block after invScreen block applies settings live (baseFov, lookSensitivity, world.renderDistance) then handles actions. TESTED: builds; user in-game verify pending.
**PER-BLOCK BREAK PROGRESS + CRACK HEALING (2026-07-03, BUILT CLEAN).** User: release should NOT reset cracks instantly — they fade at half the dig speed ("resets double the time it took"), and break phase must be part of the world save. New game/Mining.{h,cpp} (+CMakeLists): vector<Crack{ivec3 cell, BlockState state, float progress}>; dig(cell,state,need,dt) accumulates + returns true on completion (resumes leftover progress; restarts if the cell holds a different state); update(world,dt) heals every non-dug entry at 0.5*dt, drops entries at <=0 or when world.getState != stored state (swap-remove with dugThisFrame_ index fixup). Multiple blocks can carry cracks at once. main.cpp: mineCell/mineProgress deleted; survival branch = need>0 ? mining.dig : need==0 instant; after the interaction block each frame: mining.update + build draws. Renderer API changed: setCrack(stage,boxes) -> setCracks(vector<Renderer::Crack{AABB box,int stage}>), layer lookup per draw. SAVE NOTE: Mining::cracks() is the serializable state — fold into M6 world save (told user save/load doesn't exist yet; this ships with M6).
**THROW -20% + MINING CRACK OVERLAY (2026-07-03, BUILT CLEAN).** spawnThrown impulse 6.5->5.2, up 1.3->1.05 (inherited player velocity untouched). Crack overlay (survival timed mining): copied destroy_stage_0-9.png (16x16) from add-later into assets/textures/blocks; main.cpp appends the 10 names to registry.textureNames() before textures.build. New shaders/crack.{vert,frag}: reuses outline unit-cube vbuf + pipelineLayout_ + PushConstants{chunkPos=boxMin, boxSize; boxSize.w=texture layer}; vert inflates 0.003 off faces but passes UNinflated world pos; frag derives face UVs from cross(dFdx,dFdy) dominant axis + fract(world) so slabs/snow show the matching crop; alpha blend, depth test on/write off, discard a<0.05. Renderer: createCrack() clone of outline pipeline w/ crack shaders, setCrack(stage,boxes) (-1=off), drawn after outline before weather; layer looked up per frame via texture_->layer("destroy_stage_N"). main.cpp: in survival mining branch, when !doBreak && need>0 && mineProgress>0: stage=min(9,progress/need*10), boxes=hitBlock.collisionBoxes offset by cell (fallback full cell for no-collision blocks like plants); renderer.setCrack after the edit block each frame. Creative stays instant-break (no crack).
**MOMENTUM -5% MORE + DROP FRICTION (2026-07-03, BUILT CLEAN).** Second tune pass: rates +5% (kGroundAccel 16.8, kGroundStop 14.7, kAirAccel 3.7, kAirDrag 0.42, kFlyAccel 12.0, kFlyGlide 4.2). Drops.cpp ground friction 8/s -> 20/s ("dirt not ice") + snap-to-stop when horizontal speed < 0.25 m/s, so thrown drops skid ~0.7m at sprint speed instead of gliding meters.
**MOMENTUM TUNE + FORWARD-ONLY SPRINT (2026-07-03, BUILT CLEAN, user: "sweet spot").** User liked the momentum feel but wanted ~15% less weight and sprint/fly boost gated to forward movement. Rates bumped ~15%: kGroundAccel 14->16, kGroundStop 12->14, kAirAccel 3->3.5, kAirDrag 0.35->0.4, kFlyAccel 10->11.5, kFlyGlide 3.5->4.0. Sprint latch (walk + fly) now requires W held: `if (Ctrl && forward) sprinting_=true; if (!moving || !forward) sprinting_=false;` — no sideways/backward sprint. Gotcha hit: moveFly already has local `glm::vec3 forward` (camera dir), so the key flag there is named `fwdKey`.
**PLAYER MOMENTUM PHYSICS + THROW VELOCITY INHERITANCE (2026-07-03, BUILT CLEAN).** User: player "feels light like a piece of paper", A/D turns instant, no air physics, throws ignore run speed. Rework in Player.cpp: moveWalk no longer snaps velocity_.xz to wish*speed — exponential approach `v += (target-v)*(1-exp(-rate*dt))` with rates kGroundAccel=14 (moving), kGroundStop=12 (friction, no keys), kAirAccel=3 (weak mid-air steering — jumps keep takeoff momentum), kAirDrag=0.35 (coasting). Sprint jump adds wish*kSprintJumpBoost(1.8) on takeoff (vanilla leap). moveFly now velocity-based too: normalize(move), same exp approach with kFlyAccel=10 / kFlyGlide=3.5 → creative flight drifts/glides to a stop; collisions zero the blocked axis; landing sets flying=false as before (also fixes stale velocity_ during flight that kept updateBodyYaw in "moving" state). Player.h: added `glm::vec3 velocity() const`. Drops::spawnThrown gained `inheritVel` param (default 0) added to item.vel; both main.cpp call sites (in-world Q, inventory-screen Q) pass player.velocity() → running throws carry momentum. Tuning knobs are the k* constants at the top of Player.cpp.
**DROP PILE MERGING + CLUSTER VISUALS (2026-07-03, BUILT CLEAN):** per user optimization: same-id drops within 1.0 blocks merge into one entity at the COUNT-WEIGHTED centroid (pos+vel weighted avg; longer remaining pickup delay kept; O(n^2) pass at end of Drops::update, swap-remove). Merge refuses when combined > kMaxStack(64) so a full pile stays put and newcomers seed the next agglomerate. buildDraws: piles render min(count,16) mini cubes clustered (phase-hashed stable offsets +-0.15, tiny y stagger, spin offset v*0.4; first cube unoffset); real count unlimited to 64 behind the scenes. Draw calls = 16 max per pile via heldRenderer.

**Q VOMIT TWEAKS (2026-07-03, BUILT CLEAN):** spawnThrown from the HEAD (eye + dir*0.4, no lowering), power dir*6.5 + up 1.3; holding Q auto-repeats every 0.12s (qDropCooldown, first throw instant, reset on release; shift+held Q dumps whole stacks repeatedly). In-screen Q stays edge-only.

**Q DROP (2026-07-03, BUILT CLEAN):** Drops::spawnThrown(id, count, eye, dir): tossed forward (dir*4.5 + up 1.2 + jitter, spawn eye+dir*0.4 lowered 0.4) with pickupAfter = 1.2s delay (Item field; magnet+pickup gated on age >= pickupAfter so throws aren't vacuumed back; mined drops keep 0). GAMEPLAY: Q edge (qPrev) while !invOpen && canEdit throws from the SELECTED hotbar stack - 1 item, shift = whole stack (user chose shift over vanilla ctrl); decrements/clears the stack (both modes). IN-SCREEN: Input.dropPressed (Q edge); handleSlotClicks: Q over any slot queues {id, 1 or stack} into pendingDrops_ + decrements; main drains via takeDrops() (swap-out) after build and spawnThrown's each in the look direction. Works in survival screen + creative inventory panel.

**SPLIT-DRAG LIVE PREVIEW (2026-07-03, BUILT CLEAN):** while LMB-dragging a carried stack, each visited slot now shows a GHOSTED icon (alpha 0.65) + projected count (existing + share) live during the drag; cursor count shows what would remain (dragPreviewRemaining_ member, reset -1 each build, computed in the ghost loops which mirror the commit math: per = max(1, count/N), sequential, capped by stack space). Both screens (creative inventory panel + survival). Commit on release unchanged.

**DOUBLE-CLICK COLLECT + CATALOG DISCARD-FIRST (2026-07-03, BUILT CLEAN):** (1) collectAll: plain LMB double-click (<0.4s) ON THE SAME SLOT while carrying sweeps every same-id stack from hotbar+storage onto the cursor up to 64 (user spec: only same-slot consecutive clicks trigger - lastLeftCell_/lastLeftTime_ members; clicking another slot resets). Checked before drag-start; cancels dragLeft_. Typical flow: pick wool from slot 15 then quickly click 15 again = whole inventory's wool gathered. (2) Creative catalog while carrying a DIFFERENT block: any pick-click (L/M/R) only DISCARDS the carried stack (vanilla creative trash); next click picks (LMB one/+1 same id, MMB 64, RMB 32).

**DRAG INTERACTIONS + CREATIVE PICK AMOUNTS (2026-07-03, BUILT CLEAN, awaiting user verify):** handleSlotClicks member (shared: survival cells + creative inventory panel rcell, both call it; drags COMMIT here even when cursor leaves slots). LMB w/ held on empty-or-same-id slot: starts dragLeft_ w/ dragCells_ (deferred!); dragging over more compatible unvisited slots adds them; ON RELEASE: 1 slot = clickStack (place all/merge), N slots = split held.count/N each (min 1, capped by stack space, remainder stays on cursor). Different-id slot = immediate swap. RMB w/ held: placeOne + dragRight_/rightVisited_ - keeps placing ONE into each NEW slot while held down. RMB empty cursor: pickHalf. Shift+LMB quickMove, shift+dbl massMove (all inside handler). Input gained rightDown + middlePressed (main tracks midPrev, passes rightNow level). CREATIVE CATALOG PICKS (user spec): LMB = ONE item (+1 if already carrying same id, capped 64), MMB = full stack, RMB = half stack (32), shift+LMB = stack straight to inventory. NOTE: left-drag has no live preview (commits on release); dragged slots show no visual marker yet.

**VANILLA STACK INTERACTIONS + EMPTY START (2026-07-03, BUILT CLEAN, awaiting user verify):** per user spec, both screens: RMB w/ empty cursor = pick HALF (ceil, pickHalf); LMB = pick/place/merge/swap (clickStack); RMB w/ held = place ONE (1-item stack just places, placeOne unchanged); SHIFT+LMB = quickMove hotbar<->storage (addToRange helper: top-up then fill, returns leftover); SHIFT+LMB DOUBLE-CLICK (<0.4s, lastShiftClick_ member) while carrying = massMove: deposits held + every stack of that id from the hovered section into the other; creative catalog SHIFT+LMB = inv.add(id,64) straight to inventory; creative catalog scroll ONLY while cursor over the catalog panel (overCatalog bounds check). Input struct gained shiftDown + time (main passes L/R shift level + glfwGetTime now). DEBUG HOTBAR SEEDING REMOVED - inventory starts empty in both modes (pick from creative inventory). NOTE: massMove direction = hovered section -> other; vanilla nuances (double-click collect-to-cursor without shift) not implemented.

**RE-EDITED CONTAINER PNGs REMEASURED (2026-07-03, BUILT CLEAN, awaiting user verify):** user edited nearly all container images; pixel-rescanned (System.Drawing, slot-grey 139 + alpha bounds). NEW LAYOUTS IN USE: tab_items.png 195x168, catalog 9x8 rows y18..144 cols x9 stride 18, scrollbar x175 track kBarH=142. tab_inventory.png 195x154, storage 4 ROWS (9,54..108), hotbar (9,130), destroy X (173,130). inventory.png (survival) 176x184, storage 4 rows (8,84..138), hotbar (8,160); armor col/player box/2x2 craft+arrow+result visuals inert. Inventory::kMainSlots 27->36 (4 rows; add()/screens auto-adapt via kMainRows = kMainSlots/9). Panels top-aligned at shared py (creative pair heights differ 168 vs 154). Also present but NOT wired yet (no UIs): crafting_table.png (edited: 2x2? scan showed 3x3 cols x30..66 y17..53 - actually 3x3 craft), furnace.png, generic_54.png (chest, 6 rows storage y18..108 + player 4 rows y140..194!). NOTE for future container UIs: user's generic_54 also has 4-row player inventory.

**SIDE-BY-SIDE CREATIVE SCREEN + YAW FIX + BATCH-2 BLOCKS (2026-07-03, BUILT CLEAN, awaiting user verify):** (1) USER EDITED THE PNGs: tab_items.png = pure catalog 9x6 grid (rows y18..108, cols x9..153 stride 18, NO title-row change, NO hotbar row, scrollbar x175 track ~106) - measured via System.Drawing pixel scan for color 139,139,139. tab_inventory.png = armor-ish slots around player box (inert, y20/y33), storage 27 at (9,54+18r), hotbar (9,112), DESTROY X SLOT at (173,112) = trashes carried stack. buildCreative now draws BOTH panels side by side centered as a group (kPanelGap 4, group 394 GUI px * 3 = 1182 fits 1280): catalog+tabs LEFT, inventory panel RIGHT (shows inv.slots + inv.main with full stack ops via shared clickStack/placeOne helpers, counts, tooltip, highlight incl destroy). Z-ORDER FIX: unselected tab sprites drawn BEFORE the panels (behind, vanilla), selected AFTER; tab icons last. Textures struct gained creativeInventoryPanel. Survival screen unchanged. (2) YAW: engine meshes face 90deg off vanilla model space -> global correction rotY(-90) ("90deg to the RIGHT" per user, after first trying +90 left) appended after the display transform in BOTH ItemIconRenderer gui model and main.cpp held dispM. (3) BATCH-2 (~85 blocks): granite/diorite/andesite +polished, deepslate family (pillar + cobbled/bricks/tiles/polished), tuff, calcite, dripstone, amethyst, mud family, nether/red-nether/quartz/end-stone bricks, purpur (+pillar), basalt/polished (pillar), blackstone family, red_sand(stone) family, copper/cut_copper/raw_* blocks, terracotta +5 colors, 8 concretes, remaining 8 wools, dark_oak/acacia/jungle planks+logs+woodSet(fence/gate/door), cherry_planks, hay_block, honeycomb, note_block, jukebox, target; +8 slabs (granite/diorite/andesite/nether_brick/red_sandstone/dark_oak/acacia/jungle -> 17 slab materials = 289 hidden combos) +8 stairs. 68+13 files copied (batch2 textures + stairs/fence_inventory/gate/door_bottom_left model jsons). CRITICAL SCALE FIX: ItemIconRenderer now SKIPS hiddenFromHotbar blocks entirely (no mesh/image/UI slot - 289 hidden combos would have blown the descriptor pool); iconTexId stays -1 for them; kMaxTextures 256->512. Registry now ~460 blocks (~170 visible).

**VANILLA TAB SPRITES (2026-07-03, BUILT CLEAN):** user dropped `assets/textures/gui/sprites/container/creative_inventory/` (tab_top/bottom_selected/unselected_1..7 all 26x32, scroller.png + scroller_disabled.png 12x15). InventoryScreen::init now takes a Textures struct {creativePanel, survivalPanel, tabSelected[3], tabUnselected[3], scroller, scrollerDisabled} (main registers tab_top_*_1..3); tabs draw as real sprites 26x32 at (27*i, panelTop-28) - drawn AFTER the panel so the selected tab's bottom 4 rows overlap/erase the border seam like vanilla (unselected bottoms are transparent); tab icon at +5,+9; scrollbar thumb = scroller/scroller_disabled sprite. Flat-quad fallbacks kept for any id==0. Tab hit spacing kTabStride=27.

**INVENTORY FINISHED: TABS + SURVIVAL SCREEN + ~60 NEW BLOCKS (2026-07-03, BUILT CLEAN, awaiting user verify):** (1) TABS: Block.h kTabBuilding/Nature/Functional (+kTabCount), Properties.tab + .inTab(); existing blocks tagged. InventoryScreen creative: catalog filtered by tab, 3 drawn tab buttons ABOVE the panel (26x28 GUI px, quads: selected 0xC6 grey +4px taller, unselected 0.42; icon = byName bricks/grass/oak_door), title = tab name, tab click resets scroll + rebuilds frame w/ cleared click. (2) SURVIVAL SCREEN: Inventory gained main[27] (add() tops up hotbar then main, then fills); InventoryScreen::build(..., bool creative) branches: survival draws container/inventory.png (176x166, storage item cells (8+18c, 84+18r) r0..2, hotbar y142; armor/craft visuals inert), full stack ops: left = pick/place/merge/swap, right = place ONE, counts drawn everywhere, tooltip; held_ now ItemStack (creative pick = {id,64}); setOpen(open, Inventory*) returns carried stack via add() on close (main passes &inv at E + Esc); main build passes mode==Creative. drawTooltip/drawCount helpers. (3) BLOCKS: +68 textures copied add-later->assets/textures/blocks (83 total), +13 model jsons (stairs/fence_inventory/gates/door_bottom_left/oak_pressure_plate for display transforms; cubes+slabs use defaults). registerDefaults helpers (hardCube pickaxe-req, softCube prefers+sound, woodSlab/stoneSlabF/woodStairs/stoneStairs, woodSet=fence+gate+door): bricks family, stone_bricks x4, smooth_stone, mossy_cobble, sandstone(faces)+smooth, quartz(pillar), 7 mineral blocks, obsidian(50s), bedrock(strength -1 = unbreakable), spruce/birch planks+logs(pillar)+fence+gate+door, iron_door(pickaxe), bookshelf(column), 8 wools(shears), crafting_table(faces), glowstone(light 15), tnt(faces), 8 ores, gravel, clay, pumpkin/melon(pillar), snow_block, ice, packed_ice, sponge, netherrack, soul_sand, end_stone, oak_pressure_plate; slabs: oak/spruce/birch/brick/stone_brick/smooth_stone/sandstone (9 slab materials -> 81 hidden double combos); stairs: brick/stone_brick/sandstone/spruce/birch. SKIPPED deliberately: spruce/birch leaves + grass variants (vanilla textures are GRAYSCALE colormap-tinted -> would render grey; needs biome tint), animated textures (sea_lantern/magma/prismarine - TextureArray needs uniform dims). Total registry ~170 blocks; UI textures ~180/256.

**SURVIVAL LOOP: ITEM DROPS + PICKUP + STACK COUNTS (2026-07-03, BUILT CLEAN, awaiting user verify):** (1) Inventory.h: ItemStack{BlockId id; int count; empty()}; Inventory slots = array<ItemStack,9>, kMaxStack 64, selectedBlock() (AIR when empty), consumeSelected() (survival place, clears at 0), add(id,n)->leftover (tops up stacks then empty slots). Seeding gives {id,64}. (2) NEW src/game/Drops.{h,cpp}: Item{id,count,pos(bottom-center),vel,age,phase}; spawnFromBlock rolls Drop pools (chance, min..max, byName; unknown item names logged+skipped - saplings etc until non-block items exist), pop velocity random; update(): gravity 18, axis-wise collide vs world.boxCollides (0.1 half / 0.2 tall box), ground friction, magnet glide <1.6 (6 blocks/s), pickup <0.55 -> inv.add (leftover stays on ground), despawn 300s or y<-64; buildDraws: bob sin(age*2.2+phase)*0.06+0.1, spin age*1.4, scale 0.25, centered. Draw struct {mat4 m; uint16 block}. (3) Renderer: includes game/Drops.h, setDropDraws(vector<Drops::Draw>), recorded via heldRenderer->record per drop right after the held block (per-block meshes already exist there). (4) Hud::build gained (const Font*, const int* slotCounts): counts >1 drawn bottom-right of icon (x+17*s-width, y+9*s) white+shadow, vanilla pos. (5) main: survival break -> drops.spawnFromBlock (creative: no drops, still instant); survival place consumes 1 at all THREE success sites (merge/combine/place); drops.update after world.update (collect unless spectator); HUD passes counts (empty slot icon -1). (6) InventoryScreen: creative click writes {id,64} full stack, lift/clear use ItemStack (re-drop = fresh 64 - creative semantics), number-assign {id,64}. CMake +Drops.cpp. NOT yet: mining crack overlay, drop merging, non-block items, drops in save.

**3D FANCY CLOUDS (2026-07-03, BUILT CLEAN, awaiting user verify):** flat plane replaced with vanilla-fancy boxes: clouds.png alpha kept as CPU mask (loadTexture gained maskOut param, alpha>127, maskW_/maskH_); buildCloudMesh(anchorCx, anchorCz) emits 12x12x4 boxes per cloud texel in radius 30 cells, in FOUR contiguous sections (top / bottom / x-sides / z-sides; sides only where neighbor texel empty; every box face flat-samples its texel center uv; mask wraps mod 256). Mesh is LOCAL to the anchor cell, cached, rebuilt only when floor((camX+drift)/12) or floor(camZ/12) changes (~20s or 12 blocks); smooth motion via push offset = (acx*12 - drift, 192, acz*12) - rain.vert push grew to {vec4 tint; vec4 offset}, pos += offset.xyz (rain pushes offset 0). Drawn as 4 draws w/ per-face brightness {1.0,.7,.9,.8} * cloudBrightness, alpha 0.8, own cloudVbuf_[2] (kMaxCloudVerts = 61^2*36 verts) and own cloudPipeline_ with depth-write ON (self-occlusion, no double-blend; createPipeline(bool depthWrite) shared layout guarded). drift 0.6 blocks/s. Tunables: kCloudRadius/kCloudCell/kCloudThick/kCloudY in WeatherRenderer.h, kDrift + faceLight in record().

**CLOUD LAYER (2026-07-03, BUILT CLEAN, awaiting user verify):** flat vanilla cloud plane added INTO WeatherRenderer (right blend/depth/REPEAT sampler already there). env/clouds.png; plane y=192, quad follows camera (half-extent 480) but UVs world-anchored (1 texel = 12 blocks, texture tiles every 3072 blocks) so clouds stay put; drift 0.6 blocks/s via u += time*0.6/3072; alpha 0.8; brightness param dims night (0.18 floor) + rain (-35%): main computes cloudB = clamp(0.18+0.82*max(0,sunHeight*2)) * (1-0.35*wet), Renderer::setWeather gained 3rd param. loadRainTexture generalized to loadTexture(path, image&, alloc&, view&, set&) called for rain + clouds (params shadow member names so body unchanged - slightly ugly, works); pool samplers 2, maxSets +1; record() restructured: rain verts (only if raining) + cloud quad appended to same per-frame VB, two draws w/ different tex set + push tint; kMaxVerts +6; cleanup destroys cloud image/view. Clouds drawn even when clear. If user reports clouds too low/high: kCloudY; too fast: kDrift; scale: kSpan.

**WEATHER CYCLE + STAR SIZE (2026-07-03, BUILT CLEAN, awaiting user verify):** stars shrunk per user ((0.35+0.45rand) -> (0.12+0.10rand) * dist/100). WEATHER: NEW `src/core/Weather.h` (header-only state machine: clear 300-900s <-> rain 90-240s, first shower 120-300s after launch, intensity 0..1 eased ~4s tau; toggle() flips immediately) + NEW `src/gfx/WeatherRenderer.{h,cpp}` + `shaders/rain.{vert,frag}`. RAIN RENDERING: per-frame rebuilt host VB (kFramesInFlight), one camera-facing quad per column in radius 8 (17x17) around the player; column scan from camY+15 down to camY-20 finds first non-air block -> quad spans (surface+1, camY+15]; roof overhead -> scan hits roof -> column collapses -> DRY under cover (works without lighting/heightmap); no solid -> full span. UVs: u = colRand..+1 (REPEAT sampler), v = y*0.25 + time*3.2 + colRand*7.31 (streaks fall DOWN; if user says rain "falls up" flip the time sign); alpha = 0.65*intensity, normal alpha blend, depth TEST on / write OFF, drawn AFTER world+outline, BEFORE UI (Renderer::recordCommandBuffer). uses env/rain.png (snow.png awaits biomes). DIMMING: main: skyColor *= (1-0.55*wet); starAlpha *= (1-wet); SkyRenderer::record gained brightness param -> sun/moon tint (1-0.75*wet). Renderer: setWeatherRenderer/setWeather(intensity, time=glfwGetTime), members weather_/weatherIntensity_/weatherTime_; setCelestial gained brightness. R KEY = debug weather toggle (edge, disabled while inventory open; in controls print). Weather NOT yet in save (no save system).

**CELESTIAL SKY PASS (2026-07-03, BUILT CLEAN, awaiting user verify):** sun + moon phases + stars riding GameTime. NEW `src/gfx/SkyRenderer.{h,cpp}` + `shaders/sky.{vert,frag}`. Textures: user's MODERN 1.21.4+ layout - `assets/textures/environment/celestial/sun.png` + `celestial/moon/<phase>.png` (8 individual files, NOT the old moon_phases.png grid). Phase order (vanilla, phase=(dayCount-1)%8, day1=full): full_moon, waning_gibbous, third_quarter, waning_crescent, new_moon, waxing_crescent, first_quarter, waxing_gibbous. RENDERING: drawn FIRST in the world pass (Renderer::recordCommandBuffer right after scissor, before chunk pipeline), depth test+write OFF (terrain covers sky), additive blend ONE/ONE with premultiplied rgb*a in sky.frag (transparent texels add 0). Pipeline: 2 set layouts (set0 = shared camera UBO per frame, set1 = per-texture sampler; pool 2 UBO + 10 samplers), push {mat4 model; vec4 tint} 80B VERTEX, dynamic viewport/scissor, sampler NEAREST. GEOMETRY: unit quad w/ normal +X (model = T(camPos) * Rz(sunAngle) * T(kDist,0,0) * S(size)); kDist=300 (far plane 1000), sun half-size 0.30*dist, moon 0.20*dist (vanilla ratios); stars = 900 quads baked on the sphere (fixed seed 9469, sizes ~0.35-0.8 * dist/100, random spin), drawn with the same Rz rotation (sky rotates as one), tint.a = starAlpha, white 1x1 texture. GameTime convention verified: sunAngle = dayFraction*2pi, 0 = dawn sun at +X horizon, pi/2 = noon overhead, moon = opposite side (extra Rz(pi)). Missing textures: logged, element skipped (game still runs). WIRING: Renderer stores SkyRenderer* via setSkyRenderer (avoids drawFrame signature growth), setCelestial(sunAngle, moonPhase, starAlpha) per frame from main, camEye_ = camera.renderEye() captured in drawFrame; main: skyRenderer.init after heldRenderer w/ renderer.cameraUBOs(), starAlpha = clamp(-sunHeight*3, 0, 1) (fades in right after sunset); cleanup first among sub-renderers. CMake: +SkyRenderer.cpp; sky shaders auto-globbed. NEXT in this arc: weather cycle (clear<->rain state machine, rain.png/snow.png camera-follow quads, sky darken) - textures already in environment/; also clouds.png available for a cloud layer.

**GROUND SPAWN + J POSE REMOVED + VANILLA BLOCK TEXTURES STAGED (2026-07-03, BUILT CLEAN):** (1) SPAWN: player no longer starts flying - main.cpp holds the creative hover (flying=true default kept for the wait) until the spawn column (0,0) has generated terrain, then scans y255->0 for the first non-air block, sets feet on it (eye = y+1+1.62 at x/z 0.5) and flying=false -> spawns walking at ground level. spawnPlaced flag checked right after world.update each frame. (2) J DEBUG POSE fully removed per user: Player debugPose_/jPrev_/debugPose()/handleDebugPose gone, update() movement no longer wrapped, updateAnimation debugPose target-0 line gone, main.cpp frozen-inspection-pose branch deleted. J is a free key now. (3) TEXTURES: sparse git clone (depth1, filter=blob:none, branch 1.21.11) of Faithful-Pack/Default-Java -> copied assets/minecraft/textures/block into `add-later/textures/block` = 1210 files, 1.8MB (vanilla 1.21.11 default block textures incl .mcmeta animation files; NOT read into context per user credit concern; dormant per add-later convention - pull into assets/textures/blocks when blocks need them). Clone removed from scratchpad after copy.

**STEP-UP LANDS FLUSH (2026-07-03, BUILT CLEAN):** user report: stepping onto snow layers (and other low shapes) lifted the player too high then he "falls a bit until it hits the ground" every step. Cause: tryStepUp lifted by the FIXED kStepHeight (0.55) and relied on gravity to settle (comment even said so) - snow 2-layer collision is 0.125 -> 0.425 of visible falling; even slabs popped 0.05. Fix (Player.cpp tryStepUp): query world.boxCollides on just the body's step-height SLICE (feet+e .. feet+kStepHeight, player footprint) with outTopY -> highest colliding box top = exact surface; lift = topY - feet; reject lift<=e or >kStepHeight (a full wall overlaps the slice but its top pokes above -> rejected on height; head-height-only blockers return no slice hit); then collidesAt(up) headroom check; eye.y += lift lands feet EXACTLY on the surface (same convention as the landing snap topY + kEyeHeight). Same-frame vertical pass then snaps onGround - zero pop. kStepHeight still 0.55 (user rule: max auto-step = slab height, taller = jump).

**MODEL DISPLAY TRANSFORMS + SLAB COMBINE FIX (2026-07-03, BUILT CLEAN, awaiting user verify):** (1) SLAB BUG (user report): tryCombineAt ignored click position - aiming at the TOP part of a sibling's face in front of an existing TOP slab still filled the cell with a bottom slab. Fix: tryCombineAt signature gained PlacementCtx (Block.h base + SlabBlock + LayerBlock forward + main.cpp call passes pctx); SlabBlock computes wantTop via placementState(ctx) (normal.y>0 -> bottom, <0 -> top, side -> hitY fract>0.5) and returns false when wantTop == existingTop (that half occupied -> NO placement). LayerBlock::tryCombineAt now forwards the real ctx. (2) DISPLAY TRANSFORMS (user request "display table with gui table so it manipulates alone where the item/block sits in the slot ui and in hand 1st/3rd person"): NEW `src/world/ModelDisplay.{h,cpp}` - ItemTransform{rotationDeg, translation (16ths of a block), scale (blocks)} + DisplaySet{gui, firstPerson, thirdPerson}; loadModelDisplay(modelsDir, modelName) parses assets/models/block/<name>.json via anim/Json, walks "parent" chain (modelStem strips "minecraft:block/"), nearest definition per entry wins, missing file/entry -> vanilla block/block defaults (gui [30,225,0] s0.625; fp_right [0,45,0] s0.40; tp_right [75,45,0] t[0,2.5,0] s0.375). Block.h includes ModelDisplay.h, stores DisplaySet display_ + display()/setDisplay + virtual itemModelName() (default name_); overrides: FenceBlock -> name+"_inventory" (gui yaw 135), LayerBlock -> "snow_height2" (thin_block chain lifts hand items +4.2), DoorBlock -> name+"_bottom_left" (no display there -> defaults; future mesher hook). BlockRegistry::loadDisplayTransforms(modelsDir) inline in header; main calls it after resolveTextures, BEFORE iconRenderer.init. Staged models WITH display sections: block, fence_inventory, stairs (gui [30,135,0]!), template_fence_gate, thin_block. ICON RENDERER: new `shaders/icon.vert` (UBO view/proj + push_constant mat4 model; chunk.frag reused), pipeline pushRange = sizeof(mat4), UBO view = IDENTITY, proj = ortho(-0.5..0.5) (one block unit across = vanilla 16px slot semantics, 0.625 gui scale fills icon ~98% like MC; icons ~13% bigger than the old +/-0.9 window), draw loop pushes per-block model = T(gui.translation/16) * Rx*Ry*Rz(gui.rotationDeg) * S(gui.scale) - moving rotation from view to model matrix is identical math, so old hardcoded look == model rotX30*rotY45; new data gives rotY225 (cubes look same, stairs/gates/fences now face vanilla way). HELD BLOCK (main.cpp): replaced hardcoded rotY(45)*scale(4.5)*center with per-perspective data: disp = display().firstPerson (1st) / .thirdPerson (3rd); kPxPerBlock = 32/1.85 (player-model px per block); dispM = trans(disp.translation * kPxPerBlock/16) * rotX*rotY*rotZ(disp.rotationDeg) * scale(disp.scale * kPxPerBlock); heldM = rightArm * trans({handX,10.5,-4}) * itemDelta * dispM * center. NOTE: fp scale now vanilla-exact 0.40 blocks = 6.92px vs old tuned 4.5px (user had asked "decrease" when it was oversized pre-data; if they say too big again, add a global multiplier constant). 3rd person now gets vanilla tp tilt rotX75 + y+2.5 lift. Rotation signs mapped DIRECT (evidence: old working rotY(+45) == vanilla fp [0,45,0]); if user reports mirrored/flipped icon or hand block, flip per-axis signs (the animator's Bedrock mapping (-x,y,+z) does NOT necessarily apply - this is Java model json space). CMake: + src/world/ModelDisplay.cpp; icon.vert auto-globbed.

**CREATIVE INVENTORY SCREEN (2026-07-03, BUILT CLEAN, awaiting user verify):** E opens the vanilla creative screen using the user-dropped `assets/textures/gui/container/creative_inventory/tab_items.png` (256x256 sheet, GUI = top-left 195x136, scale x3 like the hotbar; vanilla layout: 9x5 catalog item cells at (9+18c, 18+18r), hotbar row items at (9+18i, 112), scrollbar track x175 y18 12x112, thumb 12x15 = drawn quads since the user has no scroller sprite). NEW FILES: `src/game/Inventory.h` (struct Inventory: array<BlockId,9> slots + selected + selectedBlock(); replaced main.cpp's hotbar vector + parallel slotIcons vector - HUD slot icons now built per frame from iconRenderer.iconTexId), `src/ui/InventoryScreen.{h,cpp}` (open/setOpen/build/hoveredBlock; Input struct {mouseX/Y framebuffer px, leftPressed edge, leftDown level, rightPressed edge, scroll}; catalog = all registry blocks !hiddenFromHotbar in id order; wheel scrolls rows + draggable thumb; click catalog = pick copy onto cursor (held_), click hotbar slot = drop it there / lift existing, click empty space = discard, right-click hotbar slot = clear, hover + 1-9 assigns hovered block to that slot (handled in main via hoveredBlock(), level-triggered = idempotent), tooltip = prettified name (underscores->spaces, title case) w/ vanilla purple-bordered box, hidden while carrying; grey-quad fallback panel if texture missing (registerTexture returns 0)). WIRING: main.cpp - E edge toggles (not in spectator), Esc edge closes screen first THEN quits (was level-triggered quit), setCursorCaptured lambda (captured = CURSOR_DISABLED + raw motion; released = NORMAL + cursor centered via glfwSetCursorPos w/ WINDOW coords; mouse for screen rescaled window->framebuffer for HiDPI), player.update gained acceptInput param (default true) - Player.cpp file-static g_acceptInput gates the keyDown() helper (ALL movement/mode/crouch keys read released; physics+anim continue) + skips handleLook/ModeSwitch/Perspective/DebugPose + sets firstMouse_=true each closed frame so look re-syncs on resume without a camera jump (handleDebugPose glfwGetKey -> keyDown); while open: no mining/placing/outline/whiff-swing, wheel -> catalog, 1-9 -> hover-assign. leftEdge/rightEdge precomputed at mouse read (leftPrev updates BEFORE the UI section, so edges computed late were always false - trap!). Hud::build gained showCrosshair param (crosshair hidden while screen open; hotbar stays, dimmed under the screen's 55% black overlay). UIRenderer kMaxTextures 32->256 (every block icon takes a descriptor slot; we were at ~30/32 - would have overflowed the pool on the panel texture or any new block). Vanilla behavior refs: title "Blocks" at (8,6) color 0x404040 no shadow, hover = 50% white over 16px item area, held icon rides cursor at -8,-8 GUI px.

**IN-USE MODELS STAGED BACK (2026-07-02):** user reorganized to `add-later/models/block/` (2659 files) and asked for the in-use blocks' models back in assets. Copied **81 files** to assets/models/block: per-block models for every registered block (grass_block, dirt, stone, cobblestone, sand, glass, oak_planks, oak_log(+horizontal), oak_leaves, {stone,cobblestone}_slab(+_top), {stone,cobblestone,oak}_stairs(+_inner,_outer), oak_fence_{post,side,inventory}, oak_fence_gate(+open,wall,wall_open), oak_door_{bottom,top}_{left,right}(+_open ×8), stone_pressure_plate(+_down), snow_height2-14 + snow_block, air) PLUS all parent templates (block, cube*, cube_mirrored, thin_block, slab*, stairs templates, fence_*, template_fence_gate*, door_*, pressure_plate_*) — parent-chain verified complete (script checked every "parent" ref resolves). These are still NOT read by the engine (future data-driven mesher); staged per user convention: assets = in use, add-later = dormant. Build syncs 81 files, still fast.

**MODELS MOVED TO add-later/ (2026-07-02, DONE):** user moved ALL 2659 Java block-model JSONs to `add-later/` (project root, FLAT — no subfolders). Engine reads NONE of them (block geometry = C++ classes); runtime-loaded JSONs = assets/animations/*.animation.json ONLY (+ textures/cameras/gui). Removed empty assets/models/block + purged the stale 2659-file copy from build/bin/Debug/assets (post-build copy never deletes!). assets/models/entity/player.json kept (unused reference). No-op build now 1.2s. Pull individual models from add-later/ into assets/ when the data-driven mesher needs them.

**FLY-ANIM FIX + ADD-LATER FOLDER (2026-07-02):** (1) updateAnimation: while flying (Spectator, or Creative && flying) limbAmount target = 0 and stride distance frozen — no run cycle mid-air (also stills the fp hand bob via moveAmount). Builds clean. (2) USER CONVENTION: unused reference assets (esp. the 2659 Java block-model JSONs) live in an **add-later folder at project root** (user moving assets/models/block there themselves; outside assets/ so the post-build copy skips them). When a feature needs a file (stairs/fence/bed json etc.), COPY it from add-later/ back into assets/. Check add-later/ before assuming a reference file is missing.

**FENCE OUTLINE COLLINEAR FIX (2026-07-02):** user still saw verticals mid-pole with 1 or 2-adjacent connections — the polygon kept the old post corners even when an arm made that side one flat plane. outlineEdges now filters collinear vertices (2D cross of prev→v, v→next ≈ 0 → drop) before emitting rings+verticals; e.g. N-only connection collapses to a single 4-corner rectangle footprint. Builds pending (game running at edit).

**FENCE SEAMLESS OUTLINE (2026-07-02, per user spec):** outline post 4x4x16 + arm 4x6x16 per connected side (arms now POST-WIDTH 6..10px and FULL HEIGHT, both in outlineBoxes for targeting AND collision arms widened 7..9→6..10). FenceBlock::outlineEdges = union silhouette: clockwise footprint polygon (post corners + arm detours to cell edge per connection) extruded — bottom ring + top ring + corner verticals; zero joint seams. Visual mesh rails unchanged (2px, y6-15). Builds pending (game was running).

### PLANNED (user asked to add; NOT yet built): DAY/NIGHT + WEATHER CYCLE (sun/moon billboards + rain remain; sky-color day/night DONE via GameTime).
User already dropped sun/rain/etc textures (check assets/textures/environment or /gui). Add as part of/after M6 beauty: (a) TIME CYCLE — world clock (ticks, ~20min day like MC), sun/moon direction drives a directional light + sky gradient color (day/dusk/night/dawn), sky clear color lerps, stars at night, sun/moon billboarded quads using their textures. (b) WEATHER CYCLE — state machine clear<->rain<->thunder with random durations; rain = camera-follow particle/quad layer (rain texture) + darkened sky + optional fog; snow in cold biomes later. Hook into the existing sky/clear-color + a new SkyRenderer pass and a simple particle system. Ties into M6 (fog/lighting/water). Store world time + weather in save later.

### OLD player-model notes (superseded by Stage A/B above): parse player.json bones→cubes (origin/size/uv/inflate/pivot/parent/mirror); build per-cube mesh with 64x64 skin UVs (box-unwrap); render 3rd-person + hands in 1st person; bone transforms for animation. SKIN SELECTION LOGIC (prepare now-ish): a PlayerSkin module — if user set a skin use it; else scan entity/player/{slim,wide}/ and pick a RANDOM name + body type (slim=alex geo, wide=steve geo) from what exists; body type determines which geometry (arm width). 
- **ANIMATION collab plan:** use a keyframe/pose system per bone (rotation about pivot). Options to offer user: (1) hardcode procedural anims in C++ (walk = leg/arm sin sway, idle, mine = arm swing) — simplest; (2) load Bedrock .animation.json (keyframes) — matches their asset pipeline. Guide: user provides animation.json OR describes desired motions; we map bone name→rotation over time. Start procedural walk/idle/arm-swing, then data-driven.

### M5a design (reference) — 2D UI (crosshair + hotbar bar + selection highlight)
- shaders/ui.vert + ui.frag (auto-globbed by CMake, no CMake shader change). Vertex: vec2 pos already in NDC (CPU converts pixels→NDC), vec2 uv, vec4 color. frag: outColor = texture(uTex, uv) * color. For solid quads bind a 1x1 white texture so white*color=color.
- src/gfx/UIRenderer.{h,cpp}: 2D pipeline drawn INSIDE the chunk's vkCmdBeginRendering pass (after chunk draws, before EndRendering), so its VkPipelineRenderingCreateInfo MUST declare colorFormat=sc->imageFormat AND depthAttachmentFormat=sc->depthFormat to match the pass, but depthTestEnable=FALSE depthWriteEnable=FALSE. Alpha blend ON (srcAlpha/oneMinusSrcAlpha). Dynamic viewport+scissor. Descriptor set0 binding0 = combined image sampler (white tex for M5a; icon atlas for M5b). Per-frame host-mapped vertex buffer (kFramesInFlight, ~6*4096 verts) refilled each frame. API: beginFrame(w,h) clears CPU verts+stores size; quad(x,y,w,h,vec4 color) appends 6 verts (pixel coords, top-left origin; ndc.x=x/W*2-1, ndc.y=y/H*2-1 — NO flip, Vulkan ndc y down = screen y down); recordDraw(cmd,frameIndex) memcpy CPU→gpu[frame].mapped, bind pipeline/desc/vbuf, vkCmdDraw. Need a 1x1 white VkImage+view+sampler (reuse TextureArray-style upload via ctx.immediateSubmit, or a tiny createImage helper).
- src/ui/Hud.{h,cpp}: build(UIRenderer&, w, h, int selectedSlot, int slotCount) → crosshair (2 thin white rects ~2px x 16px at center, alpha ~0.7), hotbar bar (dark rect ~ slotCount*slotPx wide centered bottom, slotPx~50, alpha ~0.5), per-slot cell outline, selected slot white highlight border.
- Renderer: drawFrame gains UIRenderer* param; recordCommandBuffer draws chunks then calls ui->recordDraw(cmd,currentFrame_) before vkCmdEndRendering. main: create UIRenderer (init after swapchain), build Hud each frame (uiRenderer.beginFrame(extent); hud.build(...)) then renderer.drawFrame(cam,world,&ui). Add gfx/UIRenderer.cpp + ui/Hud.cpp to CMake SOURCES. Cleanup ui before swapchain/ctx.

### M5b — 3D isometric block icons (headline feature)
- src/ui/ItemIconRenderer: for each block, render a unit cube ONCE into a small offscreen color+depth image (e.g. 64-96px) using ortho projection + GUI transform (rotate ~30deg X then ~45deg Y) so top + 2 side faces show, sampling the existing block TextureArray with per-face layer + per-face shading (top brightest). Cache: render all blocks into ONE atlas texture (grid), record name->uv. Then UIRenderer draws hotbar slot icons as textured quads sampling that atlas (switch UI descriptor to the icon atlas, or use 2 draw batches: white-tex quads + atlas quads). Re-render only when textures change (once at load). Reuses chunk.vert/frag-like shader but ortho + per-instance. iconType per block: CUBE (3D), FLAT (items/*.png 2D sprite), CROSS (plants).
- Hotbar then shows the real block in each slot; selected slot enlarged/highlighted. Plan: ui/UIRenderer (2D ortho textured-quad pipeline, alpha blend, screen-space) for crosshair + hotbar bar + slot highlight; ui/ItemIconRenderer (render each block as isometric cube ONCE to an offscreen color+depth image, cache as sampled texture, draw in hotbar slots) — reuses chunk TextureArray + a small ortho+30°X/45°Y transform; bitmap Font later for counts. Then M6 beauty, M7 block-shapes, save/load. Need: ui/UIRenderer (2D textured quads, ortho, alpha blend, separate pipeline), crosshair, hotbar bar + slot highlight, ui/ItemIconRenderer (render each block as isometric cube to offscreen image once, cache as texture, sample in hotbar), bitmap Font for counts. Then M6 save/load + beauty (lighting/fog/water, back-face + frustum culling, mipmaps).

**FRUSTUM CULLING + DISTANT-HORIZONS LOD (2026-07-05, RECONFIGURE — new source LodMesh.cpp + shaders lod.vert/frag; BUILT CLEAN, awaiting user visual verify).** User: options buttons already self-fixed; wanted (a) the culling terms clarified, (b) frustum culling (chunks/faces behind camera WERE all being drawn — no frustum test existed), (c) Distant-Horizons-style LOD (far colored downsampled terrain). Terms told user: hidden-face removal (mesher) + back-face culling (GPU) already existed; missing = frustum culling; faces-behind-mountains = occlusion culling (deferred, big system). (1) **Frustum culling**: Renderer::updateCameraUBO now also extracts 6 Gribb-Hartmann planes from proj*view into frustumPlanes_ (near = row2, 0..1 depth since GLM_FORCE_DEPTH_ZERO_TO_ONE); aabbInFrustum(mn,mx) positive-vertex test; chunk loop skips off-frustum chunks + counts renderedChunks_ (F3 "Chunks: L loaded, R rendered" now real, via renderer.lastRenderedChunks()). (2) **Avg block colors**: TextureArray::build computes per-layer alpha-weighted average in LINEAR space (sRGB->linear before mean) -> averageColor(layer/name); main builds lodTopColors/lodSideColors[BlockId] from faceLayers()[FACE_PY]/[FACE_PX], set on session->world.setLodColors each enterWorld. (3) **LOD**: WorldGen::surfaceBlock(height)->grass/sand; world/LodMesh.{h,cpp} LodVertex{pos,color} + buildLodTile(gen,top,side,cx,cz,step): samples heightAt every kLodStep=8 blocks over the 32-footprint into macro-cells = flat top quad + vertical SKIRTS down to lower orthogonal neighbors (sampled from global field so tiles are seamless; NO chunk allocation for far terrain). World manages LOD parallel to chunks (streamLod/collectAndUploadLod/unloadLod on the SAME ThreadPool, ring renderDistance+1..min(lodDistance,kMaxRing=32), budget 6/frame, deferred-destroy; lodTiles_ map keyed x,0,z). (4) **Render**: shaders/lod.{vert,frag} (world-space pos+baked color, DISTANCE FOG blending to skyColor_ from renderDistance*32 to lodDistance*32-16 -> fades LOD edge into sky); Renderer createLod() own pipeline (LodVertex input, cull NONE, depth on, own layout = camera set + FRAGMENT LodPush{camEye+fogStart, fogColor+fogEnd}); drawn after chunk loop, frustum-culled. (5) **Settings**: lodDistance (0=off..32, default 16) persisted + live via Session::applySettings(world.lodDistance); Video Settings "LOD Distance" slider (id 21); F3 "LOD tiles: N". Startup smoke test clean (no validation errors, LOD pipeline builds at init). KNOWN/DEFERRED: LOD fog is LOD-only so a faint haze step vs crisp real chunks at the boundary (chunk fog = future); occlusion culling (faces behind mountains) not done; LOD step is fixed (no per-distance coarsening yet); surfaceBlock only grass/sand (matches current worldgen).

**LOD v2: MULTI-LEVEL + CHUNK-SOURCED + NO FOG (2026-07-05, rebuild only; BUILT CLEAN, runs clean no validation errors, awaiting user visual verify).** User wanted the LOD to behave like the Distant Horizons mod: (a) remove fog ("i want to see a big world"), (b) multiple detail levels, (c) build LODs from real chunk data (edits show) not just re-generated noise. Answered their Q first: v1 LOD was SINGLE-level + re-sampled from WorldGen::heightAt only (NOT the session chunks — which are never in the LOD ring anyway; LOD is beyond render distance), so player edits didn't show. (1) **No fog**: lod.frag outputs vColor directly; removed LodPush struct + the FRAGMENT push-constant range (lodPipelineLayout_ now pushConstantRangeCount=0) + the per-frame push in recordCommandBuffer. (2) **Multi-level**: World free fn lodLevelFor(d,renderDistance,&step,&chunkSourced) — bands by Chebyshev dist: <=rd+3 step4 chunkSourced, <=rd+8 step8, else step16 (coarser=cheaper=further). LodTile gained `int step`; lodInFlight_ changed set->map<coord,int step>; CompletedLod gained step. streamLod enqueues when !inflight && (no tile || tile.step!=desiredStep) -> tiles REBUILD when they cross a band as player moves; collectAndUploadLod retireOld() defer-destroys the previous mesh on rebuild. (3) **Chunk-sourced**: WorldSave::hasChunk(cx,cy,cz) (fs::exists, worker-safe). LodMesh buildLodTile(gen, save*, top, side, cx, cz, step, chunkSourced) splits into buildFromWorldGen (cheap heightAt path = v1) and buildFromChunks (only when chunkSourced && save->hasChunk for any cy=0..3): generates the 4 vertical Chunk cubes (make_unique — Chunk has atomics, NOT vector<Chunk>-able) via gen.generate + save.loadChunk overlay, scans each macro-cell's representative column top-down for highest non-air -> real surface height+block+color (shows edits); tile-edge skirt neighbors fall back to heightAt. So only EDITED near columns pay the full-gen cost; everything else stays cheap worldgen. Shared emitCell() (flat top + skirts) used by both. Wiring: World::streamLod passes save_ + chunkSourced into the worker; buildLodTile threads it. NOTE worldgen currently has NO trees/caves/ores/biomes, so today chunk-sourced's only visible benefit over heightAt is PLAYER EDITS — it future-proofs for when worldgen grows features (flip near band to always chunkSourced then). KNOWN/DEFERRED: edits only show in the near LOD band (step4, rd+1..rd+3); far bands are worldgen; hard LOD cutoff at the far edge now that fog is gone (user chose this). Cache invalidation of edited tiles is automatic (inside renderDistance the LOD tile is unloaded, so re-entering the ring rebuilds from the updated save).

**RENDER/LOD DISTANCE 64 + WIDER LOD BANDS + ON-DEMAND RING (2026-07-05, BUILT CLEAN, runs clean).** User: wants render distance up to 64, a bigger/configurable LOD distance (LOD 32 let them "see the end of the world"), and MORE spacing between LOD levels. (1) Settings: renderDistance clamp 2..12->2..64, lodDistance 0..32->0..64; MenuScreen sliders remapped (RD `2+t*62`, LOD `t*64`). (2) World: removed fixed `kMaxRing=32`; added `rebuildRing(radius)` + `builtRingRadius_` — ringOffsets_ is now rebuilt to `max(renderDistance+1, min(lodDistance, kLodCap=64))` in init AND each update() when it changes, so a big distance only inflates the per-frame ring scan when actually set (normal RD5/LOD16 now scans a radius-16 ring = FASTER than the old fixed 32). streamLod/unloadLod use kLodCap. (3) lodLevelFor now takes lodDistance + has 4 bands (steps 4/8/16/32) with thresholds scaling to the LOD range (t0~17%, t1~40%, t2~70%) so levels spread far apart at large distances; step32 => n=1 (one flat cell/tile) for the coarsest far ring. Answered user Q: LODs (and chunks) BEHIND the camera ARE generated + kept resident (streaming is distance-based, only DRAWING is frustum-culled) — that's why turning feels heavy; frustum-GATED streaming would fix it but pops on turn = part of the deferred streaming-bottleneck work. WARNING GIVEN: RD 64 full chunks (~65k chunks) is very heavy/memory-hungry — intended use is low RD + high LOD. **STILL TODO (user asked, not yet done):** (a) cloud LOD (WeatherRenderer already meshes 3D clouds from clouds.png via buildCloudMesh — user wants clouds extended as a single coarse LOD level), (b) OCCLUSION CULLING (faces/chunks behind mountains) — approach TBD (GPU occlusion queries robust w/ ~1frame latency, vs cheap CPU heightmap risky in caves). NOTE occlusion culling targets DRAW/overdraw time, NOT the user's streaming/spawn bottleneck (different problem). (c) deferred: streaming bottleneck on chunk-destroy/LOD-spawn.

**GPU OCCLUSION CULLING + CLOUD LOD (2026-07-05, BUILT CLEAN, startup no-validation-errors; IN-WORLD behavior awaiting user visual verify).** User picked GPU occlusion queries (over cheap heightmap / frustum-gated streaming) + wanted clouds to LOD (1 level). (1) **OCCLUSION (CHC-lite, temporal, ~2-frame latency)**: Chunk.h += `bool occVisible=true` (render-thread only). Renderer: 2 `VkQueryPool occlusionPool_[kFramesInFlight]` (OCCLUSION type, kMaxOcclusionQueries=16384) + `occlusionPipeline_` (reuses outline.vert/frag + pipelineLayout_; depthTest LESS_EQUAL, depthWrite OFF, colorWriteMask 0, cull NONE — draws a chunk's bbox to count visible samples) + `querySlotCoord_[2]` (slot->ivec3 coord). createOcclusion() at init; cleanup destroys pool+pipeline. drawFrame: after the frame-slot fence wait, `applyOcclusionResults(world)` reads pool[cur] (results from 2 frames ago, ready no-stall) via vkGetQueryPoolResults 64BIT (no WAIT) and sets chunk->occVisible=(samples>0) by coord lookup (skips unloaded). recordCommandBuffer: `vkCmdResetQueryPool` + clear querySlotCoord BEFORE vkCmdBeginRendering (resets illegal inside dynamic rendering). Chunk block REWRITTEN: frustum-cull into a vis vector, sort FRONT-TO-BACK by dist to camEye_; **Pass A** binds chunk pipeline, for each occVisible (or out-of-slots=conservative-draw) chunk wraps its real vkCmdDrawIndexed in vkCmdBeginQuery/EndQuery; **Pass B** binds occlusionPipeline_+outlineVbuf_, for each hidden chunk draws its bbox wrapped in a query (so it's re-detected when it reappears). renderedChunks_ counts Pass A. Out-of-slot chunks drawn without query (safe/conservative). Occlusion is per-3D-chunk (cx,cy,cz). Known artifact: fast rotation reveals previously-occluded chunks ~2 frames late (brief holes); front chunks never wrongly hidden (they have samples); new chunks default visible. Title/world=null: reset still runs, no queries, applyOcclusionResults early-returns. (2) **CLOUD LOD**: WeatherRenderer cloudCounts_[4]->[6], kMaxCloudVerts grew; buildCloudMesh adds passes 4(far top)/5(far bottom): coarse cells kCloudLodStep=4 texels (48 blocks) out to kCloudLodRadius=48 cells (~2304 blocks), coverage = majority of sub-texels cloudy, SKIPS the detailed near square (xOut||zOut test) so no overlap/z-fight, top+bottom flat quads only (no sides). record() draws 6 passes with faceLight[6] {1,.7,.9,.8,1,.7}. Cheap single-level far cloud horizon. **STILL DEFERRED (user, later): the chunk-destroy/LOD-spawn STREAMING bottleneck** (occlusion culling was draw-time, doesn't touch it) — the fix lever is frustum-gated streaming (only build/keep what's near the view cone) + maybe far-LOD tile merging to cut draw calls at big lodDistance.

**GEN PRIORITY + CLOUD EXTEND (no LOD) + OCCLUSION THRESHOLD (2026-07-05, BUILT CLEAN, startup no-validation; in-world awaiting user verify).** Three user asks: (1) **Nearest-first gen priority (MC-style "flying fast prioritizes near chunks")**: World got `std::atomic<int> genInFlight_/meshInFlight_` + budgets genBudget_/meshBudget_ (=threadCount*4) / lodBudget_ (=threadCount*2), computed in init() after pool_.start() (also resets counters — stop() drops queued jobs leaking their pre-increment). streamChunks/scheduleMeshing/streamLod now `return` when their in-flight count hits budget; since ringOffsets_ is nearest-first, the bounded pool queue always holds the chunks CLOSEST to the player (fast flight always has near ground). gen task captures `this` + fetch_add before enqueue / fetch_sub at end; mesh task fetch_sub after pushing completed_ (per-chunk `meshInFlight` bool is separate from the global `meshInFlight_` counter). LOD gated by lodInFlight_.size() (that map already tracks in-flight LOD). Throughput unchanged (workers still saturated); only the QUEUE is bounded, so no more huge backlog of now-distant work. (2) **Clouds: REMOVED the coarse LOD ring** (user: "clouds LOD are shit, they're cheap, don't deserve LOD") — reverted cloudCounts_ to [4], deleted far passes. Instead the DETAILED cloud radius is now dynamic: record() computes desiredR = clamp(ceil(viewChunks*32/kCloudCell), kCloudRadius=30, kCloudMaxRadius=96) where viewChunks = lodDistance>0?lodDistance:renderDistance (reads world.lodDistance/renderDistance — both public), rebuilds cloud mesh when radius or anchor changes; cloudRadius_ member drives the buildCloudMesh loops. kMaxCloudVerts sized for kCloudMaxRadius at 20 verts/cell + an emission guard `if size+36>max break`. So clouds cover out to ~1152 blocks (cap) matching the view; buffer ~30MB. (3) **Occlusion camera threshold**: Renderer camFront_ (=camera.front()) + lastQueryEye_/lastQueryFront_. In the chunk block, `doQueries` = camera moved > kOccMoveThresh(1.5 blocks) OR turned > ~2.6deg (dot(front,lastFront)<0.9990) since last query; when false, Pass A draws the currently-visible chunks WITHOUT wrapping queries and Pass B is skipped entirely (`if(!doQueries) break`) — visibility frozen, saving the per-frame query+bbox GPU cost on tiny nudges/idle. Fixed the hidden-skip condition so frozen frames still DON'T draw hidden chunks: `if(!occVisible && (!doQueries || slots.size()<max)) continue`. **STILL DEFERRED (user, later): the chunk-destroy/LOD-spawn STREAMING bottleneck** — the gen budget helps priority but the deeper fix is frustum-gated streaming (only build/keep near the view cone; user confirmed off-screen chunks/LODs ARE currently generated+resident) + far-LOD tile merging to cut draw calls at big lodDistance.

**LOD YIELDS TO CHUNKS (priority pool) + HIGH-ALTITUDE RAIN FIX (2026-07-05, BUILT CLEAN).** (1) User: "the actual good looking chunks are competing with the LODs generation" — real chunk gen was sharing the worker pool equally with LOD gen. Fix: ThreadPool now has TWO queues (tasks_ normal + lowTasks_) and `enqueue(task, bool lowPriority=false)`; workerLoop always drains normal-priority (chunk gen/mesh) before touching low (LOD): `queue& q = tasks_.empty()? lowTasks_ : tasks_`. streamLod enqueues with lowPriority=true. So detailed chunks NEVER wait behind LOD — LOD only soaks leftover worker time. stop() clears both queues. (2) User screenshot at Y≈508 (flying high) while raining showed a hard diagonal "cut" band of rain floating in the sky + the LOD terrain/clouds below. Diagnosed: it's the RAIN (I only ever touched cloud code, not rain). WeatherRenderer rain draws a band around the CAMERA for columns in [bottom=cy-20, top=cy+15]; when no ground is found in that window (flying high above the 128-tall world) it still drew a floating 35-tall rain band with a hard radius edge. Fix: track `found` in the surface scan; `if(!found) continue` — no ground near camera => no rain (removes the floating sky band; rain still normal when near the ground). NOTE the streaks-everywhere was just rain being on; gray blocky ground = LOD, white blobs = clouds seen from above. **ALSO: user reported jump no longer feels like 1.25 blocks — CONFIRMED I did NOT touch Player.cpp (kJumpSpeed=8.8/kGravity=32/kEyeHeight=1.62/kHalfWidth=0.3/kStepHeight=0.55 all unchanged); jump uses semi-implicit Euler with per-frame dt so height is FRAMERATE-DEPENDENT (8.8 was calibrated ~60fps). The new rendering load (occlusion queries, LOD, RD/LOD up to 64, bigger clouds) can drop FPS (user's F3 showed 40fps) -> bigger dt -> jump overshoots. Offered fixed-timestep jump (accumulator) as the proper fix — AWAITING user go-ahead.** Deferred streaming/frustum-gated-streaming + far-LOD tile merging still open.

**FAR-PLANE "SLICE" FIX + FIXED-TIMESTEP PHYSICS (2026-07-06, BUILT CLEAN).** (1) User screenshot: terrain AND clouds sliced along a curved horizontal line — NOT chunk limits. Root cause: Camera::projection far plane HARDCODED 1000.0f while LOD reaches lodDistance*32 (up to 2048+); the far-plane SPHERE clipped everything past 1000 blocks (also what the "fake horizon curve" actually was). Fix: Camera gained `float farPlane=1000.0f` member used in projection(); Session::applySettings sets `player.camera.farPlane = max(1000, (max(renderDistance,lodDistance)+3)*32)` so it always reaches past the farthest LOD/clouds. (Depth precision: near 0.1 / far up to ~2100 — LOD is coarse so z-fight risk low; revisit with reverse-Z if far LOD z-fights.) (2) Fixed-timestep player physics (fixes framerate-dependent jump): Player.h += `float physicsAccum_`. Player::update now runs movement (moveWalk/moveFly) in a FIXED-STEP accumulator loop at `kPhysicsStep = 1/60` (== the framerate the feel was tuned at, so kJumpSpeed=8.8's ~1.25-block jump is preserved EXACTLY and is now identical at any FPS); kMaxCatchup=0.25 prevents spiral-of-death. Input handlers (look/mode/perspective + handleFlyToggle MOVED here) run once per frame; applyCameraEffects/updateBodyYaw/updateAnimation/updateThirdPersonCamera stay per-frame. Held keys re-sampled each substep; jump trigger is `onGround && key` (not edge) so substepping can't double-jump. **BROADER FIXED-TIMESTEP (answered user): Minecraft runs ALL gameplay on a 20 TPS fixed tick + interpolated rendering — so the same pattern should cover: all player+entity/mob physics (gravity/fall/knockback/walk), Drops::update (item bob/gravity/friction/despawn), Mining::dig break time, GameTime day/weather, and future block ticks (crop growth, redstone, fluid flow, furnace smelting, random ticks, mob spawning). Rendering, camera smoothing, cosmetic anims stay per-frame. Recommended future: promote the per-frame dt systems in Session (drops/mining/gameTime) onto the same fixed tick for determinism.** Deferred streaming/frustum-gating + far-LOD tile merging still open.

**FIXED 20 TPS TICK BACKBONE + gametime + SEEDED/PERSISTENT WEATHER + GAMETIME-DRIVEN CLOUDS (2026-07-06, BUILT CLEAN, Phase 1 of the MC tick refactor).** User: "make everything as minecraft" (clouds/weather/player/mobs/gametime/drops/mining/future ticks). Explained MC has TWO clocks: daytime (wraps 24000, sun) vs gametime (monotonic total ticks, saved, drives clouds/redstone); day=24000 ticks=20min at 20 TPS. **Phase 1 built (backbone + the user-observable MC gaps):** (1) Session got a fixed 20 TPS accumulator: `tickAccum_` + `uint64_t gametime_` (monotonic, SAVED) + `kTickDuration=1/20`; update() now runs `while(tickAccum_>=kTickDuration) tickWorld()` (clamp 0.25 anti-spiral); tickWorld() = ++gametime_ + gameTime.advance(tick) + weather.advance(tick). partialTick = tickAccum_/kTickDuration. (2) **Weather.h reworked**: RNG now seed()'d from the WORLD SEED (was std::random_device) -> same seed = same weather sequence; setState/timer() accessors so it's SAVED/restored (raining+timer+intensity). Session::start seeds weather from level_.seed; applyLevel restores state + gametime_; captureLevel writes them. (3) **WorldSave::Level** += gametime/raining/rainTimer/rainIntensity; level.txt lines "gametime:%llu" + "weather:%d,%.2f,%.3f". (4) **Clouds now drift on GAME time**: renderer.setWeather time = (gametime_+partialTick)/20 seconds (was wall-clock in.now) -> clouds are seed-consistent, persist across reload, pause with the game, smooth via partialTick. (in.now now unused for weather.) Day/night speed unchanged (20*0.05=1s/s). **PHASE 2 STILL TODO (told user, tick loop is ready for them):** player physics ON the 20 TPS tick with render INTERPOLATION (prev/cur pos lerp by partialTick) — currently player is on its own 1/60 fixed substep (already framerate-independent, jump=1.25 preserved); moving to true 20 TPS needs interpolation + jump retune (MC jump=0.42 blk/tick, gravity 0.08 blk/tick²=32 blk/s² which ALREADY matches our kGravity). Also Phase 2: drops + mining-dig on the tick (interpolated), mob AI/physics, block/random ticks (crop growth/redstone/fluids/furnace). NOTE far-plane fix + fixed-timestep(1/60) player + rain-high-altitude fix were the prior batch. Deferred streaming/frustum-gating + far-LOD tile merging still open.

**TICK PHASE 2: PLAYER PHYSICS ON 20 TPS + RENDER INTERPOLATION (2026-07-06, BUILT CLEAN, awaiting in-world verify).** User chose "Full 20 TPS + interpolation (like Minecraft)" for the player (after I clarified interpolation prevents the "little teleports" worry). (1) **Camera**: added `renderPosition` (interpolated eye) alongside `position` (authoritative physics/streaming/save); `renderEye()` now returns renderPosition-crouchOffset. (2) **Player**: removed the 1/60 physicsAccum_; added `prevPos_`. `update()` SPLIT into `handleInput(window,acceptInput)` (per-frame: look/mode/perspective/flyToggle/sneak), `tick(window,world)` (ONE fixed 1/20 physics step; prevPos_=camera.position then moveWalk/moveFly at h=0.05), `updateVisual(window,world,dt,partialTick)` (per-frame: camera.renderPosition=mix(prevPos_,camera.position,partial) THEN applyCameraEffects/updateBodyYaw/updateAnimation/updateThirdPersonCamera). `resetInterp()` snaps prevPos_+renderPosition to position (called on spawn/load + ground-spawn scan to avoid a render-eye slide across the map). `feetPosition()` now uses renderPosition (smooth model + drops magnet). (3) **Jump retuned for 20 TPS**: kJumpSpeed 8.8->9.8 (~1.25 block at h=0.05 semi-implicit Euler; note kGravity 32 == MC's 0.08 blk/tick²). (4) **Session**: tickWorld(window) now also calls player.tick(); update() restructured — handleInput at top, then the 20 TPS accumulator loop (tickWorld) MOVED UP before raycast/interact so targeting uses this frame's motion, then player.updateVisual(partialTick); the old lower tick block removed (renderer sky/cloud feeds kept, use gametime+partialTick). Physics runs every tick even with inventory open (player keeps falling; movement keys gated by g_acceptInput) — MC-like. Authoritative camera.position still used by world.update streaming + save + F3 + collision; only rendering/targeting use the interpolated renderEye. **VERIFY IN-WORLD:** movement smooth at 40fps (no teleport), jump ~1.25 (nudge kJumpSpeed if off), break/place targeting accurate, 3rd-person model smooth, no camera slide on spawn. Known-accepted: quantized jump + fast-creative-fly can clip thin walls (both MC-authentic). **PHASE 3 TODO:** drops + mining onto the tick (drops need interp or accept MC-steppy), then MOBS + block/random ticks (crop growth/redstone/fluids/furnace) hang off tickWorld().

**TICK PHASE 3: GROUND DROPS ON 20 TPS + INTERPOLATION (2026-07-06, BUILT CLEAN).** Drops (item entities) moved onto the world tick like Minecraft. Drops::Item += `prevPos`; Drops::update renamed `tick(world, playerFeet, inv, collect)` (no dt param — uses internal `kTick=1/20`), sets `it.prevPos=it.pos` at the start of each item's step; spawnFromBlock/spawnThrown/setItems/merge all set prevPos=pos (merge sets prevPos to the new centroid so it doesn't interp-slide). buildDraws(out, partialTick) now renders at `mix(prevPos,pos,partial)` and a `renderAge = age - kTick*(1-partial)` so the bob/spin clock is smooth too. Session::tickWorld calls drops.tick right after player.tick using the AUTHORITATIVE feet (camera.position-1.62, not the interpolated feetPosition) for the magnet; per-frame drops.buildDraws(dropDraws, partialTick). **TICK MIGRATION OF EXISTING SYSTEMS NOW COMPLETE**: player physics, drops, weather, gametime, day/night all on 20 TPS; clouds driven by gametime; **mining left as-is on purpose — its break progress accumulates by real dt = elapsed real time, so it's ALREADY framerate-independent + deterministic + pauses with the game** (moving it to the tick needs an interact/input refactor for negligible gain; MC's only extra nicety is tick-quantized break which is imperceptible). REMAINING "everything as MC" items are NEW FEATURES that don't exist yet and will hang off tickWorld() when built: MOBS (AI/physics/spawning), BLOCK+RANDOM TICKS (crop/tree growth, redstone, fluid flow, furnace smelting, leaf decay). So the tick backbone + entity-interpolation pattern is fully in place; future gameplay systems just add a call in tickWorld() + (for rendered entities) a prevPos/interp like drops/player.

**PERF PROFILING + RELEASE BUILD + LOD YIELDS-TO-CHUNKS PRIORITY (2026-07-06).** User: 10 fps on a flat world w/ LODs, wants me to analyze where the bottleneck is (CPU/GPU/RAM) from ~4min of play; ALSO observed LODs render before HD chunks + wants HD chunks prioritized (LODs in background, not stopped). (1) **HUGE LIKELY CAUSE FLAGGED: all my builds this whole project were `--config Debug`** (build/bin/Debug) — MSVC Debug is 5-20x slower (no opt + iterator checks). Built + verified **RELEASE** (build/bin/Release/MineCraftCPP.exe, assets+shaders copied, runs clean on 780M). User should run the RELEASE exe. (2) **Profiler**: Renderer times the vkWaitForFences (gpuWaitMs_, high=GPU-bound) + recordCommandBuffer (recordMs_, high=CPU-draw-bound/too many draw calls) via <chrono>, getters gpuWaitMs()/recordMs(). main loop times session->update (sim ms) + accumulates, appends an averaged line to **perf.log** (via mc::resolve -> exeDir, i.e. build/bin/Release/perf.log) every ~2s: `fps | sim gpuWait record ms | chunks L/R rendered | lod N | RAM/VRAM MiB`. perf.log removed at startup. READ build/bin/Release/perf.log after the user plays to diagnose (gpuWait high=GPU/fillrate-bound likely the 4752 LOD tiles' draw calls -> needs far-LOD tile MERGING; sim high=streaming CPU; record high=draw-call count). (3) **LOD priority**: streamLod now early-returns when `genInFlight_>genBudget_/2 || meshInFlight_>meshBudget_/2` — while the chunk pipeline is busy, no new LOD work is queued so the playable HD area loads first; LODs fill in behind once chunk load eases (plus the existing low-priority worker queue drains chunk jobs before LOD jobs). Suspected FPS culprits (confirm via perf.log): Debug build (#1), LOD draw-call count (4752 tiles), per-frame ring iteration. DEFERRED still: far-LOD tile merging (cut draw calls at big lodDistance) + frustum-gated streaming.

**PERF DIAGNOSIS FROM perf.log + UNLOAD CAPS (2026-07-06).** User played Release (much smoother than Debug — confirms Debug was the 10fps killer), cycled RD 5->16->32->64->16, flew 2000 blocks. **DIAGNOSIS from build/bin/Release/perf.log: the game is CPU `sim`-bound (world streaming), NEVER GPU-bound — gpuWait=0.0ms the ENTIRE log.** Steady-state is great: settled at RD16/32/EVEN 64 = ~55-60fps (sim->0, record ~4-11ms). The FPS drops happen ONLY while LOADING/moving: sim (gen+mesh+upload+unload in world.update) spikes 12-70ms. The "big crashes" on RD change = mass load/unload: RD16->64 = 70ms sim spike; RD64->16 = **272ms** (erasing ~60000 chunks at once). **RAM is the hard wall, quadratic in RD**: RD16=~1GB (4900 chunks), RD32=~3.5GB (19564), **RD64=9-11GB (68644 chunks × 128KB block-vector each)** — RD64 full-detail is simply not viable, that's what LOD is for. record (draw) at RD64 settled ~11ms = building+sorting the vis vector over ~13000 ready chunks each frame. **KEY TAKEAWAY: the deferred far-LOD tile MERGING is NOT the priority — GPU isn't the bottleneck; CPU streaming + RAM are.** FIX APPLIED: capped per-frame unloads — unloadDistant kUnloadBudget=96 chunks/frame + early-break the scan once budget met; unloadLod kUnloadBudget=128/frame. Spreads RD-change mass-unload over frames (kills the 70-272ms hitch). Rebuilt Release. **STILL-OPEN streaming fixes (ranked): (1) immediateSubmit per mesh-upload batch STALLS CPU on GPU inside sim during loading — move to async transfer queue / non-blocking upload; (2) per-frame O(loaded chunks) work (unloadDistant scan, ring iteration streamChunks/scheduleMeshing) — gate on player-chunk-crossing + maintain a ready-to-mesh list instead of scanning the ring; (3) cap loaded RAM / lower practical RD max; (4) record vis-vector build+sort each frame is O(ready) — cache/skip-sort. Recommend user keep RD<=16-24 + high LOD distance (RD16 was smooth @ ~1GB).**

**LOD-OVER-HD OVERLAP FIX (2026-07-06).** User cycled RD 16->64->16 and saw peach flat LOD tiles interleaved with gray HD chunks (opening level.txt was unrelated — no edit made). CAUSE: the per-frame unloadDistant cap I'd just added (96/frame) meant dropping RD 64->16 unloads ~64000 chunks over ~11s; during that window the lingering far HD chunks (still at d 17..64) and the newly-created LOD tiles (also 17..64 for RD16) occupied the SAME columns and both drew -> interleave. FIX: (1) **Renderer LOD draw now skips any tile whose column has a chunk drawn this frame** — builds `hdColumns` (unordered_set<int64_t> packed cx,cz) from the frustum `vis` list (cheap, ~drawn chunks) and `if (hdColumns.count(columnKey(coord.x,coord.z))) continue;` before drawing each LOD tile. So LOD can NEVER render where a real chunk renders, regardless of unload timing / boundary transitions. (2) unloadDistant kUnloadBudget 96->256 (faster RD-down transition, ~256*4.5us=~1ms/frame, still no 272ms hitch). VisChunk/vis are in the same scope as the LOD block so reused. Rebuilt Release clean. This robustly fixes LOD/HD overlap AND any boundary z-fight. NOTE the deeper perf items (async mesh upload, O(N) per-frame streaming work, RD64 RAM) are still open — user should keep RD~16 + high LOD.

**LOD KEEP-ALIVE UNDER HD + STRICT HD PRIORITY (2026-07-06, Release BUILT CLEAN).** Two follow-up bugs from the RD 16->64->16 cycling: (a) user saw destroyed LOD tiles NOT replaced by HD chunks (dark void holes) — cause: unloadLod was destroying tiles the HD ring covered (d < renderDistance+1) DURING the transition, but HD wasn't meshed yet; when user waited, chunks appeared "delayed" = they were regenerating on low priority. FIX: **unloadLod no longer destroys tiles covered by the HD ring — only tiles past the LOD outer range unload**; covered tiles stay resident and the RENDER gate (hdColumns skip, from prior fix) hides them under drawn chunks. So RD-down transitions reveal LOD instantly (no void, no regen wait); streamLod still won't CREATE tiles inside the chunk ring. (b) user: "HD chunks aren't having the right priority over LOD" — the streamLod gate was lenient (only backed off when gen/mesh > budget/2). FIX: **strict gate — streamLod returns while genInFlight_>0 || meshInFlight_>0**: zero new LOD work while ANY chunk gen/mesh is in flight; all workers go to HD first, LOD fills only when the chunk pipeline is fully idle. Safe because kept-alive tiles mean the horizon is already populated during the wait. Memory cost of keep-alive: LOD tiles under the HD ring stay resident (~KBs each, minor vs chunks). Both verified building + running clean (earlier LNK1104 was just the game locking the exe).

**STREAMING PIPELINE REVISION (2026-07-06, Release BUILT CLEAN) — root-caused the "neither HD nor LOD" holes + the stand-still LOD delay.** User (RD 64->16): scattered columns with NO terrain at all + lots of LOD near player + HD only replacing LOD after standing still. THREE ROOT BUGS FOUND+FIXED: (1) **unloadDistant erased individual CUBES in unordered_map hash order** — a column could lose its SURFACE cube while an underground cube stayed READY+drawn; the renderer's hdColumns gate (built from drawn chunks) then suppressed the LOD tile for a column with no visible surface = hole. FIX: **column-atomic unload** — collect out-of-range columns (dedup by packed x,z key, kScanCap=256/frame), sort FARTHEST-FIRST, erase all kVerticalChunks cubes of up to kColumnBudget=64 columns/frame (edited cubes still saved first). Partial columns from unloading are now impossible + the HD ring recedes coherently. (2) **LOD suppression gate now requires the column to be FULLY loaded**: vis-build loop in Renderer also counts READY cubes per column (`colReady` unordered_map<int64,int>, counts empty all-air cubes too — they get CHUNK_READY w/ indexCount 0); LOD tile hidden only when count >= World::kVerticalChunks (made PUBLIC static constexpr on World; removed the World.cpp anon-namespace copy). Same hole existed on load-UP (one meshed underground cube used to hide the LOD). Brief LOD+partial-HD overlap during a column's load is accepted (beats holes). (3) **Removed the strict streamLod gate (genInFlight>0||meshInFlight>0 -> return)** — it meant LOD only EVER generated when the chunk pipeline was fully idle (i.e. standing still). The ThreadPool's two-queue priority ALREADY guarantees workers drain all chunk jobs before any LOD job, so queuing LOD never slows HD. Now LOD trickles in on leftover worker time even while flying. (4) Perf: ring scans early-BREAK using sorted-euclidean property (`x²+z² > 2R² -> break`) in streamChunks/scheduleMeshing (R=genR/meshR) + streamLod (R=outer) — ring can be radius 64 (LOD) while RD=16, was scanning 16k offsets with 4 map lookups each per frame. PIPELINE NOW: update() = rebuildRing -> streamChunks(gen, nearest-first, budget t*4) -> scheduleMeshing(needs neighbors GENERATED, budget t*4) -> collectAndUpload(8/frame, blocking immediateSubmit — still the open async-upload item) -> unloadDistant(column-atomic, farthest-first, 64 col/frame) -> streamLod(low-prio queue, lodBudget t*2, keep-alive under HD) -> collectAndUploadLod(6/frame) -> unloadLod(only past lodDistance+2) -> deferred destroys. Renderer: frustum -> colReady full-column gate -> occlusion queries.

**NEAR-CHUNK PRIORITY RESTORED + SKY-CUBE SKIPPING (2026-07-06, Release BUILT CLEAN).** User: (a) nearest-first gen priority "isn't working anymore" (flying over invisible chunks again), (b) "cubic chunks means we don't need to spawn whole columns — only spawn what the player can see". (a) ROOT CAUSE: removing the strict streamLod gate let LOD jobs occupy ALL workers between chunk bursts (in-progress LOD tasks can't be preempted; lodBudget was t*2 ≈ 2x worker count). FIX: streamLod dynamic cap — while genInFlight_+meshInFlight_ > 0 at most **2** LOD jobs in flight (chunks get ~whole pool while flying), when chunk pipeline idle cap = lodBudget_ (full parallelism to fill horizon). (b) Implemented the sound half of "only visible" (full frustum-gated streaming still deferred — pop-in): **sky cubes are never created**. WorldGen: public `kMaxTerrainY = 92`, heightAt clamps to it (was clamp ..126; practical terrain max ~91 unchanged) → any cube with baseY >= 92 (i.e. cy=3, y96..127 = 25% of all cubes ≈ 2GB at RD64) is guaranteed pure air. streamChunks skips creating them; getState already returns air for missing cubes; Mesher already treats null neighbor as air. scheduleMeshing: neighbor readiness via `settled()` = atLeastGenerated || (missing && sky && !saved) — applied to ALL 6 neighbors (horizontal too: on-demand sky cubes need meshable neighbors). **Saved sky cubes still load**: WorldSave::listChunks() (scans chunks/ dir filenames c_x_y_z.bin once); World::savedChunks_ (unordered_set<ChunkCoord>) populated in setSave (now out-of-line; cleared in init for title worlds), inserted on every saveChunk (unloadDistant + flushEdited) — streamChunks creates sky cubes present in the set. **Building high**: setState materializes a missing sky cube on demand (all-air Chunk, state=READY, empty mesh, then normal dirty->remesh; edited flag -> saved -> in set -> reloads next visit). **Renderer column gate updated for 3-cube columns**: colReady -> colState (ivec2 created/READY per column, counted over ALL chunks incl. non-READY); column complete ⇔ created >= kVerticalChunks-1 && ready == created (was ready >= 4 — would NEVER be true with sky cubes skipped => LOD would never hide; caught before it shipped). F3 "chunks loaded" now ~25% lower. All built + running clean.

**OOM CRASH DIAGNOSED + RAM GUARD (2026-07-06, Release BUILT CLEAN).** User: RD64, flew to fresh chunks, game froze then crashed seconds later. perf.log tail: RAM climbed 6.8GB -> 10.8GB in ~24s and still rising at the cut (54k cubes loaded, mesh backlog 7k/54k) = classic OOM: system pages (freeze) -> allocation fails in a worker (bad_alloc uncaught in pool thread -> terminate = crash). 780M iGPU shares system RAM making it worse. FIX: **memory-pressure guard**: core/SystemStats += `systemMemoryLoadPercent()` (GlobalMemoryStatusEx dwMemoryLoad, system-wide % used). World: `memoryPressure_` checked every 30 frames in update(); >= 85% system load -> (1) streamChunks returns (world stops growing), (2) streamLod returns (no new tiles), (3) unloadDistant budgets escalate (64->256 columns/frame, scan cap 256->1024); logs "[World] system RAM x% full: pausing generation" / "pressure over: resumed". Graceful degradation instead of crash — at RD64 the world just stops expanding when RAM runs out. NOTE: real fix for RD64 remains "don't do RD64" (RAM is quadratic); guard makes it survivable. Deferred perf items unchanged (async upload etc.).

**GENERATOR-OWNED SKY CEILING + BLOCK-PERFECT NEAR LOD (2026-07-06, Release BUILT CLEAN).** User pushback (correct): (a) hardcoding "y>=96 is air" (kMaxTerrainY=92) bakes today's heightmap into world logic — future biomes will add big mountains/floating islands; (b) nearest LOD band looked "weird" (step-4 terraces) — wants it shaped like HD blocks, just flat "cooked" colors. FIXES: (a) **kMaxTerrainY DELETED**; heightAt clamp restored to kWorldHeight-2 (=126). New `WorldGen::surfaceCeiling(cx,cz)` = max of a 9x9 (step-4) heightAt sampling over the cube footprint + kSkyMargin=8 — conservative per-column ceiling derived from the ACTUAL noise; documented as THE one place future biomes/islands must update (e.g. islands => return "no ceiling" in their y-range). World: `columnCeiling(cx,cz)` caches per column (`columnCeiling_` map, cleared in init; main-thread only); the 3 call sites (streamChunks skip, scheduleMeshing settled(), setState on-demand materialize) now use it. Sky-cube skip still saves ~25% (flat terrain ceiling ~96); tall-hill columns near max height keep their cy=3 cube (correctness first). (b) **lodLevelFor band0: step 4 -> step 1** — block-resolution LOD: exact terrain silhouette (per-block tops + 1-block skirts; buildFromWorldGen/FromChunks already handle step=1: n=32, cell centers become exact blocks), flat cooked colors, chunkSourced (edits show). Band0 is a FIXED 4-chunk ring (t0 = rd+4, not %-scaled) to bound its ~10x vertex cost (~5k verts/tile); t1/t2 = max(6, 40%) / max(8, 70%); ladder now 1/8/16/32. Handoff HD->LOD is now silhouette-seamless, only texture->color changes.

**TIGHT MESH BOUNDS + OUTLINE DEPTH FIX + PLACEMENT SUPPORT RULES (2026-07-10, Release BUILT CLEAN, smoke-tested).** (1) User: occlusion bbox should hug the mesh. ChunkMeshData/ChunkMesh now carry `boundsMin/boundsMax` (tight chunk-local vertex bounds computed at the end of buildCubeMesh, copied through uploadMesh + World::collectAndUpload's Upload struct). Renderer uses them for the frustum test + distance sort AND the Pass B occlusion proxy box (padded kOccPad=0.05 for depth precision; occlusion pipeline is LESS_OR_EQUAL so coplanar = visible = safe direction). Better culling of thin terrain slices; no visual change expected. (2) User: clouds drew in front of the selection outline. Cause: outline pipeline depth-tested but depthWrite OFF and weather records later. FIX: outline pipeline (the one in createOutline, NOT crack/occlusion) now depthWriteEnable=TRUE (bars are opaque a=1.0). (3) User feature: **data-driven placement support config**. Block.h: `enum class BlockSide {Bottom,Top,North,South,West,East}` (BLOCK-LOCAL: North = block's front, rotates with facing) + `struct SupportRule{side, blocks[], tags[]}` + `Properties::support` + builders `needsSupport(side)` (any sturdy FULL face: some collision box spans the whole shared face — top-slab tops yes, fence posts no) / `needsSupport(side, blocks, tags)` (neighbor name/tag match, shape irrelevant). Rules OR'd; empty = can float. New virtuals: `facingOf(BlockState)` (overridden by Stair/FenceGate/Door — all share 0=-Z,1=+X,2=+Z,3=-X), `canSurviveAt` (support-only check; canPlaceAt defaults to it; split so placement-only conditions like door headroom don't evict placed blocks). Block.cpp anon helpers sideDir (facing-rotated) + sturdyFace. **Pop-on-support-loss like MC**: World::setState now sweeps the 6 neighbors after every edit and `popBlock()`s any that fail canSurviveAt (recursive chains OK); World::popBlock = setState(air) + onBroken + `onBlockPopped` callback (std::function member; Session::start points it at drops.spawnFromBlock; null in title world). Door item accounting (avoids dupes): DoorBlock::drops(upper)={} (item lives on lower half); onBroken(upper) -> world.popBlock(lower) (drops the door); onBroken(lower) -> silent remove upper; DoorBlock::canSurviveAt(upper) = below is same door id. Wired: snow_layer, both pressure plates, all 7 doors get needsSupport(Bottom). Known tiny MC deviation: creative-mining a door's UPPER half spawns 1 door item (pop drops aren't gamemode-gated). Future blocks (torches/crops/ladders) = one needsSupport line, e.g. crops: needsSupport(Bottom, {"farmland"}); wall-torch: needsSupport(South) (back).

**SUPPORT-SHAPE + FLIGHT BRAKE TUNING (2026-07-10, Release BUILT CLEAN).** User feedback round: (a) "snow can stack on itself" — clarified same-cell 1..8 layering is vanilla (tryMergeInto/tryCombineAt, bypasses canPlaceAt correctly); REAL bug was full 8-layer stacks couldn't support anything (collision tops at 7/8). Added `Block::supportBoxes(state,world,cell)` virtual (defaults to collisionBoxes; sturdyFace now uses it); LayerBlock overrides: layers>=8 -> full cube (MC's snow support shape), partial -> collision (supports nothing). (b) Flight felt draggy on key release: kFlyGlide 4.2 -> 12.0, NEW kFlyGlideY = 18.0 (vertical brakes hardest, MC-like); moveFly now blends per-axis (blendXZ/blendY); kFlyAccel unchanged (12). Release skid now ~0.8 blk at base fly speed, altitude pins near-instantly on jump/sneak release. AWAITING USER FEEL-TEST of both numbers.

**FLIGHT BRAKE ROUND 2 + AIMED DOUBLE-SLAB OUTLINE (2026-07-10, Release BUILT CLEAN).** User: 12 was too grabby -> kFlyGlide 8.0 (=~1.2 blk slide base / ~3.5 boost), kFlyGlideY 12.0. Also: double slab outline should be the aimed HALF only. Added `Block::aimedOutlineEdges(state,world,cell,hitPoint,normal)` virtual (default = outlineEdges; Session's outline build calls it with hit_.point/normal) + `static Block::appendBoxEdges(AABB, out)` helper (outlineEdges refactored onto it). DoubleSlabBlock overrides aimedOutlineEdges; half-pick logic factored into Blocks.cpp anon `aimsTopHalf(hitPoint,cell,normal)` shared by breakResult + outline so they can never disagree.

**CRACK OVERLAY REWORK + TOOLTIP PADDING (2026-07-10, Release BUILT CLEAN).** User: crack overlay covered both slab halves + was "completely broken" on fences (drew on COLLISION boxes — 1.5-tall fence volume; frag mapped UVs in cell space -> slivers/wrapping). FIXES: (a) new `Block::crackBoxes(state,world,cell,hitPoint,normal)` virtual, default = outlineBoxes (visual silhouette); DoubleSlabBlock overrides -> aimed half only (aimsTopHalf shared with breakResult+aimedOutlineEdges, which now derives from crackBoxes). (b) Mining::Crack stores hitPoint+normal (dig() gained params; updated each dig so the half follows aim; restored saves default to cell center). Session crackDraws uses crackBoxes. (c) crack.vert now outputs vLocal (unit-box 0..1) instead of vWorld; crack.frag spans the crack texture across EACH FACE of the box (axis from vLocal derivatives, uv = tangent components) — no more cell-space crops. (d) Tooltip (user added real sprites/tooltip/background+frame.png): frame border art (bd=4*kScale) is drawn inside the box and text pad equaled bd -> text hugged the frame. drawTooltip: pad = bd + 4*kScale breathing room.

**CRACK OVERLAY ROUND 2: CENTERED CROP + FENCE UNION BOX (2026-07-10, Release BUILT CLEAN).** User: don't STRETCH the crack to fit a face — keep 16x16 natural block scale, CENTERED on the face (a 10px face shows the middle 10px crop); and fences must show ONE overlay (was one per sub-box: post + each arm). FIXES: crack.vert now outputs vBlock (position in box, BLOCK units) + flat vSize (box extents); crack.frag: uv = p - s*0.5 + 0.5 per face (face center -> texture center, 1:1 scale, cropped; faces > 1 block would tile via sampler repeat). FenceBlock::crackBoxes override = union AABB of its outlineBoxes -> single merged box (for perpendicular L-connections the box bounds the L, per user request "connect the 2 cubes like it is 1").

**CRACK OVERLAY ROUND 3: GENERIC GEOMETRY REBUILD (2026-07-10, Release BUILT CLEAN).** User: make it complete for EVERY block (stairs: tread/riser centered on their true 8x16, L-shaped sides centered on the L); stacked-slab halves shared dig progress (wrong); crack z-fights/blinks with the block when moving camera. REWRITE: crack overlay is now REAL GEOMETRY built CPU-side in Session.cpp anon-ns `buildCrackMesh(boxes, cell, out)`: (1) every crackBoxes face -> CrackRect{axis,dir,coord,mn,mx}; (2) each face clipped by rect-subtraction against every other box occupying the space just outside it (coincident internal faces vanish; stair base-top becomes the true 8x16 tread); (3) coplanar same-facing rects merge into one REGION (stair L side), texture CENTERED on region bounds at natural scale: uv = t - center + 0.5; (4) verts lifted 0.004 along the face normal. Renderer: Crack = {vector<CrackVertex{pos,uv}>, stage}; per-frame host crackVbuf_[2] (kMaxCrackVerts=4096), crack pipeline vertex input pos+uv, static depth bias (constant -1, slope -1.5) vs z-fight shimmer; push constant only carries the stage layer in boxSize.w. crack.vert/frag now trivial pass-through+sample. Per-part progress: `Block::aimedPart(state,cell,hitPoint,normal)` virtual (0 = whole block; DoubleSlab -> 1/2 via aimsTopHalf); Mining::Crack stores part; dig() restarts progress when part changes. Stairs needed NO block-specific code (generic boxes algorithm). AWAITING USER VISUAL VERIFY (stairs/fence/slab cracks + no blinking).

**CRACK FACE NEIGHBOR CULLING (2026-07-10, Release BUILT CLEAN).** User: crack faces pressed against opaque neighbors were still generated (fence-on-ground bottom face visible via depth bias = "information running for nothing"). buildCrackMesh now takes (world, boxes, cellI, out): any face lying ON the cell boundary (coord <= e or >= 1-e on its axis) whose neighbor cell is opaque (world.registry().isOpaque) is skipped before clipping — same rule as the chunk mesher. Interior faces unaffected.

**DESIGN PHILOSOPHY SHIFT (2026-07-10, user-stated):** the goal is now a "RENOVATED Minecraft — as if Notch still owned it": vanilla behavior is the BASELINE, but the user deliberately deviates where better tech/design improves on vanilla. Don't default to strict MC parity anymore — present vanilla vs improved options where they diverge. First application: user chose SCREEN-SPACE PROJECTED DECALS for the mining crack overlay (replacing the geometry-based buildCrackMesh system) — plan agreed: split frame after opaque world, depth to read-only + sampled, decal pipeline w/ tri-planar projection + depth-derivative normals; keep crackBoxes as the decal volume source (double-slab half, fence union); KEEP the selection outline as geometry (crisp lines don't project well). Decal system doubles as future infra: scorch marks, splatters, blob shadows, path wear. NOT YET IMPLEMENTED (assessment given; user has not said "build it").

**SCREEN-SPACE DECAL SYSTEM + CRACKS PORTED TO IT (2026-07-10, Release BUILT CLEAN, awaiting in-game verify).** Implemented the agreed decal plan. Renderer: (1) Swapchain depth image += VK_IMAGE_USAGE_SAMPLED_BIT; (2) CameraUBOData += invViewProj (all shaders declaring the prefix still fine); (3) createCrack -> createDecal: decalSetLayout_{0 UBO V|F, 1 blockTex F, 2 sceneDepth F} + decalPool_/decalSets_[2] + depthSampler_ (nearest/clamp) + decalPipelineLayout_ (2-vec4 push, V|F stages) + decalPipeline_ (unit-cube volume = outlineVbuf_, cullMode FRONT so camera-inside works, NO depth test, color-only rendering fmt depthAttachmentFormat=UNDEFINED); (4) frame SPLIT in recordCommandBuffer only when cracks exist: world pass (depth storeOp STORE conditional) -> end -> barriers (color WAW + depth ATTACHMENT->SHADER_READ_ONLY) -> color-only decal segment (one vkCmdDraw(36) per crack box, push=boxMin/boxSize/.w=stage layer) -> barriers back -> resume pass (color+depth LOAD) for model/held/drops/outline/weather/UI; (5) drawFrame repoints decal binding 2 at sc_->depthView every frame after the fence wait (resize-safe); cleanup destroys all decal objects. Shaders crack.vert/frag REWRITTEN as decal: vert = padded (0.01) box transform; frag = texelFetch depth, invViewProj reconstruct, volume test slack 5e-3, camera-facing normal from dFdx/dFdy cross, INWARD-boundary rejection (2e-3, n>0.7: kills paint on ground/walls AROUND the block), tri-planar world-anchored fract(uv) at natural scale, alpha*0.9. Session: buildCrackMesh/CrackRect machinery DELETED (~130 lines); crackDraws = one Renderer::Crack{world AABB, stage} per crackBoxes box. Renderer::Crack back to {box, stage}. KEPT: Block::crackBoxes (decal volume: double-slab aimed half, fence union), aimedPart per-part progress, outline as geometry. Decal pass is reusable infra (scorch/splatter/blob shadows). Perf: split only runs while a block is cracked.

**DECAL NOISE FIX: GRID-SNAPPED PLANE REPROJECTION (2026-07-10, Release BUILT CLEAN).** User screenshot: decal worked but crack pixels were salt-and-pepper dithered. Cause: depth-reconstructed world position carries sub-mm per-pixel noise; NEAREST 16-texel crack sampling dithers at texel edges (crack lines are all edges). FIX in crack.frag: reconstruction is now only used to FIND the face — face axis from derivatives, plane coordinate snapped to the 1/16 block-pixel grid (round(approx[axis]*16)/16; ALL block geometry lies on that grid), then the camera ray is re-intersected with the snapped plane analytically -> exact, noise-free positions/UVs; normal = axis-aligned camera-facing (n[axis] = -sign(ray[axis])); grazing rays (|ray[axis]|<1e-6) discarded. Volume + inward-boundary tests now run on exact positions (crisp edges). CONSTRAINT to remember: this assumes paintable surfaces are axis-aligned on the 1/16 grid — if non-grid/rotated geometry ever becomes paintable, the decal shader needs a fallback path.

**DECAL FIX ROUND 2: DEPTH COPY + OCCLUDER PRE-REJECTION (2026-07-11, Release BUILT CLEAN, awaiting verify).** User: noise persisted after the grid-snap AND cracks showed through occluding mesh. Diagnosis: (a) reconstruction math error is ~0.1mm (can't dither) => the depth DATA was bad — sampling the live depth attachment in SHADER_READ_ONLY layout returned compressed/garbage tile-patched reads on AMD 780M (matches rectangular noise patches; Release has no validation layers to catch it); (b) through-mesh was MY snap bug: a nearer occluder's snapped plane can cut through the crack box, re-intersection lands inside -> painted. FIXES: (a) new depthCopy_ image (D32, SAMPLED|TRANSFER_DST, swapchain-sized; createDepthCopy/destroyDepthCopy; recreated in recreateSwapchain; Renderer.cpp now includes <vk_mem_alloc.h>): decal segment now does depth ATTACHMENT->TRANSFER_SRC, vkCmdCopyImage -> depthCopy, copy->SHADER_READ_ONLY, depth back ->ATTACHMENT immediately (pass-3 re-begin no longer transitions depth); decal set binding 2 -> depthCopyView_. (b) crack.frag: PRE-REJECTION — raw reconstructed `approx` must be inside box +-0.02 BEFORE the snap (snap only refines a point known to be on the cracked block). Depth copy cost only while digging (~8MB blit).

**DECAL FIX ROUND 3: VIEW-SPACE RECONSTRUCTION (2026-07-11, Release BUILT CLEAN, awaiting verify).** User sketch showed the crack repeated in a ~16-spacing GRID + per-pixel noise. ROOT CAUSE FOUND: the grid spacing = the 1/16 SNAP grid, not texture tiling — the face-AXIS detection (cross of dFdx/dFdy of reconstructed world pos) flickered per pixel, snapping to both horizontal and vertical plane families. The derivatives were drowned because invViewProj mixes world-scale translations into every pixel: per-pixel fp32 noise (~0.5-2mm) is the same order as one pixel's true world step. Depth DATA was never the problem. FIX: CameraUBOData invViewProj -> invProj; crack.frag reconstructs in VIEW space (small numbers, derivative noise ~1e-6): viewPos = invProj*ndc/w; ray = viewPos * mat3(view) (=R^T v); world = camPos + ray; axis from derivatives of that world (stable); everything else (pre-rejection 0.02, 1/16 plane snap + ray re-intersection, inward-boundary rejection, tri-planar fract uv) unchanged. Kept the depthCopy_ transfer path from round 2 (harmless, robust). NOTE: crack.vert/frag UBO block now declares {view, proj, invProj}.

**DECAL VERIFIED WORKING + MC CRUMBLING BLEND (2026-07-11, Release BUILT CLEAN).** User confirmed round 3 (view-space reconstruction) "nailed it" — decal cracks are clean. Then asked for MC-style blending (light crack texels lighten the block, dark darken). Decal pipeline blend switched from alpha to MC's crumbling "modulate 2x": srcColorBlendFactor DST_COLOR + dstColorBlendFactor SRC_COLOR (out = 2*src*dst; mid-gray = unchanged), alpha ZERO/ONE; crack.frag outputs the texel straight (removed a*0.9 fade), discard a<0.05 unchanged. Note: blending happens in linear space (sRGB attachment) vs MC's gamma — visually equivalent.

**DATA-DRIVEN PLAYER MODEL + SKIN LAYER TOGGLES WIRED (2026-07-11, Release BUILT CLEAN, json parse confirmed at runtime).** PlayerModel.cpp now parses assets/models/entity/player.json (Bedrock geometry, identifiers geometry.npc.steve / geometry.npc.alex): bone name -> {PP part, SkinLayer bit} map (body/jacket, head/hat, arms/sleeves, legs/pants; root/waist/cape/items skipped); bedrock +X = character's LEFT so X is mirrored (box [x,x+w] -> [-(x+w),-x], pivot.x negated) — addBox's UV convention already encodes that mirror so textures land right; cube fields origin/size/uv/inflate + bedrock `mirror` flag (addBox gained mirror: per-face U swap + east/west cell swap; alex leftLeg uses it). Old hand-coded builder kept as buildBuiltinParts fallback (stderr warning). NOTE geometry deltas now authoritative from json: arm pivots +-5 (was +-4), leg pivots +-1.9, alex arms y 11.5, overlay inflates hat 0.5 / rest 0.25. PlayerModel.h: enum SkinLayer {SL_Hat..SL_RightPants, SL_All}; buildPlayerModelParts(slim, layerMask=SL_All). The options menu's EXISTING Skin Customization toggles (opt keys skinHat/skinJacket/skinLSleeve/skinRSleeve/skinLPants/skinRPants; cycle index 0=ON 1=OFF) are now WIRED: main.cpp skinLayerMask() lambda -> modelLayerMask; per-frame check rebuilds model+preview on change (vkDeviceWaitIdle + modelRenderer cleanup/init + playerPreview.rebuildSkin — same pattern as skin upload, which now also passes the mask). Held-item hand anchor/FP arm pose still hardcoded in main.cpp (item display tables + animations were already data-driven).

**SELECTION OUTLINE PORTED TO THE DECAL PASS (2026-07-11, Release BUILT CLEAN, awaiting verify).** User asked for the outline as a decal too (and confirmed understanding that outline "lines" are thin bars = triangles; corners covered because adjacent bars meet — matches their sketch; stairs stay one union silhouette since Session's outline_ bars come from aimedOutlineEdges). IMPLEMENTATION: Session unchanged (already produces world-space bar AABBs, kBarHalf=1/128). Renderer: decal segment now also runs when outlineBoxes nonempty (`doDecals = !cracks_.empty() || !outlineBoxes.empty()` gates segment + depth storeOp) and draws each bar as a thin decal volume with pc.boxSize.w = -1.0 (color mode). crack.frag: `if (pc.boxSize.w < 0) { outColor = vec4(0.03) }` — near-black under modulate-2x blend = crisp dark line painted ON the surfaces crossing the bar; shared slack tightened 5e-3 -> 1.5e-3 so bars paint at true width. REMOVED: outlinePipeline_ (creation/draw/destroy; createOutline now only builds the shared unit-cube vbuf; outline.vert/frag still used by the OCCLUSION pipeline). Depth-copy+decal segment now runs whenever a block is targeted (~always in game; ~8MB blit, fine). Behavior notes: outline paints only on surfaces present in the depth buffer (silhouette edges get the line on the block's own face only; base edges show a hairline on adjacent ground = crease look).

**OUTLINE REVERTED TO GEOMETRY — DECAL LIMIT ACKNOWLEDGED (2026-07-11, code done, build pending game close).** User found the decal outline's fundamental limit: a fence's selection box is LARGER than its mesh (full-height arm boxes vs two thin rails), so outline edges floating in air have no surface in the depth buffer to paint on — parts of the box vanish. AGREED SPLIT (matches vanilla): cracks = SURFACE DAMAGE -> decal (kept); outline = SELECTION VOLUME that may float in air -> GEOMETRY (restored). Reverted: outlinePipeline_ re-added (depth test+WRITE LESS_OR_EQUAL — keeps the clouds-over-outline fix), pass-3 outline draw restored, doDecals = !cracks_.empty() only, outline draws removed from the decal segment. KEPT: crack.frag's color mode (boxSize.w < 0 -> flat near-black; unused now, ready for future flat-color decals), slack 1.5e-3, all decal infra.

**FENCE OUTLINE HUGS THE MESH (2026-07-11, Release BUILT CLEAN).** User: bounding box closer to the mesh + not floating/no z-fight. FenceBlock::outlineBoxes now vanilla-style: full-height 4x4 post + per-connection 2px-wide arm box spanning both rails (y px6..px15, z px7..px9) instead of full-height 4-wide slabs; deleted the FenceBlock::outlineEdges union-silhouette override (default 12-edges-per-box is right now that boxes match the shape; small junction rectangles on the post face are vanilla-like). appendFenceArms now used by collision only. crackBoxes still union bbox of outlineBoxes. Also affects targeting: can no longer target the empty air above fence rails (vanilla ✓). No-z-fight/no-float comes from the bar design: bars straddle the edges (kBarHalf each side) + outline pipeline writes depth.

**PLAYER MODEL LIMBS SPLIT AT JOINTS (2026-07-11).** assets/models/entity/player.json REWRITTEN: geometry.npc.steve AND geometry.npc.alex now have 25 bones each — limbs split into upper+lower child bones with joint pivots: leftForearm/rightForearm (elbow, steve y=18 / alex y=17.5), leftShin/rightShin (knee y=6), overlays split too (leftForeSleeve/rightForeSleeve/leftShinPants/rightShinPants), leftItem/rightItem re-parented onto forearms. Split cubes use PER-FACE UV (no box uv). Alex 3-wide arm UVs computed for the split; alex leftLeg no longer uses mirror (own region [16,48] etc. like steve — modern-skin correct). geometry.cape untouched. All parents verified resolving. STOPGAP in PlayerModel.cpp boneTarget(): the 8 new bone names FOLD into their parent PP part (leftForearm→PP_LeftArm etc.) so the 6-part game renders full limbs (rigid, as before) until rig A1 — marked with FUTURE(ik) comment.

**ANIMATION SYSTEM V2 "AnimGraph" — DESIGN LOCKED (2026-07-11). Doc = docs/animation.md (THE contract; read it before touching src/anim).** User chose: FULLY CUSTOM runtime (not Bedrock controllers), true blend trees from day one, whole runtime scope. Architecture: entity-agnostic (rig+graph+blackboard per entity; player/horse/boat all data) — Blackboard (typed params + auto-clear triggers, gameplay writes, Molang reads as v.<name>) → layers (bone masks, override/additive-stub) → state machines (Molang-guarded prioritized transitions, cross-fade smoothstep, one-shots w/ on_finished + interruptible_after, frozen-pose interrupts) → blend nodes (Clip/Blend1D day1; Blend2D+Additive = LOUD parsed stubs) → clips (native .clip.json w/ optional import_bedrock from Blockbench exports + events{time,name,bone} + named scalar curves + sync_group). Pose = per-bone local TRS, QUAT nlerp shortest-arc (P5, never blend euler). Principles: P1 gameplay decides WHAT/anim HOW (world probing never in runtime), P2 physics-authoritative no root motion (root kept in pose for later warping), P3 events/curves ARE gameplay signals (oar thrust windows, hit windows, footsteps), P6 loud stubs no dead code. Sync groups: normalized phase, highest-weight leader, events fire from DOMINANT member only (one foot_down per blended step). Sockets = bone/locator world transforms (rightItem held-item anchor migrates off main.cpp hardcode; saddle/feet later). Debug introspection DAY ONE: DebugSnapshot ring buffer (~10s) per update → future anim_studio timeline debugger (CS2 animgraph/GTA-VI-leak style: state bars, transition wedges, event ticks — user explicitly requested). FUTURE(tag) grep convention registered in doc §13: combat/parkour/mounts/boat/decals/snow/ik/root-motion/anim-debugger/debug-draw/sync-markers/smooth-keys/first-person/tools. Parkour = Blend2D(height,distance)+playback_rate arc matching; boat = cross-entity sync + oar_in_water curve; footprints/snow = foot_down event + foot socket consumers. Milestones A0-A7 + T1 in doc §12. Tooling decision: Blockbench stays THE keyframe editor (import_bedrock); graphs = hand JSON; anim_studio tools exe (T1, AFTER runtime A4+) = preview + timeline debugger — user REQUIRED editors as SEPARATE exes, never in game exe.

**A0 DONE (2026-07-11, Release BUILT CLEAN incl. stopgap).** CMakeLists split: mc_engine STATIC lib (all sources minus main.cpp; PUBLIC includes/links/defs so tools inherit) + MineCraftCPP exe (main.cpp only, links mc_engine) + option MC_BUILD_TOOLS add_subdirectory(tools) (tools/CMakeLists.txt = template comments only, no targets). Cosmetic D9025 (/W1 vs /w) on vma_impl/stb_impl via lib default — harmless. NEXT: A1 = src/anim/Rig+Pose (generic rig from geometry JSON incl. locators; add foot locators to shins in player.json), PlayerModel → per-bone meshes (kill PP_ enum), ModelRenderer → N matrices. Game runs visually unchanged (awaiting user verify of folded limbs).

**LIMB UV BREAKAGE + USER-CHOSEN FIX (2026-07-11).** The split-limb player.json initially used PER-FACE UV objects (Blockbench-export style) but PlayerModel.cpp's parser only reads BOX UV `uv:[x,y]` → limbs defaulted to uv(0,0) = Steve HAIR region, overlays went opaque-brown (user screenshot: fat dark limbs). Offered builder fix (per-face support in addBox, ~35 lines) vs JSON-only; **user explicitly chose JSON-only: player.json limbs rewritten as box-UV cubes** — upper halves keep the limb's original uv origin, lower halves shift tv +6 (e.g. rightArm (40,16)→rightForearm (40,22); sleeves/pants overlays same +6). KNOWN ACCEPTED ARTIFACT: box UV cannot place the true bottom cell for the lower cubes → hand undersides + foot soles (and overlay bottoms) sample side-band texels until per-face UV support lands with the A1 generic rig parser (then restore the exact per-face export from model.geo.json, kept in assets/models/entity/ as reference). Internal elbow/knee seam faces are meshed (box UV can't skip faces) but invisible. Asset-only change: must copy player.json into build/bin/<cfg>/assets/... manually (POST_BUILD copy only runs on rebuild).

**JSON-ONLY SUPERSEDED SAME DAY → PER-FACE UV SUPPORT IMPLEMENTED (2026-07-11, lib compiled; exe LINK BLOCKED by running game LNK1104 — relink when user closes it).** User realized their Blockbench export used per-face UV precisely because they DISABLED the internal joint faces (no skin region exists for elbow/knee cross-sections), and asked to fix the 6px-off end caps + handle joint faces — both are per-face-only features, so the builder upgrade came back with user buy-in. PlayerModel.cpp: FaceUV/PerFaceUV structs + readFaceUV/readPerFaceUV (cube "uv" object form; absent face = NOT meshed = Blockbench disabled face); addBox gained `const PerFaceUV* pf` param — each face's rect substitutes into the SAME slot orientation as box UV (band faces as-is; up: U+V flipped; down: V flipped — vanilla down faces ship negative uv_size and land upright), mirror flag IGNORED for per-face (exporters bake it). Slot mapping VERIFIED numerically: per-face export of an unsplit limb reproduces box-UV coords exactly on all 6 faces. player.json RESTORED to per-face split-limb form (from user's model.geo.json export) PLUS **textured JOINT CAPS** (user asked for a connection-texture solution): each base limb cube's joint face samples the 4x4 front-cell slice at joint height so caps match any worn skin — steve elbow caps [44,24]/[36,56], knee caps [4,24]/[20,56] (upper cube "down" + lower cube "up" share the region); alex arm caps 3-wide [44,24]/[36,56]; overlays (sleeves/pants) keep OPEN joints (base cap shows through 0.25 gap). Caps invisible while limbs straight (interior), become the visible cross-section when joints bend post-A1. End caps (hand undersides/foot soles) now correct via per-face down cells w/ negative uv_size. NOTE: per-face UV support lives in PlayerModel.cpp addBox — A1's generic rig parser should REUSE/move this logic, not duplicate it.

**JOINT-CAP LOD-AVERAGE + HAND/SHOE UV FLIPS (2026-07-11, Release BUILT CLEAN, awaiting visual verify).** User asked (LOD-style) to average the ring of texels at each limb seam and paint the hidden joint-cap faces with that flat color (base arms/legs only, not sleeves/pants). Implemented in game/PlayerSkin.cpp `patchSkinJointCaps(rgba,w,h)` (64x64 only): averages the 2 texel rows adjacent to each elbow/knee across the limb's 16-col strip (skips transparent = works for slim), writes 4 results into reserved dead pixels row0 (0,0)=rightArm (1,0)=leftArm (2,0)=rightLeg (3,0)=leftLeg; called in ModelRenderer::loadSkin before GPU upload (covers game+preview+skin-upload since all go through loadSkin). player.json joint caps now sample those pixels: cap faces use uv:[dstX,0] uv_size:[1,1]. Also per user: HAND bottom faces (forearm/foresleeve "down") U-mirrored (negative uv_size U), SHOE bottom faces (shin/shinpants "down") rotated 180 (negative U, positive V + shifted origin). All via per-face UV. Still needs in-game visual verify (caps only visible when joints bend).

**ANIM A1 DONE — GENERIC RIG + POSE, PER-BONE MESHES (2026-07-11, Release BUILT CLEAN, awaiting visual verify).** Killed the fixed 6-part PP_ model; player is now a data-driven skeleton. NEW src/anim/Rig.{h,cpp}: parses bone hierarchy+pivots+locators from geometry JSON, mirrors X to our space, TOPO-SORTS (Bedrock lists child-before-parent, e.g. body before waist), findBone/findBoneCI/findLocator; kMaxModelBones=32 constant lives here. NEW src/anim/Pose.{h,cpp}: per-bone local {rot mat4 (about pivot), pos vec3}, compose(rig,out) = world[i]=parent<0?local:world[parent]*local with local=T(pos+pivot)*rot*T(-pivot); socketWorld(locator). PlayerAnimator: REMOVED evaluate()(PP_Count); ADDED evaluatePose(in,rig,pose) — writes the vanilla layer stack as LOCAL transforms keyed by bone NAME (root/waist/body/head/leftArm/rightArm/leftLeg/rightLeg via findBoneCI), untouched bones (forearms/shins/overlays) keep identity local and inherit parent via compose. boneRotation() helper (pure Euler->matrix, no pivot). Pivots now from JSON (arms ±5 vs old ±4, legs ±1.9 vs ±2) — sub-pixel diff only on bob/attack (walk swing is X-rot, pivot.x irrelevant), accepted. PlayerModel: removed PlayerPart enum/PlayerModelParts/buildPlayerModelParts/buildBuiltinParts; ADDED PlayerRigMeshes{ModelData bones[32];int count} + buildPlayerRigMeshes(rig,slim,mask) routing each bone's cubes to bones[rigIndex] (reuses addBox+per-face UV); overlayLayerOf(name) for layer toggles (incl. foreSleeve/shinPants). ModelPose now bones[32]+count+mask+opacity (was parts[PP_Count]); ModelRenderer/PlayerPreview take PlayerRigMeshes, loop boneCount_, skip empty-mesh bones. main.cpp: reloadRig(slim) lambda loads rig+refreshes bone indices (biHead/biHat/biRightArm/fpChain[4]/biBend[4]), called at start + on skin arm-width flip; buildBodyPose(in,place,headLook) lambda = evaluatePose->head look on biHead local->F6 debug bend on forearms/shins->compose->place*world into ModelPose; 3rd-person + preview use it; 1st-person sets the right-arm CHAIN (arm+forearm+sleeves) to one rigid armM matrix (identical to old baked arm) + mask; spectator ghost = head+hat bones; held item anchors to pose.bones[biRightArm] (FUTURE: rightItem socket). NEW F6 key cycles debugBendDeg 0/45/90 to flex elbows+knees (FUTURE(ik) proof, temporary). Foot locators added to all 4 shin bones in player.json ({foot:[±1.9,0,0]}, FUTURE decals/snow). CMake: added Pose.cpp+Rig.cpp to ENGINE_SOURCES. Builds clean no warnings; json parses, 4 foot locators. AWAITING USER VISUAL VERIFY: 3rd-person body/walk/attack/sneak identical, 1st-person arm+held item unchanged, skin toggles + skin upload (steve/alex) work, spectator head+hat, F6 bends forearms/shins independently. NEXT: A2 (clips: native .clip.json + import_bedrock) or migrate held item to rightItem socket.

**HELD ITEM ANCHOR NOW DATA-DRIVEN via rightItem BONE (2026-07-12, Release BUILT CLEAN, awaiting visual verify).** User pointed out the held item should use the JSON parent hierarchy, not a hardcoded offset: rightItem bone is parented to rightForearm in player.json with the vanilla hand pivot (steve [-6,15,1], alex [-6,14.5,1]), so it auto-follows the forearm via Pose::compose. main.cpp: removed hardcoded biRightForearm + trans({handX,10.5,-4}) + handX var; added biRightItem=findBone("rightItem"); fpChain grown to 5 to include rightItem (so pose.bones[rightItem]=armM in FP); held anchor = pose.bones[biRightItem] * trans(playerRig.bone(biRightItem).pivot) (bone world follows forearm incl. F6/elbow bend; pivot = actual attach point; slim/wide handled by the JSON pivot, not code). CAVEAT: the attach POINT changed from the old tuned hardcode {6,10.5,-4} to the vanilla pivot {6,15,1}/{6,14.5,1} — if the held block now sits visibly wrong, the fix is the block DISPLAY TABLE (data-driven thirdPerson/firstPerson ItemTransform) or the rightItem pivot in JSON, NOT re-hardcoding main. User to verify held block position (3rd person + F6 follow, first person) before A2. [VERIFIED — user said "seams good".]

**ANIM A2 DONE — CLIPS (native + import_bedrock), single-clip playback (2026-07-12, Release BUILT CLEAN, awaiting visual verify).** Extracted shared channel machinery into NEW src/anim/AnimChannel.{h,cpp}: moved AnimVec3/AnimKey/AnimChannel/AnimBone structs (out of PlayerAnimator.h) + free funcs toLowerName/parseChannel/sampleAnimChannel/boneRotation/boneTransform (out of PlayerAnimator.cpp anon ns). PlayerAnimator.h now includes AnimChannel.h, keeps only BedrockAnim+class; PlayerAnimator.cpp includes it, renamed toLower->toLowerName, sampleChannel->sampleAnimChannel (used by evaluatePose+evaluateFirstPerson); kPivots/boneTransform still used by FP path. NEW src/anim/AnimClip.{h,cpp}: AnimClip{duration,loop,bones(name->AnimBone),events(ClipEvent{time,name,bone,arg}),curves(name->ScalarKey[])}; load() parses assets/anim/clips/*.clip.json — optional import_bedrock{file,animation} pulls channels+length+loop from a Bedrock anim (resolve("assets/"+file)), then inline "bones" override; parses events + curves; duration falls back to max keyframe time. sample(time,rig,pose,ctx) writes per-bone LOCAL transforms (boneRotation for rot, mirror-x for pos) — same convention as evaluatePose, untouched bones left alone (layerable). curve(name,time) linear-lerp; collectEvents(from,to) fires (from,to] (no loop-wrap yet, FUTURE A6). Native + bedrock share the channel format so parseChannel works on both. Interp = linear+step only (catmull-rom = FUTURE smooth-keys). NEW assets/anim/clips/test_walk.clip.json: 1s loop walk, legs/arms swing ±35° opposite + leftShin/rightShin knee flex -55° (drives the NEW joints from a clip) + 2 foot_down events + bob_amount curve. main.cpp: loads testClip; F7 toggles clipTestOn; buildBodyPose branches — if clipTestOn plays testClip at fmod(lifeTime,dur) via sample() instead of animator.evaluatePose. CMake: added AnimChannel.cpp+AnimClip.cpp. Builds clean no warnings; clip parses (6 bones, 2 events). AWAITING USER VERIFY: press F7 in 3rd person -> body does the walk-in-place loop (legs+arms swing, knees flex), loops seamlessly; F7 again -> back to normal vanilla anim. NEXT: A3 (Blackboard + Molang binding + Blend1D + sync groups: idle->walk->run by real speed) — needs quat pose blending (Pose currently mat4 rot; A3 converts to quat nlerp per P5).

**ANIM A3 DONE — BLEND TREES (quat pose blend + Blackboard + Blend1D + sync) (2026-07-12, Release BUILT CLEAN, awaiting visual verify).** (1) QUAT POSE (P5): Pose::rot mat4->glm::quat; added Pose::blend(a,b,t) = pos mix + rot nlerp SHORTEST-ARC (flip qb if dot<0, normalize(a*(1-t)+qb*t)); compose uses mat4_cast. boneRotation() now returns glm::quat (angleAxis Z*Y*X matching old Rz*Ry*Rx); boneTransform still mat4 (FP), builds via mat4_cast. main buildBodyPose split into finishPose(Pose&,place,headLook quat) [head look now quat mul, debug bend quat angleAxis] + buildBodyPose [vanilla evaluatePose->finishPose]; head-look call sites build quats (angleAxis Y * angleAxis X). FP path unaffected (sets pose.bones mat4 directly, not pose.rot). (2) NEW src/anim/Blackboard.h (header-only): typed param store (setFloat/Bool/Int, fire trigger, getFloat/Bool, toMolang copies values+triggers as variable.<name> so Molang v.<name> reads them, clearTriggers). P1 contract: gameplay writes, graph reads, runtime never queries world. (3) NEW src/anim/Blend1D.{h,cpp}: clips at param points (sorted), bracket(x)->i0,i1,t; sample() picks 2 bracketing clips, samples BOTH at the SAME normalized phase*clipDur (implicit sync group = no foot slide), pose.blend(a,b,t); duration(x)=weighted lerp of the 2 durations (caller advances phase by dt/duration so stride freq scales w/ speed). Blend2D/Additive = FUTURE. (4) NEW clips loco_idle(2.4s breathing arms Z-sway)/loco_walk(1.0s ±35 legs+arms, knee flex -45)/loco_run(0.6s ±58 legs, ±80 arms, knee -85, body lean 12). (5) main: locoBlend.add(0=idle, 4.7=walk[kWalkSpeed], 7.5=run[*kSprint]); Blackboard blackboard; float locoPhase; F7 (repurposed from A2 single-clip) toggles clipTestOn -> world 3rd-person branch blends by length(velocity.xz), sets bb speed, advances locoPhase by dt/locoBlend.duration(speed), locoBlend.sample->finishPose. Preview stays vanilla. (6) Extracted shared channel machinery earlier into AnimChannel.{h,cpp} (A2). CMake +Blend1D.cpp. Builds clean; clips parse. AWAITING USER VERIFY: F7 on, 3rd person — stand=idle breathing, walk=walk cycle, sprint=run (bigger/faster/lean), SMOOTH blends as speed changes, no foot sliding; F7 off=vanilla. test_walk.clip.json now unused (kept as format ref). NEXT: A4 (state machines: Molang-guarded cross-faded transitions, one-shots, the stand-jump vs run-jump founding demo; graph JSON loader).

**A3 FEEDBACK ROUND 1 (2026-07-12, Release BUILT CLEAN, awaiting verify).** (1) FEET SKATING FIX: the loco stride phase was TIME-driven (locoPhase += dt/duration = fixed cadence regardless of speed = bad skate). Now DISTANCE-driven: locoPhase = fmod(player.limbSwing()/kStrideMeters, 1.0), kStride=2.0m. player.limbSwing() = metres travelled (Player.cpp updateAnimation: limbSwing_ += speedXZ*dt only while striding=onGround&&!flying), so cadence tracks ground speed like vanilla → feet plant with travel, frozen off-ground. RESIDUAL skate remains (in-place anim, no root motion/IK per P2 — full fix = FUTURE(ik) foot-lock; matches vanilla skate level). (2) DEFAULT clipTestOn=true so the loco system shows on spawn (F7 still toggles to vanilla). (3) ARM-SWAP DIAGNOSTIC: user suspects PlayerModel.cpp swaps L/R arms. loco_idle now raises rightArm (rotation [-160,0,0]) so standing shows the right arm up; leftArm keeps the Z-sway. Reading: default 3rd-person camera is BEHIND player, so character's RIGHT arm is on the VIEWER'S LEFT — the screen-left arm should raise; if screen-right raises, arms ARE swapped. (Traced the path: buildPlayerRigMeshes routes "rightArm" cubes to the rightArm rig bone meshed at our +X=right w/ rightArm UV region; evaluatePose/clip map "rightarm"->rightArm bone — no swap found in trace, test confirms.) NOTE default perspective is First person, user must F5 to 3rd person to see body. locoPhase now derived per-frame (persistent decl vestigial). [A3 VERIFIED: user confirmed arms correct (behind-view right arm on screen-right = correct; my earlier "should be screen-left" was WRONG — same-facing back view is NOT mirrored). Skating partly placeholder-clip quality; user will supply real anim later — don't over-tune now.]

**ANIM A4 DONE — STATE MACHINE + graph JSON (2026-07-12, Release BUILT CLEAN, awaiting verify).** THE founding demo (stand-jump vs run-jump). NEW src/anim/AnimStateMachine.{h,cpp}: AnimNode{Clip|Blend1D, clip ptr, Blend1D, param}; AnimTransition{target idx, MolangExpr condition, duration(crossfade s), priority, force}; AnimStateDef{name,node,oneShot,onFinished idx,interruptibleAfter,transitions}. load(graphPath) parses graph JSON: "clips"{name->file} loaded into clips_ map (stable ptrs), "states"{name->{node(blend1d w/ parameter+points[{value,clip}] | clip),one_shot,on_finished,interruptible_after,transitions[{to,condition Molang,duration,priority,force}]}}, "initial"; TWO-PASS name resolution (parse w/ target names in parallel vectors, resolve to indices, THEN stable_sort transitions by priority desc — sorting-after-resolve was the bug I fixed). update(dt,bb,stridePhase,rig,out): stateTime+=dt; eval transitions first-true-in-priority (one-shots skip non-force edges until interruptibleAfter); else one-shot done (stateTime>=clipDur) -> on_finished; on fire, FREEZE lastOut_ pose -> crossfade blend(fromPose,live,t=crossT/crossDur) over duration; sampleState: Blend1D uses bb.getFloat(param)+stridePhase, Clip oneShot=min(stateTime,dur)hold / looping=fmod(stateTime,dur). Unknown node type = loud stub (FUTURE blend2d/additive). currentState() getter. Pose stored BY VALUE in SM (fromPose_/lastOut_), header includes Pose.h. NEW clips: jump_stand(0.5 symmetric crouch+arms up), jump_run(0.5 asymmetric leap+lean), fall(0.8 loop arms out), land(0.35 crouch absorb). NEW assets/anim/graphs/player.graph.json: locomotion(blend1d speed idle0/walk4.7/run7.5) + jump_stand/jump_run(one_shot, on_finished fall, interruptible 0.18, ->land if is_on_ground pri1) + fall(->land) + land(one_shot ->locomotion, ->jump pri2). Player.h: added onGround() getter. main: playerGraph.load; Blackboard blackboard; F7/clipTestOn(default true) branch feeds bb speed=len(vel.xz)+is_on_ground, fires jump_pressed trigger on SPACE rising-edge while onGround&&!flying, stridePhase=fmod(limbSwing/2.0,1), playerGraph.update->finishPose, clearTriggers; prints '[Graph] state -> X' on change (stderr readout, NOT yet on F3 overlay). Removed standalone locoBlend/locoIdle/etc (graph owns them now). CMake +AnimStateMachine.cpp. Builds clean; graph parses 5 states/7 clips. CAVEAT: graph only updates in 3RD PERSON (pose branch); FP freezes it — user must F5 to 3rd person. Jump uses GLFW_KEY_SPACE directly (not keyBinds). AWAITING USER VERIFY: F5 3rd person; stand+jump -> symmetric jump_stand anim; run+jump -> asymmetric jump_run; smooth cross-fades; land absorb; walk off ledge -> fall->land; console prints state changes. NEXT: A5 (layers + bone masks: upper-body action over locomotion legs).

**CLIP SIGN CONVENTIONS (2026-07-12) — learned from user feedback; clips are Bedrock-convention (boneRotation negates X, leaves Y/Z).** Verified against vanilla player.animation.json: (a) LEG/ARM X rotation: NEGATIVE = swings FORWARD (toward -Z facing), POSITIVE = backward (vanilla move.legs: leftleg X = tcos0*-1.4 = fwd when moving). (b) ARM Z (idle bob outward sway): leftArm Z NEGATIVE = outward, rightArm Z POSITIVE = outward (vanilla bob signs). My placeholder clips were authored with INVERTED signs → user saw idle arms sway INWARD + knees bend wrong way. FIXES: loco_idle arm Z flipped (leftArm -3, rightArm +3 = outward). SHIN/knee: user clarified thighs swing forward FINE but the KNEE folded backward; flipped all shin X signs NEGATIVE->POSITIVE in loco_walk/loco_run/jump_stand/jump_run/land (thighs+arms UNCHANGED per user). Shin POSITIVE X = knee flex (foot tucks back/up) = natural. Asset-only change, synced to build/bin/*/assets, game RESTART needed (clips load at startup). AWAITING USER VERIFY the knee now folds the right way; if still wrong it's a one-line shin sign flip back. NOTE the F6 debug bend in main uses angleAxis directly (NOT boneRotation, no negate) so its sign convention differs from clips. [A4 + clip signs VERIFIED — user "looks good".]

**ANIM A5 DONE — LAYERS + BONE MASKS (2026-07-12, Release BUILT CLEAN, awaiting verify).** Upper-body action over locomotion legs. REFACTOR: AnimStateMachine no longer owns clips — removed clips_ member; load(path) -> build(const JsonValue& container, const map<string,AnimClip>& clips) (container = layer obj w/ its own "states"+"initial"; findClip uses shared lib; node-less states allowed = identity pose). NEW src/anim/AnimGraph.{h,cpp}: owns shared clips_ + vector<Layer>{name,BoneMask,additive,AnimStateMachine,emptyState,weight}. BoneMask{vector<char> in; empty=full body; has(b); build(rig,bones,recursive,exclude)}: marks named bones, recursive propagates to descendants via topo parent-marked pass, exclude removes bones+subtrees. load(path, RIG) [rig passed so masks resolve names->indices at load; steve/alex share names so valid across skin swap]: parses "clips" -> lib, "layers"[] each {name, mask{bones,recursive,exclude}, additive, empty_state, +states/initial via machine.build}; backward-compat: no "layers" => one full-body "base" layer from root. update(): layer0.machine.update -> out (full body base); for each higher layer: machine.update->lp, weight eases toward (currentState!=emptyState?1:0) at dt*12, then for masked bones out.rot=nlerp(out.rot,lp.rot,weight) shortest-arc + out.pos=mix — so the action fades in/out and only touches its masked bones. additive layer = loud stub + skipped (FUTURE combat). currentState(layer) getter. NEW wave.clip.json (1.3s one-shot: rightArm Z 10->130 raise out, rightForearm Z oscillates ±28 = hand wave). player.graph.json REWRITTEN to "layers":[locomotion(the A4 states) , action(mask rightArm recursive, empty_state none, states none[->wave if v.wave_pressed]/wave[one_shot ->none])]. main: AnimStateMachine->AnimGraph playerGraph; load(...,playerRig); H key fires wave_pressed trigger; currentState()->currentState(0). CMake +AnimGraph.cpp. Builds clean; graph parses 2 layers, action mask rightArm recursive. AWAITING USER VERIFY: F5 3rd person, press H while standing/walking/running -> RIGHT arm raises + waves while legs keep walking/running + left arm keeps swinging; wave fades in/out smoothly; jumps still work. NEXT: A6 (events + curves + debug snapshot ring: foot_down fires per step, one per blended step).

**ANIM A6 DONE — EVENTS (dominant-clip) + DEBUG SNAPSHOT RING (2026-07-12, Release BUILT CLEAN, awaiting verify).** Blend1D::dominantClip(x) = higher-weight bracketing clip (t<0.5?i0:i1) — the sync-group leader whose events fire (one foot_down per blended step, not two). AnimStateMachine: struct AnimFiredEvent{name,bone}; update() gained `std::vector<AnimFiredEvent>* events` param + prevStridePhase_ member; captures prevStateTime (pre-dt), resets to 0 on transition; fireEvents(state,bb,prevStateTime,stridePhase,events): picks the active clip (Clip node: pT/cT from stateTime, oneShot=min(_,dur)/looping=fmod; Blend1D node: dominantClip + pT=prevStridePhase_*dur, cT=stridePhase*dur), collectEvents((pT,cT]) w/ loop-wrap handling (cT<pT -> collect (pT,dur] + (-eps,cT]) -> AnimFiredEvent. AnimGraph: events_ vector cleared+filled each update (all layers' machines get &events_), events() getter; GraphSnapshot{time,speed,layers[{state,weight}],eventCount} recorded every update into a 600-frame (~10s) ring_ + latestSnapshot() — infra for T1 anim_studio timeline (FUTURE anim-debugger). loco_walk events foot_down leftShin@0.0/rightShin@0.5; loco_run leftShin@0.0/rightShin@0.3. main drains playerGraph.events() -> stderr '[anim] foot_down (leftShin)' (the P3 fan-out point: FUTURE sound/decals/snow/boat hook here). Builds clean. AWAITING USER VERIFY: F5 3rd person + walk -> console prints ONE foot_down per step alternating leftShin/rightShin; sprint faster cadence; accelerate walk->run = still one per step (no double at blend); stand=silent. NEXT: A7 (retire legacy PlayerAnimator once graph covers everything; first-person parity) — OR jump to T1 anim_studio tools exe (timeline debugger over the snapshot ring), or wire real decals/sound to foot_down. Curves parsed on AnimClip but not yet exposed on AnimGraph (wire when boat/thrust needs it).

**FOOT-LOCK IK DONE (2026-07-12, Release BUILT CLEAN, awaiting verify).** User chose foot-lock IK over root motion (keeps crisp physics-driven movement, pins planted feet so no skate; discussed: root motion = weighty but fights voxel feel + needs authored root translation; NO physics engine needed — existing voxel AABB collision suffices; Box3D stays for ragdolls/debris). NEW src/anim/FootIK.{h,cpp}: solveTwoBoneIK(hip,target,L1,L2,poleHint,&upperWorld,&lowerWorld) analytic law-of-cosines 2-bone IK in model space; clamps reach; knee bend plane from poleHint perpendicular to hip->target; outputs WORLD quats rotating rest down-axis(0,-1,0) onto each bone dir (quatFromTo shortest-arc). foot_up events added to loco_walk (leftShin down0/up0.5, rightShin down0.5/up0.99) + loco_run (left down0/up0.3, right down0.3/up0.59). main: reloadRig computes ikLeg/ikShin[2] indices + ikHip[2]=leg pivot + ikFoot[2]=shin "foot" locator pos (per side, [0]=left); FootLock{pin,weight,planted}[2] state. In loco branch after playerGraph.update: release locks if !onGround; foot_down event -> planted+freshPin, foot_up -> unplant; compose p->ikWorld; on freshPin record pin = place*shinWorld*ikFoot, then pin.y = feetPosition().y (plant on ground plane); weight eases to planted?1:0 at dt*15; if weight>0: target_model=inverse(place)*pin, solveTwoBoneIK(ikHip, target, 6,6, pole(0,0,-1)) -> p.rot[leg]=slerp(_,thighWorld,w), p.rot[shin]=slerp(_,inv(thighWorld)*shinWorld,w); then finishPose recomposes. ASSUMES leg parent (root) has no rotation (true for loco) -> thighLocal=thighWorld. L1=L2=6px. CMake +FootIK.cpp. Builds clean. AWAITING USER VERIFY + likely TUNING KNOBS: (1) if KNEE bends backward -> flip pole (0,0,-1)->(0,0,1); (2) feet POP on lock/unlock -> weight ease rate dt*15 or foot_up timing; (3) feet FLOAT/SINK -> pin.y projection (currently feetPosition().y); (4) legs JITTER -> pin recording / double-compose; (5) leg TWIST -> shortest-arc roll (may need knee-forward constraint). Only active when clipTestOn (F7 on, default true) + 3rd person + onGround.

**FOOT-LOCK IK ROUND 2 — STOP-RELEASE FIX + PELVIS BOB (2026-07-12, Release BUILT CLEAN, awaiting verify).** User feedback: legs froze on stop; steps 'canceled'. Root causes: (1) locks never released when STOPPED (distance-driven phase freezes mid-step -> pending foot_up never fires); (2) GEOMETRY: hip at model y=12, leg len 12 (thigh6+shin6) = straight leg exactly spans hip-to-ground, so a ground-pinned foot is UNREACHABLE at ANY horizontal offset -> IK maxed out instantly. FIXES: (1) release locks when `striding = onGround && speed>0.6` false (main IK block). (2) USER'S INSIGHT (correct, I'd missed it): real walk = pelvis dips when legs splay so legs reach farther. Added PELVIS BOB: after pins+weights, compute pelvisDrop = max over planted feet of (hipY - sqrt(legLen² - horizOffset²))*weight, cap 4.5px; lower p.pos[biRoot].y -= drop; recompose (hips now lower); THEN foot IK from lowered hip (hip = ikWorld[biRoot]*ikHip[fi]). This gives ~0.55 block reach (foot planted) + natural body bob + KNEE BEND (the 2-bone IK bends the split shin to reach — also satisfies user's 'bend knees when walking' ask). biRoot index added to reloadRig. Builds clean. CAVEAT/MATH: 4.5px cap = 0.26 block bob = ~14% of height = BOUNCY (physical cost of planting feet at MC super-human speed 4.7blk/s on 0.69blk legs; less bob cap -> feet slip sooner). AWAITING USER VERIFY + TUNE: (1) knee bend direction (pole (0,0,-1); flip to +1 if backward); (2) bob amount (cap 4.5px — lower if too bouncy, accept more slip); (3) cadence frantic? -> raise kStride 2.0 or lower walk speed; (4) pops on plant/lift -> weight ease dt*15 or foot_up timing. Feet now plant + knees bend + body bobs + no freeze on stop.

**FOOT-LOCK IK REVERTED (2026-07-12, Release BUILT CLEAN) — user rejected the approach.** User: "you are hardcoding the hips to go down instead [of] using the whole animation system" + the pelvis-drop clipped the body through the ground + the walk (knee bends timed to stride, body passing over the bent leg then extending) should be AUTHORED, not procedural. "later when you make the animator program i get the animation clean." REMOVED from main.cpp: the whole foot-lock IK block (pins/weights/pelvis-drop/solveTwoBoneIK application), FootLock state, biRoot/ikLeg/ikShin/ikHip/ikFoot indices, FootIK.h include. KEPT: anim/FootIK.{h,cpp} module + CMake entry (unused now, reusable for mounts/creatures, doc FUTURE(ik)); foot_down/foot_up events in loco clips (A6 footsteps still fire); the A6 event stderr print. Authored clips now drive the whole body cleanly (loco_walk has placeholder knee bends via shins). Skating back to vanilla/distance-driven level (accepted; user authors clean walk later). LESSON: don't procedurally hack body motion in main — author it in clips; body knee-bend/bob belongs in the animation. NEXT DIRECTION: user wants "the animator program" for clean authoring — clarify scope (Blockbench authoring works TODAY + exports our import_bedrock format; vs T1 anim_studio custom tool = preview + CS2-style timeline debugger over the snapshot ring; vs full from-scratch keyframe editor = rejected early as months for a worse Blockbench). USER CHOSE: Blockbench for authoring + I build anim_studio (T1) preview/debug tool.

**T1 anim_studio TOOL — CLIP PREVIEW DONE (2026-07-12, Release BUILT + SMOKE-TESTED OK on 780M).** First standalone dev tool (the A0 tools/ split pays off). tools/anim_studio/main.cpp + tools/CMakeLists.txt target (add_executable anim_studio, links mc_engine, POST_BUILD copies shaders+assets next to it). Standalone Vulkan app (~430 lines): reuses engine (Window/VkContext/Swapchain/ModelRenderer/UIRenderer/Font/Rig/Pose/AnimClip/buildPlayerRigMeshes) but drives its OWN minimal frame loop (the game Renderer is world-coupled) — own cmd pool/buffers[2], sync (imageAvail[2], renderDone[per-image], inFlight[2]), camera UBO[2]{view,proj}, barrier helper, dynamic-rendering pass (color+depth) recording model.record(pose)+ui.recordDraw, acquire/submit2/present, resize-recreate. Loads rig (steve) + all assets/anim/clips/*.clip.json + a default wide skin. FEATURES: orbit camera (left-drag yaw/pitch, wheel zoom, R reset), playback (Space play/pause, Left/Right scrub, [ ] prev/next clip), on-screen timeline (progress bar + playhead + clip name/time/state via Font). Model at origin scale 1.85/32, camera orbits (0,0.95,0). Smoke test: launched, Vulkan init OK, loaded 9 clips, ran loop no crash/no validation errors (exit 124=timeout). AWAITING USER: run build\bin\Release\anim_studio.exe, orbit + scrub clips (esp. loco_walk to author-check knee bends). NEXT for T1: the CS2/GTA TIMELINE DEBUGGER (load player.graph.json via AnimGraph, drive it with mock blackboard sliders, draw per-layer state bars + events + weights over time from the snapshot ring) — the introspection half. Also possible: side/angle default view better for locomotion; hot-reload clips on file change; render to window at full size (currently orbit works). Runtime A1-A6 + tool T1a complete; A7 (retire PlayerAnimator) + graph-debugger remain.

**T1 anim_studio GRAPH TIMELINE DEBUGGER DONE (2026-07-12, Release+Debug BUILT + smoke-tested OK).** The CS2/GTA-style introspection half (user's founding inspiration). AnimGraph: added history(out) [snapshot ring oldest->newest] + layerNames(out). anim_studio: TAB toggles clip-preview <-> graph-debugger. Graph mode drives player.graph.json via AnimGraph with a MOCK blackboard: hold W ramps v.speed 0->7.5 (idle->walk->run), Space=jump_pressed (if on-ground), H=wave_pressed, G toggles v.is_on_ground; stride phase = fmod(accumStride/2,1); graph.update each frame -> posed model. TIMELINE UI (bottom, from graph.history() over ~8s window, scrolling right, 'now' playhead): per-layer rows (locomotion, action) with colored state bars (stateColor = FNV-hash name -> 8-color palette, alpha = layer weight) + current state name at right; an 'events' row with yellow ticks where eventCount>0 (footsteps). HUD shows speed + on_ground. So you drive inputs and WATCH states transition + cross-fade + events fire live. Also up/down arrows (not [ ]) switch clips; tool built to BOTH build/bin/Debug + build/bin/Release. Smoke test: graph loaded 2 layers/8 clips, no crash. Runtime A1-A6 + T1 (preview + debugger) COMPLETE. AWAITING USER: run anim_studio, Tab to graph mode, hold W (see idle->walk->run bars), Space (jump bars), H (wave on action layer), watch the timeline. NEXT: A7 (retire legacy PlayerAnimator, first-person via graph) is the last runtime milestone; or polish (hot-reload clips, side default view, real footstep sound/decals wired to foot_down).
